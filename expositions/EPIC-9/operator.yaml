schema_version: 1
spec_id: EPIC-9
audience: operator
title: 'Identity UX: auto-acquire on init, string node ids, per-user preferences'
summary: Three changes that together make node identity feel like a personal touch instead of CAS-loop bookkeeping. Discussed in the 2026-05-09 walkthrough strategy thread.
rationale: 'Provides implementation and governance for ''Identity UX: auto-acquire on init, string node ids, per-user preferences''.'
key_constraints:
- '`aida init` always emits a `nodes.toml` entry for the current clone'
- Node ids accept `[A-Za-z][A-Za-z0-9_-]*` (alphanumeric + dash/underscore, must start with letter)
- A user with `preferred_node_id = "JM"` in prefs gets it on init when free
- 'Two clones on the same host with the same preferred id: first wins, second falls back with a clear message'
- '`aida node list` shows the string ids verbatim'
- Merge-gate still produces short global ids (FR-15) — node-id strings only appear in the node-aware form
- Migrating existing `nodes.toml` entries to string form. Numeric stays valid.
tradeoffs: []
open_questions: []
acceptance_criteria_citations:
- criterion_id: AC-1
  plain_summary: '`aida init` always emits a `nodes.toml` entry for the current clone'
- criterion_id: AC-2
  plain_summary: Node ids accept `[A-Za-z][A-Za-z0-9_-]*` (alphanumeric + dash/underscore, must start with letter)
- criterion_id: AC-3
  plain_summary: A user with `preferred_node_id = "JM"` in prefs gets it on init when free
- criterion_id: AC-4
  plain_summary: 'Two clones on the same host with the same preferred id: first wins, second falls back with a clear message'
- criterion_id: AC-5
  plain_summary: '`aida node list` shows the string ids verbatim'
- criterion_id: AC-6
  plain_summary: Merge-gate still produces short global ids (FR-15) — node-id strings only appear in the node-aware form
- criterion_id: AC-7
  plain_summary: Migrating existing `nodes.toml` entries to string form. Numeric stays valid.
provenance:
  generator: template-extract
  generated_at: 2026-09-22T16:52:31.232411329+00:00
  source_hash: sha256:26841f8ee995d5fcd15e03477759ce98874a7de194b7b659ba214c8eb4e20ae4
  closure_spec_ids: []
audit:
  evaluator: mechanical-audit
  evaluated_at: 2026-09-22T16:52:31.232683725+00:00
  readability_score: 0.88
  jargon_saturation_score: 0.0
  constraint_preservation_score: 0.95
  deterministic_checks_passed: true
  needs_revision: false
  findings:
  - 'Advisory: canonical spec emphasizes CI invariants not mentioned in exposition'
