schema_version: 1
spec_id: STORY-1424
audience: operator
title: Let an acceptance criterion carry an executable check the reviewer RUNS instead of reads — graded, never a hard rule
summary: 'GOAL: shrink what the review phase has to JUDGE, by letting individual acceptance criteria carry a command the reviewer executes. Not to make every criterion executable — see the non-goal.'
rationale: Provides implementation and governance for 'Let an acceptance criterion carry an executable check the reviewer RUNS instead of reads — graded, never a hard rule'.
key_constraints:
- A criterion may be prose OR carry a check. Mixed is the expected normal case, not a degraded one.
- At review, every criterion carrying a check is RUN. Its verdict is a deterministic pass/fail with the exit code and output captured.
- The LLM reviewer is then asked to judge ONLY the remainder. Its prompt names which criteria were machine-verified and excludes them.
- Value is proportional. Four executable criteria out of six means the judge decides two instead of six. There is no threshold to cross and no minimum ratio.
- A spec with zero executable criteria behaves EXACTLY as today. No new refusal, no new gate, nothing blocks on adoption.
- A criterion carrying a check is executed during the review phase; its pass/fail and captured output appear in the review record.
- The reviewer prompt lists machine-verified criteria as already settled and asks for judgment only on the remainder.
- A spec with no executable criteria produces byte-identical review behaviour to today.
- A machine-verified result records the sha it was produced at.
- No surface refuses, warns, scores or nags about a spec whose criteria are all prose.
tradeoffs: []
open_questions: []
acceptance_criteria_citations:
- criterion_id: AC-1
  plain_summary: A criterion may be prose OR carry a check. Mixed is the expected normal case, not a degraded one.
- criterion_id: AC-2
  plain_summary: At review, every criterion carrying a check is RUN. Its verdict is a deterministic pass/fail with the exit code and output captured.
- criterion_id: AC-3
  plain_summary: The LLM reviewer is then asked to judge ONLY the remainder. Its prompt names which criteria were machine-verified and excludes them.
- criterion_id: AC-4
  plain_summary: Value is proportional. Four executable criteria out of six means the judge decides two instead of six. There is no threshold to cross and no minimum ratio.
- criterion_id: AC-5
  plain_summary: A spec with zero executable criteria behaves EXACTLY as today. No new refusal, no new gate, nothing blocks on adoption.
- criterion_id: AC-6
  plain_summary: A criterion carrying a check is executed during the review phase; its pass/fail and captured output appear in the review record.
- criterion_id: AC-7
  plain_summary: The reviewer prompt lists machine-verified criteria as already settled and asks for judgment only on the remainder.
- criterion_id: AC-8
  plain_summary: A spec with no executable criteria produces byte-identical review behaviour to today.
- criterion_id: AC-9
  plain_summary: A machine-verified result records the sha it was produced at.
- criterion_id: AC-10
  plain_summary: No surface refuses, warns, scores or nags about a spec whose criteria are all prose.
provenance:
  generator: template-extract
  generated_at: 2026-09-22T16:52:37.268346185+00:00
  source_hash: sha256:6886c6952e7de366451bd5d1d29dcdce7f8d1a2afb50174c72db893f9f715c9f
  closure_spec_ids: []
audit:
  evaluator: mechanical-audit
  evaluated_at: 2026-09-22T16:52:37.268843562+00:00
  readability_score: 0.88
  jargon_saturation_score: 0.0
  constraint_preservation_score: 0.95
  deterministic_checks_passed: true
  needs_revision: false
  findings: []
