{
  "cli": {
    "name": "AIDA CLI (human-formatted: emoji + tables)",
    "surface": "cli",
    "mcp": false,
    "allowed_tools": ["Bash", "Read", "Grep", "Glob"],
    "disallowed_tools": [],
    "env": { "AIDA_AGENT_OUTPUT": "0" },
    "preamble": "# Tools\n\nYou are working inside an AIDA project. AIDA tracks requirements/specs and a work queue. The `aida` CLI is installed. Use it via the Bash tool for all requirement, queue, graph, status, and findings operations. Run `aida --help` or `aida <command> --help` to discover subcommands and flags. Do NOT use any MCP tools. Examples:\n- `aida queue next`  / `aida queue list`\n- `aida show <SPEC-ID>` / `aida graph <SPEC-ID> --blocked-by`\n- `aida status` / `aida list`\n- `aida findings list`\n- `aida add --title \"...\" --type task --description \"...\"`\n\nAnswer the user's question, then stop."
  },
  "mcp": {
    "name": "AIDA MCP tools (aida mcp-serve, loaded upfront)",
    "surface": "mcp",
    "mcp": true,
    "allowed_tools": ["mcp__aida", "Read"],
    "disallowed_tools": ["ToolSearch", "Bash"],
    "preamble": "# Tools\n\nYou are working inside an AIDA project. AIDA tracks requirements/specs and a work queue, exposed through a registered MCP server named `aida`. Use the AIDA MCP tools (e.g. `queue_next`, `show_requirement`, `query_graph`, `status_unified`, `list_findings`, `add_requirement`) for all requirement, queue, graph, status, and findings operations.\n\nIMPORTANT: Do NOT use the `aida` CLI or any shell command. You must use the AIDA MCP tools exclusively.\n\nAnswer the user's question, then stop."
  },
  "mcp-toolsearch": {
    "name": "AIDA MCP tools (aida mcp-serve, ToolSearch / on-demand schema loading)",
    "surface": "mcp-toolsearch",
    "mcp": true,
    "allowed_tools": ["mcp__aida", "Read", "ToolSearch"],
    "disallowed_tools": ["Bash"],
    "preamble": "# Tools\n\nYou are working inside an AIDA project. AIDA tracks requirements/specs and a work queue, exposed through a registered MCP server named `aida`. The AIDA MCP tools are DEFERRED: their schemas are not all loaded upfront. Use the `ToolSearch` tool to find and load the AIDA MCP tool(s) you need (search by intent, e.g. 'queue next', 'show requirement', 'graph blocked-by', 'project status', 'list findings', 'add requirement'), then call the loaded `mcp__aida__*` tool. This is how a real MCP client loads schemas on demand rather than paying for all of them upfront.\n\nIMPORTANT: Do NOT use the `aida` CLI or any shell command. You must use the AIDA MCP tools (loaded via ToolSearch) exclusively.\n\nAnswer the user's question, then stop."
  },
  "toon": {
    "name": "AIDA token-efficient CLI / TOON mode (AIDA_AGENT_OUTPUT, TASK-964)",
    "surface": "toon",
    "mcp": false,
    "allowed_tools": ["Bash", "Read", "Grep", "Glob"],
    "disallowed_tools": [],
    "env": { "AIDA_AGENT_OUTPUT": "1" },
    "preamble": "# Tools\n\nYou are working inside an AIDA project. AIDA tracks requirements/specs and a work queue. The `aida` CLI is installed and runs in its token-efficient AGENT output mode (TOON: a compact tabular encoding — a `name[N]{fields}:` header then one comma-joined line per row, no emoji/tables/JSON chrome; single specs render as `key: value` head fields plus a relationships table). Use `aida` via the Bash tool for all requirement, queue, graph, status, and findings operations. `aida list` defaults to a minimal id/title/status/type schema; pass `--fields a,b,c` to widen it. Run `aida <command> --help` to discover flags. Do NOT use any MCP tools. Examples:\n- `aida queue list` / `aida queue next`\n- `aida show <SPEC-ID>` / `aida graph <SPEC-ID> --blocked-by`\n- `aida status` / `aida list` / `aida list --fields id,title,status,priority,tags`\n- `aida findings list`\n- `aida add --title \"...\" --type task --description \"...\"`\n\nAnswer the user's question, then stop."
  }
}
