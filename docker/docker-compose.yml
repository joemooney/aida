version: "3.9"

services:
  traefik:
    image: docker.io/traefik:v3.2
    container_name: traefik
    restart: unless-stopped

    command:
      - --providers.docker=true
      - --providers.docker.exposedbydefault=false
      - --providers.docker.endpoint=unix:///var/run/docker.sock

      - --entrypoints.web.address=:80
      - --entrypoints.websecure.address=:443

      # Let's Encrypt via Cloudflare DNS-01
      - --certificatesresolvers.cloudflare.acme.email=you@joemooney.com
      - --certificatesresolvers.cloudflare.acme.storage=/letsencrypt/acme.json
      - --certificatesresolvers.cloudflare.acme.dnschallenge=true
      - --certificatesresolvers.cloudflare.acme.dnschallenge.provider=cloudflare

      # (optional) logs
      #- --log.level=INFO
      - --log.level=DEBUG

    ports:
      - "80:80"
      - "443:443"

    environment:
      # Traefik reads the token from the secret file
      CF_DNS_API_TOKEN_FILE: /run/secrets/cloudflare_api_token

    secrets:
      - cloudflare_api_token

    volumes:
      #docker - /var/run/docker.sock:/var/run/docker.sock:ro
      - traefik_letsencrypt:/letsencrypt
      #podman
      - ${XDG_RUNTIME_DIR}/podman/podman.sock:/var/run/docker.sock:ro

    networks:
      - proxy


  # 🔐 Dummy service to trigger wildcard cert issuance
  wildcard:
    image: docker.io/library/alpine:3.19
    container_name: traefik-wildcard
    command: ["sleep", "infinity"]
    networks:
      - proxy
    labels:
      - traefik.enable=true
      - traefik.http.routers.wildcard.rule=Host(`wildcard.joemooney.com`)
      - traefik.http.routers.wildcard.entrypoints=websecure
      - traefik.http.routers.wildcard.tls=true
      - traefik.http.routers.wildcard.tls.certresolver=cloudflare
      - traefik.http.routers.wildcard.tls.domains[0].main=joemooney.com
      - traefik.http.routers.wildcard.tls.domains[0].sans=*.joemooney.com


  gitlab:
    image: docker.io/gitlab/gitlab-ce:18.7.0-ce.0
    container_name: gitlab
    restart: unless-stopped
    shm_size: "256m"

    hostname: gitlab.joemooney.com

    # Git over SSH (direct)
    ports:
      - "2222:22"

    environment:
      GITLAB_OMNIBUS_CONFIG: |
        external_url 'https://gitlab.joemooney.com'
        gitlab_rails['gitlab_shell_ssh_port'] = 2222

        # GitLab's bundled nginx listens HTTP internally; Traefik terminates TLS
        nginx['listen_port'] = 80
        nginx['listen_https'] = false

    volumes:
      - gitlab_config:/etc/gitlab
      - gitlab_logs:/var/log/gitlab
      - gitlab_data:/var/opt/gitlab

    networks:
      - proxy

    labels:
      - traefik.enable=true

      # HTTPS router
      - traefik.http.routers.gitlab.rule=Host(`gitlab.joemooney.com`)
      - traefik.http.routers.gitlab.entrypoints=websecure
      - traefik.http.routers.gitlab.tls=true
      - traefik.http.routers.gitlab.tls.certresolver=cloudflare

      # Service port inside container
      - traefik.http.services.gitlab.loadbalancer.server.port=80

      # HTTP -> HTTPS redirect
      - traefik.http.routers.gitlab-http.rule=Host(`gitlab.joemooney.com`)
      - traefik.http.routers.gitlab-http.entrypoints=web
      - traefik.http.routers.gitlab-http.middlewares=gitlab-https-redirect
      - traefik.http.middlewares.gitlab-https-redirect.redirectscheme.scheme=https


  postgres:
    image: docker.io/library/postgres:15-alpine
    container_name: postgres
    restart: unless-stopped

    environment:
      POSTGRES_DB: aida
      POSTGRES_USER_FILE: /run/secrets/pg_user
      POSTGRES_PASSWORD_FILE: /run/secrets/pg_password

    secrets:
      - pg_user
      - pg_password

    volumes:
      - pg_data:/var/lib/postgresql/data

    networks:
      - internal


  pgadmin:
    image: docker.io/dpage/pgadmin4:8
    container_name: pgadmin
    restart: unless-stopped

    environment:
      PGADMIN_DEFAULT_EMAIL: admin@joemooney.com
      PGADMIN_DEFAULT_PASSWORD_FILE: /run/secrets/pgadmin_password

    secrets:
      - pgadmin_password

    volumes:
      - pgadmin_data:/var/lib/pgadmin

    networks:
      - proxy
      - internal

    labels:
      - traefik.enable=true
      - traefik.http.routers.pgadmin.rule=Host(`pgadmin.joemooney.com`)
      - traefik.http.routers.pgadmin.entrypoints=websecure
      - traefik.http.routers.pgadmin.tls=true
      - traefik.http.routers.pgadmin.tls.certresolver=cloudflare

      # 🔒 IP allowlist: public IP + home LAN
      - traefik.http.middlewares.pgadmin-ip.ipallowlist.sourcerange=144.208.111.126/32,192.168.0.0/16
      - traefik.http.routers.pgadmin.middlewares=pgadmin-ip

      - traefik.http.services.pgadmin.loadbalancer.server.port=80


  # ═══════════════════════════════════════════════════════════════════════════
  # Cloudflare Tunnel - Secure public access without opening ports
  # ═══════════════════════════════════════════════════════════════════════════

  cloudflared:
    image: cloudflare/cloudflared:latest
    container_name: cloudflared
    restart: unless-stopped
    command: tunnel run
    environment:
      TUNNEL_TOKEN: ${CLOUDFLARE_TUNNEL_TOKEN}
    networks:
      - proxy
    depends_on:
      - gitlab
      - aida-server
      - aida-web


  # ═══════════════════════════════════════════════════════════════════════════
  # AIDA - AI Design Assistant
  # ═══════════════════════════════════════════════════════════════════════════

  aida-server:
    build:
      context: ..
      dockerfile: docker/Dockerfile.server
    image: aida-server:latest
    container_name: aida-server
    restart: unless-stopped

    environment:
      RUST_LOG: info

    secrets:
      - pg_user
      - pg_password
      - aida_database_url

    volumes:
      - aida_data:/data

    networks:
      - proxy
      - internal

    depends_on:
      - postgres

    labels:
      - traefik.enable=true

      # gRPC-Web API endpoint (for WASM client)
      - traefik.http.routers.aida-api.rule=Host(`api.aida.joemooney.com`)
      - traefik.http.routers.aida-api.entrypoints=websecure
      - traefik.http.routers.aida-api.tls=true
      - traefik.http.routers.aida-api.tls.certresolver=cloudflare
      - traefik.http.services.aida-api.loadbalancer.server.port=8080

      # CORS headers for gRPC-Web
      - traefik.http.middlewares.aida-cors.headers.accesscontrolallowmethods=GET,POST,OPTIONS
      - traefik.http.middlewares.aida-cors.headers.accesscontrolallowheaders=Content-Type,x-grpc-web,x-user-agent
      - traefik.http.middlewares.aida-cors.headers.accesscontrolalloworiginlist=https://aida.joemooney.com
      - traefik.http.middlewares.aida-cors.headers.accesscontrolmaxage=86400
      - traefik.http.routers.aida-api.middlewares=aida-cors

      # 🔒 IP allowlist (same as pgadmin for now)
      #- traefik.http.middlewares.aida-ip.ipallowlist.sourcerange=144.208.111.126/32,192.168.0.0/16
      #- traefik.http.routers.aida-api.middlewares=aida-ip,aida-cors


  aida-web:
    build:
      context: ..
      dockerfile: docker/Dockerfile.web
    image: aida-web:latest
    container_name: aida-web
    restart: unless-stopped

    networks:
      - proxy

    depends_on:
      - aida-server

    labels:
      - traefik.enable=true

      # WASM client
      - traefik.http.routers.aida-web.rule=Host(`aida.joemooney.com`)
      - traefik.http.routers.aida-web.entrypoints=websecure
      - traefik.http.routers.aida-web.tls=true
      - traefik.http.routers.aida-web.tls.certresolver=cloudflare
      - traefik.http.services.aida-web.loadbalancer.server.port=80

      # HTTP -> HTTPS redirect
      - traefik.http.routers.aida-web-http.rule=Host(`aida.joemooney.com`)
      - traefik.http.routers.aida-web-http.entrypoints=web
      - traefik.http.routers.aida-web-http.middlewares=aida-https-redirect
      - traefik.http.middlewares.aida-https-redirect.redirectscheme.scheme=https

      # 🔒 IP allowlist (same as pgadmin for now)
      #- traefik.http.middlewares.aida-web-ip.ipallowlist.sourcerange=144.208.111.126/32,192.168.0.0/16
      #- traefik.http.routers.aida-web.middlewares=aida-web-ip


networks:
  proxy:
    name: proxy
  internal:
    name: internal
    internal: true


volumes:
  traefik_letsencrypt:
  gitlab_config:
  gitlab_logs:
  gitlab_data:
  pg_data:
  pgadmin_data:
  aida_data:


secrets:
  cloudflare_api_token:
    file: ./secrets/cloudflare_api_token.txt
  pg_user:
    file: ./secrets/pg_user.txt
  pg_password:
    file: ./secrets/pg_password.txt
  pgadmin_password:
    file: ./secrets/pgadmin_password.txt
  aida_database_url:
    file: ./secrets/aida_database_url.txt

