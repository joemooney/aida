# EPIC-28 — Dependency-aware autonomous drain

(plan content saved separately due to size; see PR-273 + chat 2026-05-23 for canonical version)

Date: 2026-05-24
Specs: EPIC-28 (plus 5 child STORYs filed in step 1: STORY-446 + STORY-447 + STORY-448 + 2 more deferred)
Status: Plan
Complexity: M (first vertical — STORY 3 + STORY 4) / L (full EPIC, multi-PR)

## Summary

Today aida queue work --batch X --auto-complete collapses on first phase failure. EPIC-28 turns the drain into "shelve, skip dependents, continue". First vertical: STORY 3 (shelve on failure) + STORY 4 (continue past shelved + dependency-aware skip via existing pickability gate).

## Key decisions (8 total, see chat 2026-05-23 for full text)

- D1: Reuse NeedsAttention, no new Shelved status
- D2: Add Requirement.failure_reason sibling to attention_reason (preserves PuntCategory obstacle-shape invariant)
- D3: FailureReason.phase as string slug, not enum (keeps aida-core orchestrator-free)
- D4: Reuse .aida/punts.jsonl with resolution_path = "shelved-by-failure"
- D5: New BatchDrainOutcome::DrainedWithShelved variant + exit code 2
- D6: --max-failures N (default 5) safety cap, per-batch
- D7: resolve_batch_members filters NeedsAttention (closes latent punt re-pickup loop incidentally)
- D8: STORY 1 (--blocked-by flag) deferred to separate PR

## Critical Files

- aida-core/src/models.rs (FailureReason struct + field)
- aida-cli/src/punt.rs (append_failure_to_ledger helper + RESOLUTION_SHELVED_FAILURE const)
- aida-cli/src/auto_complete.rs (BatchDrainOutcome::DrainedWithShelved + drain_batch loop rewrite)
- aida-cli/src/main.rs (PhaseDriver::shelve_on_failure trait method + resolve_batch_members filter + summary + findings render)
- aida-cli/src/cli.rs (--max-failures flag)
- docs/autonomous-drain.md (new section)
- CLAUDE.md (--auto-complete paragraph extended)
- OVERVIEW.md (autonomy section)

## Followups (8 filed as TASK)

- aida punts --include-shelved filter
- --batches chain-wide max-failures cap
- The 3 deferred child STORYs (1, 2, 5)
- aida findings list --shelved-only / --punts-only filters
- Telemetry: shelved count -> aida usage
- Cluster-of-shelved auto-tag

## Reusable helpers (do not reimplement)

aida_core::pickability::pickability + pickability_reason_label, forbidden_attention_transition, auto_complete::recovery_hint, punt::append_to_ledger, main::resolve_batch_members, record_role_activity, is_terminal_status.

(Full plan text is saved as a separate review artifact; this stub records the architectural decisions for future implementers. Generated by web /ultraplan 2026-05-23.)
