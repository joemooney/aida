# STORY-443 — Reshape docs/aida-* namespace

Date: 2026-05-24 · Specs: STORY-443 · Status: Plan · Complexity: M (path-only refactor, ~30 files, no runtime behavior change)

## Summary

Discipline pack moves docs/aida-discipline/ → docs/aida/discipline/. Master templates move aida-core/templates/docs/aida-discipline/ → aida-core/templates/docs/aida/discipline/. Coexists with aida docs build's existing docs/aida/ graph projection (README.md, 00-constitution.md, etc.) because discipline lands in its own subdirectory.

The "no real users yet" window is the right moment to lock in the cleaner shape with one decisive cut (no deprecation shim).

## Key decisions (6 total)

1. Discipline pack at docs/aida/discipline/ (subdir, not flat) — avoids README.md collision with aida docs build
2. Master templates mirror runtime path (aida-core/templates/docs/aida/discipline/) — keeps build.rs embed_directory symmetric
3. No deprecation shim, no fallback read of old path — one decisive cut
4. git mv for both physical moves so rename history survives git log --follow
5. Dated artifacts stay frozen (CHANGELOG, docs/plans/YYYY-MM-DD-*, spec comments, commit messages); living surfaces rewritten
6. Pre-commit hook embedded twice (Rust source + template) — both update in lockstep

## Five-phase build order

- Phase A: Physical move of master templates (git mv 9 files + fix self-refs)
- Phase B: Build & scaffold pipeline (build.rs embed_directory, ensure_discipline_pack_scaffold, init output, story_255 tests, claude_md::discipline_section, hooks::generate_aida_pre_commit_hook + template)
- Phase C: Template surfaces (skills/aida-{implement,pickup,pr,review,advise}.md, commands/aida-advise.md, docs/agents/cross-agent-onboarding.md, memories)
- Phase D: Dogfooded repo content (git mv 3 files + rewrite living guidance + CLAUDE.md)
- Phase E: FROZEN (don't touch) — CHANGELOG.md, docs/plans/*, spec comments, orphan-store YAML

## Critical Files

aida-core/build.rs, templates/docs/aida-discipline/ → templates/docs/aida/discipline/, templates/hooks/aida-pre-commit.sh, templates/skills/aida-*.md, templates/commands/aida-advise.md, templates/docs/agents/cross-agent-onboarding.md, templates/memories/{feedback_dated_artifacts_immutable,MEMORY}.md, src/scaffolding/{claude_md,hooks}.rs, aida-cli/src/main.rs (ensure_discipline_pack_scaffold + init output + story_255 tests), docs/aida-discipline/ → docs/aida/discipline/, docs/{agents,architecture/autonomy-and-escalation,positioning/vs-claude-code-subagents,lifecycle,autonomous-drain,multi-advisor-coordination}.md, CLAUDE.md

## Reusable helpers

ensure_discipline_pack_scaffold (single runtime touch-point), embed_directory (generic dir-embedder; only path strings change), Scaffolder::discipline_section (single source for CLAUDE.md heredoc), Makefile sync-templates (only operates on .claude/skills + commands; no change needed).

## Risks

1. aida docs build README collision (avoided by subdir; add note above ensure_discipline_pack_scaffold)
2. Pre-commit hook embedded twice (must drift together; smoke test catches)
3. .claude/ symlinks point at templates/skills + commands; unchanged
4. aida plan verify on dated plans will false-positive (out of scope; followup TASK)
5. Dogfooded docs/aida-discipline/ has only 3 files; aida init --force lands the missing 6
6. HashMap iteration order for EMBEDDED_TEMPLATES (prefix unique post-move; smoke test asserts 9 files)

## New test

discipline_pack_lands_under_docs_aida_namespace — scaffolds + asserts docs/aida/discipline/README.md exists AND docs/aida-discipline/ is NOT created (guards fallback regression).

## Composes with

STORY-255 (introduced the pack), FR-1-077 (aida docs build — the existing writer at docs/aida/), TASK-510 (sibling init UX), STORY-441 (same "no users, do the right shape now" theme), feedback_dated_artifacts_immutable (governs which artifacts freeze vs rewrite), EPIC-24 (doc requirement type — other AIDA-owned docs surface).

(Full plan generated by web /ultraplan 2026-05-23 evening; canonical text in session chat record. PR-268 +84/-62 is the implementation.)
