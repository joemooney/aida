# `--format json` conformance audit

<!-- generated by scripts/generate-format-json-audit.py; trace:BUG-1502 -->

`--format` is a global clap option, so every command path below accepts it
syntactically. A command honours JSON only when its selected leaf declares a
dedicated `--json` projection. The runtime rejects all other leaves before
dispatch; they can never silently fall back to human or TOON output.

`--format json` and `--json` are one contract and must emit the same JSON
document wherever both spellings exist. `JSON honoured` rows are executed by
the BUG-1502 end-to-end test. `COULD NOT VERIFY` is deliberately distinct from
failure: the leaf declares JSON, but its invocation needs destructive input,
credentials, live processes, or another external condition not available to the
hermetic audit. `JSON not honoured` means unsupported and explicitly rejected.

| Command | Outcome | Probe / disposition |
|---|---|---|
| `aida do` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida derisk` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida add` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida fasttrack` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida fasttrack status` | JSON honoured | fasttrack status |
| `aida list` | JSON honoured | list --limit 1 |
| `aida show` | JSON honoured | show <SPEC> --no-git |
| `aida approvals` | COULD NOT VERIFY | Dedicated --json exists; needs command-specific state or external service |
| `aida approvals add` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida graph` | COULD NOT VERIFY | Dedicated --json exists; needs command-specific state or external service |
| `aida graph blocked-by` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida graph blocks` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida graph tree` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida graph impact` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida criteria` | COULD NOT VERIFY | Dedicated --json exists; needs command-specific state or external service |
| `aida harvest` | COULD NOT VERIFY | Dedicated --json exists; needs command-specific state or external service |
| `aida reconstitute` | COULD NOT VERIFY | Dedicated --json exists; needs command-specific state or external service |
| `aida done` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida edit` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida del` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida findings` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida findings add` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida findings recur` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida findings classes` | COULD NOT VERIFY | Dedicated --json exists; needs command-specific state or external service |
| `aida findings list` | JSON honoured | findings list |
| `aida findings dismiss` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida findings promote` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida findings calibration` | COULD NOT VERIFY | Dedicated --json exists; needs command-specific state or external service |
| `aida findings calibration annotate` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida questions` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida questions list` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida questions sweep` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida questions ask` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida questions clarify` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida questions remedy` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida questions answer` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida decide` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida research` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida advisor` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida advisor register` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida advisor unregister` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida advisor watch` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida advisor status` | COULD NOT VERIFY | Dedicated --json exists; needs command-specific state or external service |
| `aida advisor schedule` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida advisor schedule add` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida advisor schedule list` | COULD NOT VERIFY | Dedicated --json exists; needs command-specific state or external service |
| `aida advisor schedule enable` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida advisor schedule disable` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida advisor schedule remove` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida advisor schedule run` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida advisor handoff` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida punt` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida punts` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida punts list` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida punts analyze` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida punts resolve` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida punts dismiss` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida punts escalate` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida punts read` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida punts promote` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida autonomy` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida autonomy calibration` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida autonomy calibration mismatches` | COULD NOT VERIFY | Dedicated --json exists; needs command-specific state or external service |
| `aida autonomy report` | COULD NOT VERIFY | Dedicated --json exists; needs command-specific state or external service |
| `aida no-human` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida no-human acknowledge` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida no-human revoke` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida no-human status` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida load` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida load queue` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida load backlog` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida load report` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida load calibration` | COULD NOT VERIFY | Dedicated --json exists; needs command-specific state or external service |
| `aida archive` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida unarchive` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida defer` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida undefer` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida claim` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida unclaim` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida assign` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida unassign` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida feature` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida feature add` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida feature list` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida feature show` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida feature edit` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida db` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida db path` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida db migrate` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida db info` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida db sync` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida db export-git` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida db merge-gate` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida db retire-legacy-ids` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida db workspace-init` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida db status` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida db block` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida db block claim` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida db block list` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida db block status` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida db block verify` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida db reconcile-status` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida db check` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida cache` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida cache rebuild` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida cache status` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida cache verify` | COULD NOT VERIFY | Dedicated --json exists; needs command-specific state or external service |
| `aida record` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida record list` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida record prune` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida protocol` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida protocol list` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida protocol show` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida protocol seed` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida sandbox` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida sandbox create` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida sandbox reset` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida sandbox destroy` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida sandbox path` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida mailbox` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida mailbox latency` | COULD NOT VERIFY | Dedicated --json exists; needs command-specific state or external service |
| `aida mailbox send` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida mailbox inbox` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida mailbox notice` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida mailbox list` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida mailbox read` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida mailbox retract` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida mailbox delete` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida mailbox archive` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida mailbox gc` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida mailbox thread` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida mailbox sync` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida node` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida node list` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida node show` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida node acquire` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida node set-owner` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida node set-name` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida node release` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida node team` | COULD NOT VERIFY | Dedicated --json exists; needs command-specific state or external service |
| `aida node team set-role` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida node team my-role` | COULD NOT VERIFY | Dedicated --json exists; needs command-specific state or external service |
| `aida node team unset-role` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida node whoami` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida status` | JSON honoured | status <SPEC> |
| `aida awaiting` | COULD NOT VERIFY | Dedicated --json exists; needs command-specific state or external service |
| `aida ps` | JSON honoured | ps |
| `aida merge-lock` | COULD NOT VERIFY | Dedicated --json exists; needs command-specific state or external service |
| `aida merge-hold` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida merge-hold list` | COULD NOT VERIFY | Dedicated --json exists; needs command-specific state or external service |
| `aida merge-hold add` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida merge-hold clear` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida tail` | COULD NOT VERIFY | Dedicated --json exists; needs command-specific state or external service |
| `aida integrate` | COULD NOT VERIFY | Dedicated --json exists; needs command-specific state or external service |
| `aida watch` | COULD NOT VERIFY | Dedicated --json exists; needs command-specific state or external service |
| `aida contract` | COULD NOT VERIFY | Dedicated --json exists; needs command-specific state or external service |
| `aida notify` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida notify check` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida notify test` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida notify status` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida team` | COULD NOT VERIFY | Dedicated --json exists; needs command-specific state or external service |
| `aida team set-role` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida team my-role` | COULD NOT VERIFY | Dedicated --json exists; needs command-specific state or external service |
| `aida team unset-role` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida identity` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida identity link` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida identity list` | COULD NOT VERIFY | Dedicated --json exists; needs command-specific state or external service |
| `aida identity show` | COULD NOT VERIFY | Dedicated --json exists; needs command-specific state or external service |
| `aida usage` | COULD NOT VERIFY | Dedicated --json exists; needs command-specific state or external service |
| `aida usage rebuild` | COULD NOT VERIFY | Dedicated --json exists; needs command-specific state or external service |
| `aida usage show` | COULD NOT VERIFY | Dedicated --json exists; needs command-specific state or external service |
| `aida usage slowest` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida usage unused` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida usage errors` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida usage events` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida usage drains` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida usage health` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida metrics` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida metrics agent-lift` | COULD NOT VERIFY | Dedicated --json exists; needs command-specific state or external service |
| `aida metrics ai-lift` | COULD NOT VERIFY | Dedicated --json exists; needs command-specific state or external service |
| `aida field-study` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida field-study scan` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida field-study report` | COULD NOT VERIFY | Dedicated --json exists; needs command-specific state or external service |
| `aida field-study violations` | COULD NOT VERIFY | Dedicated --json exists; needs command-specific state or external service |
| `aida digest` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida remote` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida remote create` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida remote attach` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida remote status` | COULD NOT VERIFY | Dedicated --json exists; needs command-specific state or external service |
| `aida remote mirror` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida remote mirror-push` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida remote reconcile` | COULD NOT VERIFY | Dedicated --json exists; needs command-specific state or external service |
| `aida push` | COULD NOT VERIFY | Dedicated --json exists; needs command-specific state or external service |
| `aida fetch` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida internal` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida internal advisor-code-gate` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida internal record-no-verify-bypass` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida internal locking-gate` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida internal worktree-scope-gate` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida commit` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida pull` | COULD NOT VERIFY | Dedicated --json exists; needs command-specific state or external service |
| `aida rebase` | COULD NOT VERIFY | Dedicated --json exists; needs command-specific state or external service |
| `aida release` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida release patch` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida release minor` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida release major` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida release check` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida burndown` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida burndown readiness` | COULD NOT VERIFY | Dedicated --json exists; needs command-specific state or external service |
| `aida burndown plan` | COULD NOT VERIFY | Dedicated --json exists; needs command-specific state or external service |
| `aida burndown explain` | COULD NOT VERIFY | Dedicated --json exists; needs command-specific state or external service |
| `aida burndown run` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida burndown status` | COULD NOT VERIFY | Dedicated --json exists; needs command-specific state or external service |
| `aida supervise` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida supervise nudge` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida supervise redrive` | COULD NOT VERIFY | Dedicated --json exists; needs command-specific state or external service |
| `aida supervise watch` | COULD NOT VERIFY | Dedicated --json exists; needs command-specific state or external service |
| `aida autoprogress` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida health` | COULD NOT VERIFY | Dedicated --json exists; needs command-specific state or external service |
| `aida groom` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida why` | COULD NOT VERIFY | Dedicated --json exists; needs command-specific state or external service |
| `aida focus` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida focus clear` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida focus show` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida spec` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida spec dryrun` | COULD NOT VERIFY | Dedicated --json exists; needs command-specific state or external service |
| `aida spec interview` | COULD NOT VERIFY | Dedicated --json exists; needs command-specific state or external service |
| `aida worktree` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida worktree add` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida worktree enter` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida worktree exit` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida worktree list` | COULD NOT VERIFY | Dedicated --json exists; needs command-specific state or external service |
| `aida worktree gc` | COULD NOT VERIFY | Dedicated --json exists; needs command-specific state or external service |
| `aida worktree pool` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida worktree pool status` | COULD NOT VERIFY | Dedicated --json exists; needs command-specific state or external service |
| `aida worktree pool acquire` | COULD NOT VERIFY | Dedicated --json exists; needs command-specific state or external service |
| `aida worktree pool return` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida worktree pool adopt` | COULD NOT VERIFY | Dedicated --json exists; needs command-specific state or external service |
| `aida worktree pool destroy` | COULD NOT VERIFY | Dedicated --json exists; needs command-specific state or external service |
| `aida intent` | COULD NOT VERIFY | Dedicated --json exists; needs command-specific state or external service |
| `aida dev` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida dev activate` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida dev build-guard` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida dev deactivate` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida dev serve` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida dev status` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida dev ps1` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida dev shell-init` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida dev release` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida dev patch` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida doctor` | COULD NOT VERIFY | Dedicated --json exists; needs command-specific state or external service |
| `aida doctor shell-substitution-holes` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida doctor check` | COULD NOT VERIFY | Dedicated --json exists; needs command-specific state or external service |
| `aida doctor heal` | COULD NOT VERIFY | Dedicated --json exists; needs command-specific state or external service |
| `aida doctor migrate-counter-scope` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida doctor repair-stale-blocks` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida doctor scrub-collisions` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida doctor verify-relationships` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida doctor validate-trace-comments` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida doctor fsck` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida doctor convention-check` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida store` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida store status` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida store install-hook` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida store compact` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida store scrub-preview` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida schedule` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida schedule tick` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida schedule run` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida schedule status` | COULD NOT VERIFY | Dedicated --json exists; needs command-specific state or external service |
| `aida schedule list` | COULD NOT VERIFY | Dedicated --json exists; needs command-specific state or external service |
| `aida schedule due` | COULD NOT VERIFY | Dedicated --json exists; needs command-specific state or external service |
| `aida schedule done` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida schedule emit-cron` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida schedule install-cron` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida schedule uninstall-cron` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida role` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida role enter` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida role add` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida role list` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida role show` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida role repair` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida role active` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida role current` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida role end` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida role delete` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida role scaffold` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida role scope` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida role scope set` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida role scope show` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida role scope clear` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida role prompt` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida role prompt set` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida role prompt show` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida role prompt clear` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida session` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida session conversations` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida session resume` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida session focus` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida session send` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida session new` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida session start` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida session harness-worktree-register` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida session harness-worktree-release` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida session pending-approval-set` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida session pending-approval-clear` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida session end` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida session leases` | COULD NOT VERIFY | Dedicated --json exists; needs command-specific state or external service |
| `aida session show` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida session handoff` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida session manifest` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida session manifest write` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida session manifest mark-started` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida session manifest mark-completed` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida session prune` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida session gc` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida session reap` | COULD NOT VERIFY | Dedicated --json exists; needs command-specific state or external service |
| `aida terminal` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida terminal install` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida lock` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida lock acquire` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida lock verify` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida lock release` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida lock status` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida triage` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida triage acquire` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida triage release` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida triage status` | COULD NOT VERIFY | Dedicated --json exists; needs command-specific state or external service |
| `aida ship` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida pr` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida pr auto-queue-review` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida pr rebase` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida pr ship` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida pr hold` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida pr gc` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida orchestrator` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida orchestrator status` | COULD NOT VERIFY | Dedicated --json exists; needs command-specific state or external service |
| `aida zen` | COULD NOT VERIFY | Dedicated --json exists; needs command-specific state or external service |
| `aida zen status` | COULD NOT VERIFY | Dedicated --json exists; needs command-specific state or external service |
| `aida zen finish` | COULD NOT VERIFY | Dedicated --json exists; needs command-specific state or external service |
| `aida zen needs-human` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida drain` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida drain start` | COULD NOT VERIFY | Dedicated --json exists; needs command-specific state or external service |
| `aida drain resume` | COULD NOT VERIFY | Dedicated --json exists; needs command-specific state or external service |
| `aida drain stop` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida drain status` | COULD NOT VERIFY | Dedicated --json exists; needs command-specific state or external service |
| `aida drain clear` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida drain tail` | COULD NOT VERIFY | Dedicated --json exists; needs command-specific state or external service |
| `aida stack` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida stack show` | COULD NOT VERIFY | Dedicated --json exists; needs command-specific state or external service |
| `aida stack list` | COULD NOT VERIFY | Dedicated --json exists; needs command-specific state or external service |
| `aida headless` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida headless tail` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida worker` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida worker directives` | COULD NOT VERIFY | Dedicated --json exists; needs command-specific state or external service |
| `aida worker gc` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida brief` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida brief list` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida brief ack` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida brief read` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida compete` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida agent` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida agent new` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida agent new claude` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida agent new codex` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida agent new antigravity` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida agent register` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida agent ls` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida agent status` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida agent gc` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida agent dispatch-health` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida agent pause` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida agent resume` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida agent describe` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida agent stop` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida agent list-roles` | COULD NOT VERIFY | Dedicated --json exists; needs command-specific state or external service |
| `aida state-snapshot` | COULD NOT VERIFY | Dedicated --json exists; needs command-specific state or external service |
| `aida statusbar` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida statusline` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida statusline title` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida statusline setup` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida away` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida home` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida presence` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida presence away` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida presence home` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida presence status` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida solo` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida whoami` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida _bg-fetch` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida upgrade` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida upgrade check` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida upgrade diff` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida rel` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida rel add` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida rel remove` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida rel list` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida rel-def` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida rel-def list` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida rel-def show` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida rel-def add` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida rel-def edit` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida rel-def remove` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida comment` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida comment add` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida comment list` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida comment edit` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida comment delete` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida config` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida config show` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida config format` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida config numbering` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida config digits` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida config migrate` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida config user` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida config hints` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida config glyph` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida config glyph list` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida config glyph theme` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida config glyph set` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida config glyph unset` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida config glyph reset` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida config permissions` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida config permissions show` | COULD NOT VERIFY | Dedicated --json exists; needs command-specific state or external service |
| `aida config permissions set` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida config menu` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida type` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida type list` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida type add` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida type remove` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida schema` | COULD NOT VERIFY | Dedicated --json exists; needs command-specific state or external service |
| `aida export` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida import` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida user-guide` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida server` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida server status` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida server list` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida server get` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida server ping` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida trace` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida trace add` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida trace list` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida trace remove` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida trace scan` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida trace sweep` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida trace gate` | COULD NOT VERIFY | Dedicated --json exists; needs command-specific state or external service |
| `aida trace coverage` | COULD NOT VERIFY | Dedicated --json exists; needs command-specific state or external service |
| `aida trace check` | COULD NOT VERIFY | Dedicated --json exists; needs command-specific state or external service |
| `aida review` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida review prompt` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida review assemble` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida review record` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida review claim` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida review verdict` | COULD NOT VERIFY | Dedicated --json exists; needs command-specific state or external service |
| `aida review classes` | COULD NOT VERIFY | Dedicated --json exists; needs command-specific state or external service |
| `aida review normalize-shas` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida review stranded` | COULD NOT VERIFY | Dedicated --json exists; needs command-specific state or external service |
| `aida review mode` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida review mode mass-change` | COULD NOT VERIFY | Dedicated --json exists; needs command-specific state or external service |
| `aida report` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida report bug` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida report idea` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida report ai-integration` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida init` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida memories` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida memories check` | COULD NOT VERIFY | Dedicated --json exists; needs command-specific state or external service |
| `aida scaffold` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida scaffold status` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida scaffold preview` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida scaffold apply` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida scaffold extract` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida scaffold codex-prompts` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida scaffold user-instructions` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida scaffold refresh` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida scaffold upgrade` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida scaffold diff` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida gitlab` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida gitlab config` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida gitlab test` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida gitlab list` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida gitlab show` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida gitlab status` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida gitlab labels` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida gitlab refresh` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida gitlab poll` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida github` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida github config` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida github test` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida github list` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida github show` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida github push` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida github pull` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida github sync` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida github labels` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida jira` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida jira config` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida jira test` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida jira list` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida jira show` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida jira push` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida jira sync` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida jira pull` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida mcp-serve` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida mcp` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida mcp register-agent` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida mcp translate` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida mcp skill` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida mcp skill render` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida mcp skill lint` | COULD NOT VERIFY | Dedicated --json exists; needs command-specific state or external service |
| `aida tui` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida docs` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida docs build` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida docs check` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida docs glossary` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida rules` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida rules sync` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida doc` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida doc add` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida doc list` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida doc show` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida doc coverage` | COULD NOT VERIFY | Dedicated --json exists; needs command-specific state or external service |
| `aida doc suggest` | COULD NOT VERIFY | Dedicated --json exists; needs command-specific state or external service |
| `aida grep` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida queue` | JSON honoured | queue (compatibility alias to queue list) |
| `aida queue list` | JSON honoured | queue list |
| `aida queue add` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida queue load` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida queue remove` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida queue move` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida queue clear` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida queue prune` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida queue gc` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida queue next` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida queue advance` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida queue done` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida queue work` | COULD NOT VERIFY | Dedicated --json exists; needs command-specific state or external service |
| `aida queue progress` | JSON honoured | queue progress |
| `aida queue rework` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida queue recover` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida queue integrate` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida backlog` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida backlog list` | COULD NOT VERIFY | Dedicated --json exists; needs command-specific state or external service |
| `aida backlog analyze` | COULD NOT VERIFY | Dedicated --json exists; needs command-specific state or external service |
| `aida backlog groom` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida backlog load` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida autopilot` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida autopilot inspect` | COULD NOT VERIFY | Dedicated --json exists; needs command-specific state or external service |
| `aida autopilot audit` | COULD NOT VERIFY | Dedicated --json exists; needs command-specific state or external service |
| `aida autopilot challenge` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida autopilot executions` | COULD NOT VERIFY | Dedicated --json exists; needs command-specific state or external service |
| `aida autopilot revert` | COULD NOT VERIFY | Dedicated --json exists; needs command-specific state or external service |
| `aida autopilot reindex` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida rework` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida search` | COULD NOT VERIFY | Dedicated --json exists; needs command-specific state or external service |
| `aida history` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida history events` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida lint` | COULD NOT VERIFY | Dedicated --json exists; needs command-specific state or external service |
| `aida lifecycle` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida plan` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida plan verify` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida plan helpers` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida plan promote` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida plan fan-out` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida plan capture` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida plan scan` | COULD NOT VERIFY | Dedicated --json exists; needs command-specific state or external service |
| `aida deps` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida deps sweep` | COULD NOT VERIFY | Dedicated --json exists; needs command-specific state or external service |
| `aida gate` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida gate list` | COULD NOT VERIFY | Dedicated --json exists; needs command-specific state or external service |
| `aida gate show` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida gate run` | COULD NOT VERIFY | Dedicated --json exists; needs command-specific state or external service |
| `aida changelog` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida changelog generate` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida changelog refresh` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida changelog preview` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida manual` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida ultraplan` | COULD NOT VERIFY | Dedicated --json exists; needs command-specific state or external service |
| `aida import-plan` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida goal` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida human` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida human away` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida human home` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida human presence` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida human status` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida human unblock` | COULD NOT VERIFY | Dedicated --json exists; needs command-specific state or external service |
| `aida human answer` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida human decide` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida human review` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida human audit` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida help-all` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida commands` | COULD NOT VERIFY | Dedicated --json exists; needs command-specific state or external service |
| `aida skill` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida skill render` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida skill lint` | COULD NOT VERIFY | Dedicated --json exists; needs command-specific state or external service |
| `aida alias` | COULD NOT VERIFY | Dedicated --json exists; needs command-specific state or external service |
| `aida alias list` | COULD NOT VERIFY | Dedicated --json exists; needs command-specific state or external service |
| `aida alias add` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
| `aida alias remove` | JSON not honoured | Explicitly rejected before dispatch (no local --json contract) |
