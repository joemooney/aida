- user_id: role:implementer
  requirement_id: 01a0b2b5-d13b-7361-8e0a-3bdab7f18cba
  position: -3000
  added_by: role:implementer
  note: |-
    [rework ROUND 3 — one item still open; commits 7de6e89d1 + 88cf4ac91 are already on the PR and do NOT count. Produce a NEW commit, or punt naming the finding you dispute.]
    PR #1916 RequestChanges (drain reviewer, .aida/review-verdicts/PR-1916.json): non-TTY output must be byte-identical to pre-PR. Before this PR, piped `aida list --show-tags` truncated titles to 50 columns and used the fixed table geometry; render_list_table now passes usize::MAX for the title and derives tag/rule widths from content when terminal_width is None, and the new test non_tty_rendering_does_not_truncate_title_or_tags pins that CHANGED behaviour. Fix: when stdout is not a TTY (terminal_width None) reproduce the pre-PR geometry exactly — title_max 50 with --show-tags, usize::MAX without, fixed column widths as before — and confine all terminal-aware sizing to the TTY path. Replace the incorrect test with a regression test that renders piped --show-tags output for a fixture and asserts it equals the pre-PR rendering (capture the expected string from main, not from the branch). Keep the TTY-width tests from round 2.
    Finish with aida queue done BUG-1207.
  added_at: 2026-09-18T07:06:53.096848530Z
  for_role: implementer
- user_id: role:implementer
  requirement_id: 01a0b2bf-39f2-7f91-9f14-69d808dd20fe
  position: -2000
  added_by: role:implementer
  note: |-
    [rework ROUND 2 — one item still open; the previous commit on this branch is already on the PR and does NOT count. Produce a NEW commit, or punt naming the finding you dispute.]
    PR #1917 RequestChanges (drain reviewer, .aida/review-verdicts/PR-1917.json): `aida list --format human --limit 0` is accepted, truncates N matched rows to zero, and prints only "No requirements found." with no disclosure. Handle the empty human-render path: when an explicit --limit truncated a non-empty match set to zero, print the same footer shape as the non-zero case ("0 of N open — truncated by --limit 0" or the renderer's existing count-of-total form) instead of the no-results message; the genuine no-match case keeps "No requirements found.". Add a regression test in bug_1209_list_limit_footer_tests for --limit 0 with N>0 matches (footer discloses N) and for a true empty match (no footer). Consider whether --limit 0 should be rejected by clap (value_parser range 1..) — if you choose rejection instead, say so and test the error text; either is acceptable, silent emptiness is not.
    Finish with aida queue done BUG-1209.
  added_at: 2026-09-18T06:45:51.263097754Z
  for_role: implementer
- user_id: role:implementer
  requirement_id: 01a0b206-0bc8-7a72-928a-9a03e2f22d06
  position: 0
  added_by: role:implementer
  note: |-
    [rework round 4] PR #1913 — the security substance is now correct (advisor gate APPROVED r3 in .aida/review-verdicts/PR-1913-advisor-r3.json); the drain reviewer (PR-1913.json) requested two PARITY items the BUG-1197 acceptance asks for, and the advisor folds in a third so this is the last round. Fix all three in the SAME PR; add "(BUG-1211)" to the commit trailer alongside (BUG-1197) so both close on merge:
    1. Refusal-text parity: stakeholder_mcp_action_label defaults denied MCP tools to "writes" while the CLI stakeholder_action_label says "database writes" / other operation-specific labels for the equivalent command. Share ONE label function across transports (map MCP tool → the same StakeholderAction label the CLI uses: db_sync → "database writes", session_* → "session writes", role_* → "role writes", queue_* → "queue operations", add_relationship → "relationship writes", add_comment → "comment writes", update_requirement → "editing requirements", add_requirement → "adding requirements") so the refusal string is byte-identical on both surfaces for the same operation.
    2. A REAL parity test: the current stakeholder_mcp_policy_covers_every_descriptor_via_shared_actions compares the MCP decision to the shared action it was derived from (tautological). Add a table of (CLI argv, MCP tool) pairs — e.g. (["aida","db","sync"], "db_sync"), (["aida","session","start","--owns","X"], "session_start"), (["aida","role","enter","advisor"], "role_enter"), (["aida","queue","add","X"], "queue_add"), (["aida","edit","X","--title","y"], "update_requirement"), (["aida","list"], "list_requirements"), (["aida","mcp-serve"], none) — and for guest AND requester assert the allow/refuse decision matches and, when refused, the refusal text is identical.
    3. BUG-1211 (folded in): the CLI requester Add arm still accepts --parent / --feature / --owner, which link the Draft to an existing spec (a relationship write) — the exact hole r3 closed on MCP. Refuse them in the CLI arm with the same text ("setting parent, feature, or owner during intake"), share the field list as one const with mcp.rs constrain_requester_intake, and add the CLI test next to guest_refuses_db_sync (requester `aida add --type bug --parent X` refused; plain `aida add --type bug` still lands Draft + intake:requester).
    Finish with aida queue done BUG-1197.
  added_at: 2026-09-18T04:58:12.957633602Z
  for_role: implementer
- user_id: role:implementer
  requirement_id: 01a0b269-201b-77d1-b260-7ecad9095433
  position: 9000
  added_by: role:implementer
  added_at: 2026-09-18T04:35:31.165502072Z
  for_role: implementer
  added_by_machine: imac
- user_id: role:implementer
  requirement_id: 01a0b30b-3fbf-7991-9e19-f6f6d6306cfd
  position: 10000
  added_by: role:implementer
  note: 'one PR with BUG-1211: shared refusal-label fn + real (argv, tool) parity test; trailer both'
  added_at: 2026-09-18T05:44:21.283615251Z
  for_role: implementer
  added_by_machine: imac
- user_id: role:implementer
  requirement_id: 01a0b2dd-cc77-7182-afca-1a210942dbd0
  position: 11000
  added_by: role:implementer
  note: 'do together with the parity-polish task: refuse --parent/--feature/--owner in the CLI requester add arm, shared field const with mcp.rs, CLI test'
  added_at: 2026-09-18T05:44:22.166877188Z
  for_role: implementer
  added_by_machine: imac
- user_id: role:implementer
  requirement_id: 01a0b337-1b82-73a0-a98d-4dd74da2cb82
  position: 12000
  added_by: role:implementer
  note: auto-queued by `aida pr` (01a0b331); covers 1 spec
  added_at: 2026-09-18T06:32:14.341996131Z
  for_role: reviewer
  added_by_machine: imac
- user_id: role:implementer
  requirement_id: 01a0b368-b826-71e3-8488-6c28257ff97e
  position: 13000
  added_by: role:implementer
  note: ignore ac<6-hex> hash labels in next_ac_label; unit cases; rename BUG-1209.AC512631 → AC1
  added_at: 2026-09-18T07:26:33.840632645Z
  for_role: implementer
  added_by_machine: imac
- user_id: role:implementer
  requirement_id: 01a0b378-b2da-71a0-8a1b-ea16a4fbbd95
  position: 14000
  added_by: role:implementer
  note: auto-queued by `aida pr` (01a0b374); covers 1 spec
  added_at: 2026-09-18T07:43:54.268541649Z
  for_role: reviewer
  added_by_machine: imac
