id: 019e7ea8-3488-7581-90bb-95fbd5037b18
spec_id: BUG-416
agreed_id: BUG-416
title: Co-located agents (aida agent new x2 in one worktree) share session scope → aida add hint bleed
description: "Found via quizdom (external AIDA-using project), 2026-05-31: ran 'aida agent new claude' + 'aida agent new codex' in the same project/worktree; objects added in codex's session were auto-hinted to claude's session scope (STORY-15). \n\nVERIFIED MECHANISM (aida-cli/src/main.rs): 'aida add' auto-hints a new spec's scope from the ACTIVE SESSION LEASE's scope (~24600), resolved by cwd / shell-ancestry. There IS a guard to not bleed 'this session owns scope X' hints to UNRELATED shells (~21838/21916) — but two agents launched into the SAME worktree/cwd are NOT unrelated: both resolve to the same active lease, so the guard doesn't fire and the second agent inherits the first's scope. prepare_agent_launch checks for a scope-conflict lease but does not, by itself, place each agent in a DISTINCT worktree.\n\nROOT CAUSE: aida agent new does not enforce/default per-agent worktree+lease isolation; co-located agents share one session scope.\n\nREMEDIATION (suggested, quizdom advisor concurs): per-agent worktree isolation — each agent in its own 'aida session start --owns <distinct-SPEC>' worktree+lease so scope can't bleed. Open design question for the operator: should 'aida agent new' isolate by default (create a per-agent worktree), warn when launching a 2nd agent into an already-owned/shared scope, or stay manual? Connects to SPIKE-34 (claude --bg --agent native worktree isolation — does the --bg path already isolate? the foreground / codex / agy paths may not) and BUG-408 (agent new side-effects). Impact: low data-risk (hints are suggestions; quizdom ignored them, no cleanup) but a real first-impression multi-agent isolation gap. 2nd quizdom-driven AIDA signal (after the weighted-graph-walk driver) — validates multi-vendor multi-agent as a real usage frontier."
status: Approved
priority: Medium
owner: ''
feature: Uncategorized
created_at: 2026-05-31T15:30:13.256044981Z
modified_at: 2026-05-31T16:52:13.893590719Z
req_type: Bug
tags:
- aida:agent:new
- from-external-driver
- isolation
- kind:bug-spotted
- multi-agent
- quizdom
- session-scope
comments:
- id: 019e7eaa-3762-7f63-8f54-dd1e8326680f
  author: joe
  content: 'Operator decision (2026-05-31): DETECT + AUTO-ISOLATE. agent new / session start detect a LIVE lease on the current worktree (key on worktree_path; reuse the BUG-307 dormant-lease liveness check so a dead session isn''t falsely ''occupied''); if found, AUTO-CREATE a fresh per-agent worktree+lease for the new agent rather than co-locating — co-location becomes impossible by construction. Escape hatches preserved: --force/--steal + a same-creator-pid re-attach pass-through; the orchestrator is unaffected (it already provisions per-phase worktrees). Frame: one SESSION (scope-owning lease) per worktree, not one role. Substrate-as-bouncer: a programmatic gate, not a convention. Build separately from P3.'
  created_at: 2026-05-31T15:32:25.058137797Z
  modified_at: 2026-05-31T15:32:25.058137797Z
- id: 019e7ef3-49c5-7c43-a262-99d206574237
  author: joe
  content: 'Cluster map for slice 2 (auto-isolate wiring), captured 2026-05-31 after shipping slice 1 (TASK-607, the pure worktree_occupant detection core). Slice 2 provisions a fresh per-agent worktree when worktree_occupant() detects a live occupant on launch. It surger prepare_agent_launch — the SAME function BUG-408 targets (make --show-context dry, i.e. don''t start the session/worktree/lease before printing context). Recommend doing BUG-408''s dry-refactor FIRST: a prepare_agent_launch that cleanly separates ''compute the launch plan'' from ''execute side-effects'' is the natural seam slice 2 needs to (a) detect occupancy and (b) redirect to a fresh worktree before any lease is written. Also check SPIKE-34 (Done): the claude --bg path may already isolate via native worktree support, so slice 2 may only need to wire the foreground / codex / agy paths. Coherent next-session unit: BUG-408 (dry seam) → BUG-416 slice 2 (auto-isolate) → BUG-362 (pr ship lease+worktree cleanup, same session-lifecycle family). — master/advisor'
  created_at: 2026-05-31T16:52:13.893590719Z
  modified_at: 2026-05-31T16:52:13.893590719Z
