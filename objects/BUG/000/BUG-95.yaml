id: 019e201e-faa3-7770-8533-e31f14919930
spec_id: BUG-95
agreed_id: BUG-95
title: aida pull --code-only skips Done to Completed auto-bump (conditional placement bug)
description: |-
  Follow-up from PR-20 deep-pass review (STORY-86 implementation).

  Reviewer found: the aida pull auto-bump call at main.rs:20500 is nested inside the if !code_only block. Consequence: aida pull --code-only pulls the code branch (which is what brings in teammate merges) but skips the Done to Completed auto-bump that should fire on those merges. A user who runs only aida pull --code-only after a teammate's merge won't see their Done specs bumped until their next full aida pull.

  This is backwards from intent: aida pull --code-only is the leg that NEEDS the auto-bump (the code-side merges are what trigger it). The conditional should likely be inverted, or the call hoisted out of the if-block.

  ## Fix sketch

  Move the auto_bump_done_to_completed call out of the if !code_only block. The auto-bump only depends on code-branch state, so it should run whenever the code leg pulled, regardless of whether the store leg also pulled.

  ## Acceptance

  - [ ] aida pull --code-only triggers auto-bump after the code pull
  - [ ] aida pull (default, both legs) still triggers auto-bump (no regression)
  - [ ] aida pull --store-only correctly skips auto-bump (store-only pull means no new code-branch commits to scan)
  - [ ] Test: run aida pull --code-only after a remote merge, assert Done specs bump

  ## Composes with

  - TASK-106 (push/pull --code-only / --store-only granularity) — same flag surface; this is the auto-bump leg of that granularity contract.

  ## Discovered via

  PR-20 /aida-review deep-pass 2026-05-13.
status: Completed
priority: Medium
owner: ''
feature: Uncategorized
created_at: 2026-05-13T06:56:01.699959803Z
modified_at: 2026-05-31T21:36:46.262164594Z
req_type: Bug
tags:
- auto-bump
- batch:friction-fixes
- code-only
- conditional
- pull
relationships:
- rel_type: Child
  target_id: 019e134a-57d3-70e0-a15c-006dea10310d
  created_at: 2026-05-13T06:56:03.907384002Z
comments:
- id: 019e270d-0838-7250-87c3-1035b1b3ed14
  author: joe
  content: 'Resolved by adding regression-test coverage (no code change needed). Verified end-to-end against handle_pull_command: the auto-bump sits inside the !store_only block (code-pull leg) at main.rs:22233, which fires for both default AND --code-only modes. The bug-as-filed misread the conditional — the actual block is gated by !store_only (skip code pull when store-only requested), not !code_only. Tests pull_code_only_triggers_auto_bump and pull_store_only_does_not_run_auto_bump lock the correct behavior. trace:BUG-95 | ai:claude'
  created_at: 2026-05-14T15:13:46.040524310Z
  modified_at: 2026-05-14T15:13:46.040524310Z
- id: 019e290c-f045-7dc0-85c5-bc68e0a099f9
  author: joe
  content: |-
    Verified-not-reproducible via PR-27 (commit df65ff4): https://github.com/joemooney/aida/pull/27

    - Bug-as-filed claimed `aida pull --code-only` skipped the Done → Completed auto-bump because the call was nested in the wrong conditional. End-to-end audit of handle_pull_command showed the auto-bump call sits inside `if !store_only` (the code-pull leg), which correctly fires for both default AND --code-only modes
    - Added two regression tests (`pull_code_only_triggers_auto_bump` + `pull_store_only_does_not_run_auto_bump`) using a bare git remote + project clone to exercise the path end-to-end
    - No code change — just test coverage to lock the correct shape

    Test status: 609/609 workspace tests green (2 new regression tests in handle_pull_command_tests)
  created_at: 2026-05-15T00:32:54.341990374Z
  modified_at: 2026-05-15T00:32:54.341990374Z
archived: true
archived_at: 2026-05-31T21:36:46.262164594Z
implementation_info:
  implemented: true
  implemented_at: 2026-05-14T15:13:37.934151774Z
  implemented_by: joe
