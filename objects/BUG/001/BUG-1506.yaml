id: 01a0c198-74ae-7732-bdc8-dd4c3a99e2a6
spec_id: BUG-1506
agreed_id: BUG-1506
title: A spec that reaches main without ever passing through Done is never auto-bumped, so merged work sits reading Draft indefinitely
description: |
  The auto-bump only promotes Done to Completed. A spec that never entered the lifecycle - filed, worked and merged without passing through Done - is therefore never bumped at all, and merged work sits reading Draft indefinitely.

  Observed 2026-09-20 by the reviewer seat on TASK-1302: its content was on the default branch as commit 53e3176e6e while the spec still read Draft.

  THIS IS THE MIRROR IMAGE OF BUG-1454, NOT THE SAME BUG. There, the bump fires too early on the first trailered commit and hides unmerged work under Completed. Here it never fires at all and hides merged work under Draft. Both are blind spots of the same condition - "is this spec's work actually on main" - which is why they were originally filed together, and both make the open-work view lie. They are separated because BUG-1454's fix guards a bump that already runs, while this needs a bump that never starts.

  WHY THIS ONE IS THE QUIETER OF THE TWO. A spec wrongly Completed at least looks finished to someone auditing finished work. A spec wrongly Draft looks like unstarted backlog, so it is read as work still to do and may be re-implemented, re-queued, or groomed by an advisor who has no way to know the code already shipped. The cost lands on whoever picks it up next.

  Acceptance:
  1. A spec whose trailered commits are already on the default branch is not left in a pre-Done status - Draft, Approved, or Planned - merely because it skipped the intermediate states. Say what status it lands in and why: Completed directly, or Done first so a human confirms.
  2. Establish whether skipping the intermediate states is itself a defect worth blocking. If a spec can reach main from Draft, either that path is legitimate and the bump must handle it, or it is not and the gap belongs upstream at the commit or merge boundary. Decide this before implementing, since it determines whether this is a bump fix or a guard.
  3. The BUG-1454 sweep runs in BOTH directions and reports both counts: Completed specs with an open PR referencing them, and pre-Done specs whose trailered commits are already merged. BUG-1454 delivers the first count; this spec delivers the second.
  4. The second count is measured and recorded once, since it is currently unknown and stops being recoverable as branches are deleted.
  5. A test covers the shape: a spec at Draft whose trailered commit is on the default branch is detected, and one whose trailered commit is only on a branch is not.

  PROVENANCE. Originally added to BUG-1454 as items (a) and (b) in an advisor COMMENT on 2026-09-20 rather than in its ACCEPTANCE block. A drain implements the acceptance block, so the criteria were invisible at the rung the implementer reads and PR #2040 correctly implemented the six visible criteria and none of these. Split out here so BUG-1454 can merge without burying a real remaining deliverable behind a terminal Completed status. The misfiling is recorded in TASK-1303 as an instance of the rung class.
status: InProgress
priority: High
owner: ''
feature: Uncategorized
created_at: 2026-09-21T01:33:09.166796821Z
modified_at: 2026-09-23T03:14:20.863562796Z
req_type: Bug
tags:
- batch:night-0920 lifecycle drain-safe from:review-pr-2040
relationships:
- rel_type: References
  target_id: 01a0bfa0-5a97-7cb3-8382-f90973400e83
  created_at: 2026-09-21T01:33:44.325454665Z
  created_by: joe
comments:
- id: 01a0c22d-7d7b-7ae3-a687-4256700cb1e5
  author: joe
  content: |-
    [aida:advisor] LIVE INSTANCE RESOLVED, AND THE RECOVERY PATH HAS ITS OWN DEFECT. 2026-09-20 21:18.

    THE INSTANCE. PR #2045 merged as 9c8373f5d6 carrying the (STORY-1391) trailer. STORY-1391 stayed at In Progress: it never passed through Done, so the auto-bump - which promotes done to completed - had nothing to promote. That is this spec's exact case, occurring on the verdict-retention spec itself, within minutes of its own merge.

    RESOLVED with aida db reconcile-status. The spec now reads Completed. No open PR referenced it, so BUG-1454's defer-while-a-PR-is-open guard correctly permitted the flip.

    CORRECTED 2026-09-20 21:22 - MY DEFAULT-RANGE DIAGNOSIS WAS WRONG AND IS WITHDRAWN.

    I originally wrote that the default scan range missed a commit at HEAD. The code rules that out: with no --since the verb runs `git log --max-count=200 HEAD`, which STARTS at HEAD. A commit at HEAD cannot be outside it. The reviewer seat quoted the construction and verified 9c8373f5d6 is one match in that range with zero commits after it.

    WHAT ACTUALLY HAPPENED, from the reflog rather than from inference:

        main@{21:15:04}  pull -q --ff-only origin main: Fast-forward -> 9c8373f5d6

    Local main did not contain the merge until 21:15:04. The first reconcile ran against a local main that genuinely did not have the commit, so the verb was correct-but-blind: it reported nothing in range about a commit it could not see. The second run, with --since HEAD~5, came after that fast-forward - so what changed between the two was the LOCAL REF, not the range. Widening --since appeared to be the fix and was coincidental.

    THE REQUIRED ADDITION IS THEREFORE THREE-STATE, NOT TWO, and the third is the one that actually fired:

        nothing eligible                 no spec in scan range is at a graduatable status
        nothing in range                 the scan window excluded the commit
        NOTHING IN THE LOCAL CLONE       the commit is not in this checkout at all

    A two-state message would have said "nothing in range" and sent the caller to widen --since - exactly the wrong move when the commit is not local. The verb must also NAME THE REF IT SCANNED, which its output currently does not: "scanned local main at <sha>; if the commit you expect is not there, pull first."

    THE UNDERLYING SHAPE is the same as the rest of this class: the surface reports a true fact about what it LOOKED AT, the reader infers a fact about the WORLD, and nothing in the output distinguishes them. It is PRIN-5 inside a verb whose entire purpose is catching what another surface missed - and the verb's dependence on local state is invisible.
  created_at: 2026-09-21T04:15:56.283315905Z
  modified_at: 2026-09-21T04:20:07.132811070Z
  session_id: 01a0adee-16e1-7982-ac9f-e15abb207b6c
- id: 01a0cc27-f1cf-76f0-9f5d-c53e6fd4f821
  author: joe
  content: 'Claude implementer (sonnet subagent, worktree) dispatched 2026-09-22 19:47 local. Output: branch claude/bug-1506 off origin/main, pushed, NO PR, NOT merged.'
  created_at: 2026-09-23T02:46:05.007432525Z
  modified_at: 2026-09-23T02:46:05.007432525Z
  session_id: 01a0cbdf-7c46-79e0-a895-67caf4e1efce
- id: 01a0cc41-d23f-7fd1-8eb2-524df424a0f2
  author: joe
  content: Commit 5241fe12dc references this requirement
  created_at: 2026-09-23T03:14:20.863562796Z
  modified_at: 2026-09-23T03:14:20.863562796Z
  session_id: 01a0cbdf-7c46-79e0-a895-67caf4e1efce
