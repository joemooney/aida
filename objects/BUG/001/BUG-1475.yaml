id: 01a0c010-dde0-79c1-ae3f-5f4fa1ec7113
spec_id: BUG-1475
agreed_id: BUG-1475
title: 'Cross-platform nightly red 3 consecutive nights: three DIFFERENT Windows-only test-fixture defects, one per night, nobody triaging'
description: |-
  The cross-platform nightly has failed 09-18, 09-19 and 09-20. Last green was 09-17 (run 34963831883). Per CLAUDE.md, releases require cross-platform green within 24h of tagging, so this currently blocks any release. GitHub issue #1925 was auto-opened 09-18 and has been re-commented each night; nobody has triaged it.

  IT IS NOT ONE BUG. macOS passed all three nights. Windows failed all three, with a DIFFERENT single test each night. All three are test-fixture portability defects, not product defects, and each is independently fixable.

  1. 09-18, run 35337447928 (4899 passed, 1 failed)
     reconstitute::tests::scratch_gc_expires_old_runs_and_keeps_only_the_newest
     aida-cli-lib/src/reconstitute.rs:773
     Os { code: 5, kind: PermissionDenied, message: "Access is denied." } on an unwrap.
     Shape: Windows refuses to remove a file or directory while a handle is open, or one
     carrying the read-only attribute. The scratch-GC removal needs the Windows retry/readonly-
     clear treatment rather than a bare unwrap.

  2. 09-19, run 35438189645 (940 passed, 1 failed - aida-core)
     git_ops::tests::failed_pull_rebase_aborts_and_restores_branch
     aida-core/src/git_ops.rs:5668
     assertion left == right failed:  left: "value: local\r\n"   right: "value: local\n"
     Shape: CRLF. The fixture compares captured output against a LF-terminated literal. Normalize
     line endings before comparing, or write the fixture with a platform-agnostic terminator.

  3. 09-20, run 35506914381 (5092 passed, 1 failed)
     auto_bump_done_tests::auto_bump_resolves_stranded_review_pr_specs_via_forge_lookup
     aida-cli-lib/src/tests/auto_bump_done_tests.rs:2080
     "PR closed without merging should reject the review spec, was Approved"
     ROOT CAUSE, verified by reading the helper: write_fake_gh_for_pr_states (same file, ~1986)
     writes a bash script to <root>/gh with a "#!/usr/bin/env bash" shebang, sets the exec bit
     only under #[cfg(unix)], and gives it no .cmd/.bat/.exe extension. Windows cannot spawn it.
     AIDA_TEST_GH_BINARY therefore points at an unrunnable file, the forge fallback silently
     does nothing, STORY-1234 stays Approved instead of Rejected, and the assertion fires. The
     helper landed 2026-09-19 with TASK-1296 (commit b75ea81f11), which matches night 3.

  THE PATTERN WORTH MORE THAN THE THREE FIXES. PR CI is Linux-only by design (TASK-257). Three
  Windows-only test defects reached main in three days and each was caught only by the nightly
  - roughly one per day. The guards CLAUDE.md names as covering this gap did not: the required
  Rust-test portability ratchet, scripts/check-portability.sh, has no check for fake-binary
  fixtures (no shebang / cfg(unix)-only chmod / missing .cmd detection) and no CRLF-comparison
  check. It greps for cfg(unix) gating of PRODUCTION code, not of TEST fixtures. So the ratchet
  is blind to exactly the class that is actually breaking, which is why the nightly is carrying
  the whole load alone.

  And the nightly is a detector nobody is wired to. It auto-opens an issue and re-comments, but
  no seat owns triaging it, so three nights accumulated silently while the release gate stayed
  shut.

  ## Acceptance

  - All three named tests pass on windows-latest.
  - Instance 2 is fixed by normalizing line endings in the comparison, not by marking the test
    cfg(unix) - skipping on Windows would reduce coverage on the platform that is failing.
  - Instance 3 is fixed so the fake-binary fixture is spawnable on Windows (a .cmd shim, or an
    extension chosen per platform), and any OTHER test fixture writing a fake executable the
    same way is fixed in the same change - enumerate them rather than fixing only this one.
  - scripts/check-portability.sh grows a check for the two classes that escaped it: a test
    fixture that writes an executable without a Windows-runnable form, and a test comparing
    captured output against an LF-terminated literal.
  - One named seat owns nightly triage, or the nightly failure routes into the coordination
    inbox so a red nightly cannot sit three days unowned. Whichever is chosen, state it.
  - A green cross-platform nightly on main is observed before this is closed - not merely the
    three tests passing locally.
status: Approved
priority: High
owner: ''
feature: Uncategorized
created_at: 2026-09-20T18:25:25.984047465Z
modified_at: 2026-09-20T19:48:12.923927205Z
req_type: Bug
tags:
- batch:night-0920
- ci windows portability nightly release-blocker from:review-session-0920
relationships:
- rel_type: Supersedes
  target_id: 01a0bef3-82ca-7f50-8722-5f4cb9fb75da
  created_at: 2026-09-20T18:28:06.370885644Z
  created_by: joe
- rel_type: References
  target_id: 01a0b5ce-6f5d-7be3-b693-e98175dbb944
  created_at: 2026-09-20T18:28:34.407503650Z
  created_by: joe
- rel_type: References
  target_id: 01a0bb67-f38d-7750-8851-7277d32ddb25
  created_at: 2026-09-20T18:28:37.386942877Z
  created_by: joe
- rel_type: References
  target_id: 01a0bc29-3fc5-7333-8ef8-6af40da562f2
  created_at: 2026-09-20T18:28:39.301960735Z
  created_by: joe
comments:
- id: 01a0c013-b278-76a3-a084-21edf4e7563d
  author: joe
  content: |-
    [aida:advisor] Approved HIGH, mode drain. This supersedes BUG-1443, which I filed this afternoon carrying a FALSE claim — that all three nights failed the same test. They did not, and this filing has the right answer. I have corrected and superseded mine.

    VERIFIED INDEPENDENTLY from the scheduled runs, confirming all three lines of the filing:
      35337447928  09-18  reconstitute::tests::scratch_gc_expires_old_runs_and_keeps_only_the_newest  reconstitute.rs:773
      35438189645  09-19  git_ops::tests::failed_pull_rebase_aborts_and_restores_branch              git_ops.rs:5668
      35506914381  09-20  auto_bump_done_tests::auto_bump_resolves_stranded_review_pr_specs_via_forge_lookup  auto_bump_done_tests.rs:2080
    macOS green all three nights. Last green nightly 09-17.

    THE PART ABOVE THE THREE FIXES IS THE SPEC. Three Windows-only defects reached main in three days — roughly one per day — and the guard CLAUDE.md names for exactly this gap did not fire once. scripts/check-portability.sh greps for cfg(unix) gating of PRODUCTION code; it has no rule for a fake-binary fixture, none for a CRLF comparison, and none for a permission/handle assumption. It is blind to the precise class that is breaking. So the nightly carries the entire load, and the nightly is a detector wired to nobody: issue #1925 has been auto-commented into an empty room since 09-18.

    That is the SPIKE-85 shape again and a clean specimen: a portability ratchet whose blind spot is portability in tests. Its charter and its miss are the same word.

    THE TWO REFUSED SHORTCUTS ARE THE RIGHT ONES and I am reinforcing both:
      - Do NOT fix the CRLF instance by marking it cfg(unix). That deletes coverage on the only
        platform where it fails, which converts a red nightly into a silent absence of testing.
      - Do NOT fix the shebang instance at the one call site. Enumerate every fixture that writes
        a fake executable the same way. Four siblings already carry cfg gates
        (bug_779_wrapper_eval_tests, task_1171_eval_channel_tests, drain_reliability_wiring_tests,
        real_phase_driver_wiring_tests), which means the convention exists and was simply not
        followed — an unenforced convention, not a missing one.

    AND THE CLOSE CONDITION IS THE BEST THING HERE. Requiring an OBSERVED GREEN NIGHTLY ON MAIN rather than three tests passing locally is the merged artifact-over-report entry applied to its own domain. Three local passes are a report; a green scheduled run is the artifact. Keep that criterion exactly as written.

    ADDITIONS:
    1. Ratchet rules for all three classes, each demonstrated to flag its own instance BEFORE the fix lands. A rule that only passes afterwards has not been shown to detect anything.
    2. Existing sites allowlisted via BUG-1301's growth check so the ratchet blocks new instances rather than demanding a sweep.
    3. Wire the nightly's failure to a seat. A detector reporting into an empty room for three nights is a second defect, independent of the three fixes, and closing #1925 without addressing it guarantees the fourth night.
    4. Say whether BUG-1233 and BUG-1283 — both Completed, both about scratch_gc and CRLF respectively — are regressions of the same sites or different ones. If the same, the earlier fixes did not hold and that changes what "fixed" has to mean here.

    ON NOT DRAINING INSTANCE 1 HEADLESS — agreed. A Windows file-handle failure cannot be reproduced or verified on this machine, and a headless implementer would be guessing against a platform it cannot run. Route that one to the keyboard or to a dispatch-and-read-the-nightly loop.
  created_at: 2026-09-20T18:28:31.480624961Z
  modified_at: 2026-09-20T18:28:31.480624961Z
  session_id: 01a0adee-16e1-7982-ac9f-e15abb207b6c
- id: 01a0c048-f3ad-7913-b44a-ffca90ac3235
  author: joe
  content: '[aida:advisor] EXPLICIT, at the reviewer''s flag-back, so an implementer cannot read it otherwise: the ratchet work in criterion 1 is NOT optional once the three tests go green. If BUG-1233 and BUG-1283 turn out to be regressions of the same sites (criterion 4), then a green scheduled nightly is necessary but NOT sufficient to close this spec — a site that has now regressed twice is evidence that point fixes do not hold there, and the only thing that changes the outcome is the rule that would have caught it at PR time. Do not close on three green tests plus one green nightly while the ratchet rules are unwritten; that is precisely the state this spec exists to end, and it is how BUG-1233 and BUG-1283 were both closed before.'
  created_at: 2026-09-20T19:26:41.581080255Z
  modified_at: 2026-09-20T19:26:41.581080255Z
  session_id: 01a0adee-16e1-7982-ac9f-e15abb207b6c
- id: 01a0c05a-e8c9-7442-9891-f940296b96cd
  author: joe
  content: |-
    [aida:reviewer] A FOURTH Windows-only failure, now on main — and I cannot rule out that I caused it. Escalating rather than concluding.

    WHAT FAILED. PR #2024 (BUG-1432), Windows job 106124356720 at 18:13Z:
      templates::tests::project_discipline_pack_matches_master  (aida-core, 954 passed / 1 failed)
      "project copy .aida/discipline/ has guide(s) whose content differs from the embedded master"
      followed by ALL 22 non-allowlisted guides.
    Build (ubuntu-latest) PASSED on the same commit. So it is platform-specific, not stale content.

    WHY IT MATTERS FOR THIS SPEC. It is a fourth distinct Windows defect, it is on main (not a PR-only artifact — #2024's base is current main and its own diff touches only aida-cli-lib/src/lib.rs and one test file), and it means fixing the three instances above will NOT produce the green nightly this spec requires. The close condition still holds; the work is larger than filed.

    THE TIMING IMPLICATES MY MERGE AND I AM SAYING SO RATHER THAN CLEARING MYSELF.
      11:06 local  nightly run 35506914381 on main: aida-core 952 passed / 0 failed,
                   project_discipline_pack_matches_master ... ok  on Windows.
      11:09 local  I merged PR #2023 (53e3176e6e) — the only discipline-pack change in the window.
      11:13 local  PR #2024's Windows run, base includes my merge: same test FAILS, 22 files.
    #1979 (10:51) and STORY-1351 (10:54) both touched the pack and were already inside the passing nightly.

    EVIDENCE AGAINST IT BEING MINE, which is why I am not simply owning it:
      - My change touched TWO files. The failure reports TWENTY-TWO. No 2-file content change produces that.
      - Verified on current origin/main from Linux: of 22 pairs, exactly 3 differ in git —
        README.md, autonomous-burndown.md, substrate-as-bouncer.md — and those are the allowlisted
        ones, correctly absent from the Windows failure list. session-discipline.md, the file I
        merged, is byte-identical between master and project copy on main.
      - The signature (every non-allowlisted file mismatching at once) is a line-ending or
        path-separator mismatch between the on-disk project copy and the build.rs-embedded master,
        not a content drift.

    I CANNOT SETTLE IT FROM LINUX and will not assert either way. The decisive test is a
    cross-platform run pinned to a0ec8fb22f (pre-merge) versus 53e3176e6e (my merge). If pre-merge
    is red, my merge is exonerated and the 11:06 nightly pass needs its own explanation — most
    likely a cached target dir where build.rs had not re-run, which would mean this defect was
    latent and my merge only triggered the re-embed that exposed it. That hypothesis is testable
    and I have not tested it.

    IF IT IS MINE, the remedy is not a revert: the content is correct on Linux and the entry is
    sound. It would mean the parity test compares bytes across a boundary where one side is
    checkout-normalized and the other is compile-time-embedded, which is BUG-1475 instance 2's CRLF
    class at a different site — and that is the ratchet rule this spec already asks for.

    NOTE FOR WHOEVER TAKES THIS: the 09-20 nightly used --no-fail-fast, so aida-core did run and
    did pass. This is not a masked failure; it is a state change between 11:06 and 11:13.
  created_at: 2026-09-20T19:46:18.441560977Z
  modified_at: 2026-09-20T19:46:18.441560977Z
  session_id: 01a0bfde-4e75-74c0-a388-be9233da07a4
- id: 01a0c05c-a7fb-7013-9d23-ac03a32610a1
  author: joe
  content: '[aida:advisor] Dispatched run 35533456218 — cross-platform on main at 5ccfd951d5 — to settle whether templates::tests::project_discipline_pack_matches_master is red on main or specific to PR #2024''s base. Independently verified the Linux side: exactly THREE project/master pairs differ on origin/main (README.md, autonomous-burndown.md, substrate-as-bouncer.md) and all three are LOCALIZED_DISCIPLINE_GUIDES, correctly absent from the Windows failure list. My own first pass said four — test-isolation.md — which was a false positive: that guide is project-only with no master, so I hashed a nonexistent file. Worth recording because the same mistake is available to whoever picks this up. If the dispatch comes back red on Windows, this is a FOURTH distinct site in this spec''s class and the argument for the ratchet rule over point fixes gets stronger, not weaker.'
  created_at: 2026-09-20T19:48:12.923927205Z
  modified_at: 2026-09-20T19:48:12.923927205Z
  session_id: 01a0adee-16e1-7982-ac9f-e15abb207b6c
execution_mode: drain
