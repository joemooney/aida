id: 01a0bf39-b505-7e41-bf9f-afbad2b3112b
spec_id: BUG-1447
agreed_id: BUG-1447
title: A branch-protection refusal is classified as a merge conflict because both say not mergeable, so a held PR burns its retries and is reachable by auto-rebase
description: |
  `is_merge_conflict_failure` matches the substring "not mergeable", and GitHub's BRANCH-PROTECTION refusal contains that phrase. So a policy refusal — a deliberate, correct, stable gate — is classified as a merge conflict, which is exactly the case the function's own doc comment says it must exclude.

  THE FUNCTION, aida-cli-lib/src/auto_complete.rs:1015, and its own documentation three lines above it:

      /// base moved under the PR so the squash cannot apply), as opposed to any
      /// other merge failure (auth, network, BRANCH PROTECTION)? Matched against
      /// the `gh pr merge` error text carried in the failure reason. Conservative:
      /// only a conflict is rebase-recoverable, so anything unrecognized is not.
      pub(crate) fn is_merge_conflict_failure(reason: &str) -> bool {
          let r = reason.to_ascii_lowercase();
          r.contains("not mergeable")
              || ...

  The comment names branch protection as a thing this must NOT match. The first clause matches it.

  THE MESSAGE, observed 2026-09-20 14:29:15Z on PR-2014 (TASK-1298):

      gh pr merge failed for #2014: X Pull request joemooney/aida#2014 is not mergeable:
      the base branch policy prohibits the merge.

  "is not mergeable" is present. The classifier fires.

  WHAT THE HOLD WAS DOING IS CORRECT AND IS THE POINT. `merge-hold-gate: fail` on that PR — a genuine, label-driven, forge-enforced supervised hold, working exactly as intended and for the first time reliably, since the advisor discovered on 2026-09-20 that most holds had been marker-only and began applying labels. Fixing hold enforcement is what exposed this: the more holds actually hold, the more often a correct refusal is misread as a conflict.

  THREE CONSEQUENCES, in increasing order of cost:

  1. The drain spent both transient-retry attempts on a refusal that cannot succeed until a human clears it, then shelved as `tool-exit` with the hint "Investigate the merge failure". The actual next action is `aida merge-hold clear 2014`. This is BUG-1316's defect at a different site — that spec fixed the case where our OWN `pr ship` refuses; this is the case where the FORGE refuses, and BUG-1316's MergeHold kind is not reached.
  2. Worse, `is_merge_conflict_failure` gates the in-drain auto-rebase (TASK-975). A held PR can therefore be REBASED — force-pushing a branch whose review is deliberately paused, invalidating any review in flight, to "fix" a conflict that does not exist. I have not confirmed this fired on PR-2014; the budget gate (`ci_auto_fix`) may have been closed. It is reachable, and that is enough.
  3. It is the seventh instance this week of a decision made by matching another component's prose. Here the prose belongs to `gh`, which is BUG-1310's class precisely: we do not control the wording, an upstream reword changes our control flow, and no test of ours fails.

  PROPOSED: classify the forge's refusal from a typed signal rather than its sentence. `gh pr merge` distinguishes these cases; where it does not, `gh pr view --json mergeStateStatus` reports BLOCKED for a policy refusal versus DIRTY for a genuine conflict, and that is already queried elsewhere in this codebase. At minimum, exclude the branch-protection shape before the conflict test, and make the recovery hint name the hold and the clear command that are already present in the refusal text.

  ACCEPTANCE
  1. A branch-protection / policy refusal is NOT classified as a merge conflict.
  2. It does not consume a transient-retry attempt, consistent with BUG-1316's principle that a correct refusal must not spend the budget meant for transient failure.
  3. It cannot reach the auto-rebase path. A test asserts a held PR is never rebased.
  4. The recovery hint names the hold and `aida merge-hold clear <pr>` rather than "investigate the merge failure".
  5. A genuine merge conflict still classifies and still rebases as today — both directions tested.
  6. The observed PR-2014 message is the fixture, verbatim.

  Filed by the product seat; disposition is the advisor's. Note this is a direct consequence of holds being enforced properly for the first time — the defect existed all along and could not fire while the gate was inert.
status: Draft
priority: Medium
owner: ''
feature: Uncategorized
created_at: 2026-09-20T14:30:25.285712180Z
modified_at: 2026-09-20T16:24:18.034285193Z
req_type: Bug
tags:
- from:session-0919
- merge-hold
- orchestrator
- parent:EPIC-62
- prose-matching
relationships:
- rel_type: Child
  target_id: 01a09180-071a-7712-8b42-9e6f1b646259
  created_at: 2026-09-20T14:30:26.814177346Z
comments:
- id: 01a0bf3f-644b-7f81-90a8-b39bfcc97605
  author: joe
  content: |-
    SECOND AND THIRD DATA POINTS, and the scope is worse than the filing described: this is not an occasional misclassification, it is every merge the drain attempts.

      14:29:15Z  TASK-1298  PR-2014  "is not mergeable: the base branch policy prohibits the merge"  attempt 2/2, shelved
      14:35:43Z  BUG-1310   PR-2016  identical message                                                attempt 2/2, shelved

    Six minutes apart. And the population is total: I checked `merge-hold-gate` on every open PR just now —

      #1972 #1974 #1979 #1981 #1984 #1989 #1991 #1999 #2001 #2006 #2014 #2016
      12 held out of 12 open

    So there is currently NO open PR the drain can merge. Every spec that reaches its merge phase will produce this shelve, spend both transient-retry attempts, and land in triage as `tool-exit` beside genuine tool failures. At the observed rate that is a steady manufacture of false failure records for the rest of the night.

    WHAT THIS CHANGES ABOUT THE SPEC, beyond frequency:

    The original filing framed the retry burn as the cost and the auto-rebase reachability as the risk. With 12 of 12 held, the retry burn is no longer incidental — it is the drain's default outcome at phase 4. Criterion 2 (a correct refusal must not consume a retry) moves from tidiness to the thing that stops the shelve log becoming noise.

    It also means the auto-rebase reachability in consequence 2 is not a rare corner. If `ci_auto_fix` is ever armed while this stands, the drain would be rebasing held PRs as its ordinary behaviour, not exceptionally. That deserves the test in criterion 3 regardless of how the classification is fixed.

    ONE THING I CANNOT DETERMINE and someone should, because it changes the fix. Drive-mode specs are held BY THE DRAIN so that a human merges them. If that is why these are held, then the drain attempting the merge at all is a second defect upstream of the classification — it is trying to merge something it deliberately gated. Distinguishing "the drain should not attempt this merge" from "the drain should attempt it and classify the refusal correctly" decides whether the fix belongs at phase 4's entry or at the classifier. I have not established which, and the answer is in the hold provenance rather than in the message.
  created_at: 2026-09-20T14:36:37.835149064Z
  modified_at: 2026-09-20T14:36:37.835149064Z
  session_id: 01a079fe-4ad8-7ee0-8086-16d6a1b7f0e5
- id: 01a0bf56-d142-7471-badc-d944cd2ce8c1
  author: joe
  content: |-
    CORRECTION TO MY OWN OPEN QUESTION, and to the scope claim in my previous comment. I read the hold list rather than continuing to infer from the refusal message.

    I ASKED whether these PRs are held BY THE DRAIN for drive-mode specs, which would make the drain's merge attempt a second defect upstream of the classification. The answer is no, and it was checkable in one command.

    `aida merge-hold list` shows ten active holds. Only TWO are drive-mode holds placed by the drain:

      #1972  TASK-1289 is marked drive — auto-merge refused   [label: synced]
      #1984  TASK-1279 is marked drive — auto-merge refused   [label: synced]

    The other eight are ADVISOR REVIEW holds, each naming a specific unresolved finding — prose-matching in #2006, a narrowed reporting surface in #1999, a missing contract version bump in #1998, an ADR claiming coverage that does not exist in #2001, and so on.

    SO THE DRAIN IS NOT FIGHTING ITS OWN GATE. It is attempting to merge PRs whose reviewer has refused them with changes outstanding, and that refusal is correct. The defect is narrower than I framed it and sits in two places, neither of them phase 4's entry condition as I speculated:

      1. A correct, stable refusal must not consume the transient-retry budget (BUG-1316's principle, this site).
      2. It must not shelve as `tool-exit`, which reads as a tool failure and buries the actual next action —
         the hold reason and `aida merge-hold clear <pr>` are both in the refusal text and discarded.

    Whether the drain should reach phase 4 at all on a spec carrying an unresolved review hold is a real question, but it is a SEPARATE one and I no longer think it belongs in this spec.

    AND MY SCOPE CLAIM WAS WRONG. I wrote "there is currently no open PR the drain can merge" from a snapshot showing 12 of 12 held. BUG-1426 merged as #2018 at 14:58 — drain mode, no labels, no hold — fifteen minutes later. Fresh work with no outstanding findings merges normally. What cannot merge is the backlog of PRs that each have a real finding against them, which is the system working rather than failing.

    The retry-burn and the misleading shelve kind are still real and still worth fixing. The volume claim behind them was not: this fires once per held PR the drain happens to re-drive, not on every merge.

    I inferred a total population from a snapshot of a queue that was mid-drain, and stated it as a standing condition. The check that would have caught it — merge something and see — happened on its own fifteen minutes later.
  created_at: 2026-09-20T15:02:13.058293134Z
  modified_at: 2026-09-20T15:02:13.058293134Z
  session_id: 01a079fe-4ad8-7ee0-8086-16d6a1b7f0e5
- id: 01a0bf9f-c3d7-7ba0-ab25-3f8c30cbfec7
  author: joe
  content: |-
    Blast radius, measured 2026-09-20 16:20 local. The spec's diagnosis needs nothing added; this is the priority argument it is missing.

    FOURTEEN open PRs currently carry `aida:merge-hold`:

      #2021 #2019 #2014 #2006 #2001 #1999 #1998 #1991 #1989 #1984 #1981 #1979 #1974 #1972

    Every one of them is a future instance. The drain does not know a hold from a conflict, so each held PR it reaches costs two retry attempts and then a `tool-exit` shelve whose hint ("Investigate the merge failure") sends a human to look at a PR that is behaving correctly.

    TWO have already paid it. `.aida/events.jsonl` carries six matching records — a `SpecRetried` at attempt 2/2 and a `SpecShelved` for each of:

      TASK-1298  14:29:18Z shelved, 14:40:06Z retried 2/2, 14:40:08Z shelved again
      BUG-1310   14:35:43Z retried 2/2, 14:35:45Z shelved

    So the observed rate is 2 of 14 in roughly six hours, with the other twelve latent purely because the drain has not reached them yet. TASK-1298 is sitting NeedsAttention right now for this reason and for no other — its PR #2014 is green on Build (ubuntu-latest) and is waiting on review, which is not a failure state at all.

    THE SECOND-ORDER COST is worse than the wasted retries and is the reason I would not leave this in draft. The shelve reclassifies "waiting for a human reviewer" as "the tooling broke". Those need opposite responses: one is the system working, the other is an incident. `aida findings list` currently presents TASK-1298 under "Failures awaiting triage" alongside genuine tool failures, so the triage surface actively misdirects attention — and it will do so once per held PR, for a review queue that is fourteen deep.

    This is also self-reinforcing in a way that argues against waiting: the advisor began applying real labels (rather than marker-only holds) on 2026-09-20, so holds are enforcing for the first time. The better hold enforcement gets, the more often this fires.

    No change requested to the diagnosis or the fix. This is a note that the cost is being paid now, at a known rate, against a queue whose depth is the multiplier.
  created_at: 2026-09-20T16:21:53.751477777Z
  modified_at: 2026-09-20T16:21:53.751477777Z
  session_id: 01a079fe-4ad8-7ee0-8086-16d6a1b7f0e5
- id: 01a0bfa0-53d4-7052-912f-4b67b7fc0c79
  author: joe
  content: |-
    Prediction logged in advance, 2026-09-20 16:23 local, so it is falsifiable rather than a story told afterwards.

    `aida drain status` currently reports `current: BUG-1291`. BUG-1291 is status Done with PR #2001 open, and #2001 carries `aida:merge-hold` with `merge-hold-gate: failure`.

    If this diagnosis is right, the drain's merge phase on #2001 will produce the same sequence already recorded for TASK-1298 and BUG-1310: a `gh pr merge` failure containing "is not mergeable: the base branch policy prohibits the merge", classified by `is_merge_conflict_failure` as a conflict, one `SpecRetried` at attempt 2/2, then `SpecShelved` with kind `tool-exit`. BUG-1291 should then appear under "Failures awaiting triage" despite #2001 being green on Build and simply awaiting review.

    If instead BUG-1291 completes, or shelves with a kind that names the hold, the diagnosis is wrong or incomplete and this comment should be treated as evidence against it.

    I am deliberately NOT removing BUG-1291 from the batch to prevent this. Pulling individual specs out is whack-a-mole against a fourteen-PR hold queue, and a third instance recorded against a stated prediction is worth more than a spec spared.

    (Separately, the same `drain status` output shows `current: BUG-1291` while `members[9]` does not list BUG-1291 at all — the batch reporter is blind to its own current member. Noted here only because it made the drain's actual position hard to read; it is a different defect.)
  created_at: 2026-09-20T16:22:30.612482728Z
  modified_at: 2026-09-20T16:22:30.612482728Z
  session_id: 01a079fe-4ad8-7ee0-8086-16d6a1b7f0e5
- id: 01a0bfa1-f772-7fd0-8c9b-251528015867
  author: joe
  content: |-
    PREDICTION FALSIFIED, and the correction narrows this spec's scope in a way worth recording.

    I predicted twenty minutes ago that the drain, reaching held PR #2001 (BUG-1291), would fail in the MERGE phase with "the base branch policy prohibits the merge" and shelve `tool-exit`. It did not reach the merge phase at all.

    What actually happened, from events.jsonl:

        16:15:35  PhaseEntered idx=1 implementer
        16:22:08  PhaseEntered idx=2 ci
        16:22:40  CiTerminal green=false

    And the PR's check rollup at that moment:

        Build (ubuntu-latest) = in_progress
        merge-hold-gate       = failure

    So the CI phase declared CI terminally red after 32 seconds, while the actual build was still running, on the strength of the only check that had concluded — the merge hold. The spec is heading for a `ci-red` shelve, not a `tool-exit` one.

    TWO CORRECTIONS FOLLOW.

    First, this belongs to BUG-1265 ("a hold-gate-only red with the marker present must never shelve as ci-red"), not here. And it is a slightly stronger statement than BUG-1265 currently makes: the failure is not only that a hold-gate red is read as CI red, but that it is read as TERMINAL while the real build is still in progress. One concluded failing check ends the wait. That ordering detail is what makes it fire fast and reliably.

    Second, and this is the part that matters for THIS spec: the merge-phase misclassification described here can only fire when the CI phase somehow passes despite the hold. Since the CI phase normally short-circuits red on the hold gate first, BUG-1447's path is reached only in a narrower window — where CI concluded green (hold gate included) and the hold was applied between the CI phase and the merge phase. That is consistent with TASK-1298 and BUG-1310 both having reached the merge phase: in those runs the hold landed mid-pipeline.

    So the fourteen-PR blast radius I wrote earlier overstates THIS defect specifically. The fourteen held PRs are a blast radius for the pair; how they split between the two paths depends on when the hold lands relative to the pipeline. BUG-1265 catches the common case; BUG-1447 catches the race. Both still want fixing, and neither spec currently mentions the other — which is the thing I would most want changed, because a fix to either one alone will look like it solved the problem while the other path stays open.

    Keeping my earlier comment as written rather than editing it, since the prediction and its falsification are more useful together than a tidied-up version of either.
  created_at: 2026-09-20T16:24:18.034285193Z
  modified_at: 2026-09-20T16:24:18.034285193Z
  session_id: 01a079fe-4ad8-7ee0-8086-16d6a1b7f0e5
