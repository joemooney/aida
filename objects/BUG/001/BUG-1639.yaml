id: 01a0dc0a-5c93-75b1-8704-cdacd85b64ce
spec_id: BUG-1639
agreed_id: BUG-1639
title: 'Non-Claude skill packs: resolve real vendor discovery paths (.agents/skills vs .codex/.antigravity), then drive scaffolder + sync-templates from one derived manifest (aida-handoff et al. missing)'
description: |
  Codex has no `aida-handoff` skill. The Codex pack (`.codex/skills/`) and the Antigravity pack (`.antigravity/skills/`) are generated from a **hand-maintained allow-list** of 23 skills (`codex_skill_defs` / `antigravity_skill_defs` in `aida-core/src/scaffolding/mod.rs`). `aida-handoff` was added later (STORY-1006) and never made it onto either list. The legacy `~/.codex/prompts/` pack has no handoff prompt either, and Codex ≥0.142 does not discover that directory anyway (BUG-1095).

  This matters because seat rotation (STORY-1464) depends on a seat handing off before it exits, and Codex holds seats too.

  The `aida-handoff` master (`aida-core/templates/skills/aida-handoff.md`) uses no Claude-only harness tools, so it can ship to Codex as it is. No `skills-portable/` variant is needed.

  The underlying defect is that the allow-list drifts by default: every new skill silently misses the other vendors unless someone remembers to add it. Other skills are missing from those lists as well (e.g. `aida-human-audit`, `aida-fleet-watch`, `aida-grill`, `aida-triage`).

  ## Acceptance
  - `aida-handoff` is scaffolded into `.codex/skills/aida-handoff/SKILL.md` and `.antigravity/skills/aida-handoff/SKILL.md`, and delivered to existing installs by `aida scaffold refresh`.
  - The Codex and Antigravity pack lists are **derived** from the Claude skill set minus an explicit, commented exclusion list (skills that are Claude-only and have no `skills-portable/` body), rather than hand-listed.
  - A test fails when a Claude skill master exists that is neither shipped to Codex/Antigravity nor on the exclusion list, so future skills cannot silently miss the other vendors.
status: Draft
priority: Medium
owner: ''
feature: Uncategorized
created_at: 2026-09-26T04:47:41.715373320Z
modified_at: 2026-09-26T05:09:46.956085563Z
req_type: Bug
tags:
- antigravity
- codex
- parity
- scaffolding
- skills
comments:
- id: 01a0dc11-02d6-71c0-8f55-7532101e1b0e
  author: joe
  content: 'Product clarification: in the AIDA source repo, the temporary recovery path may symlink the 23 current Codex scaffold skills plus aida-handoff into .codex/skills/<name>/SKILL.md, selecting skills-portable/<name>.md where it exists and the master skill otherwise. At present only aida-orchestrate has a skills-portable file; aida-handoff correctly falls back to its master. The durable developer-repo fix should extend make sync-templates to manage .codex/skills/ and .antigravity/skills/ from the same canonical derived inventory and vendor-selection logic, without broad scaffold apply rewriting project docs. Verify Codex follows the symlinked SKILL.md form before relying on the links.'
  created_at: 2026-09-26T04:54:57.494936455Z
  modified_at: 2026-09-26T04:54:57.494936455Z
- id: 01a0dc15-56ea-7830-9d37-e2167d0b3468
  author: joe
  content: |-
    Scope expansion (2026-09-25, operator + Antigravity review):

    1. DISCOVERY PATH IS UNSETTLED — verify before fixing. The scaffolder writes Codex skills to .codex/skills/ and Antigravity skills to .antigravity/skills/. But AIDA's own docs/agents/porting-claude-code-to-codex.md (§A.1) says Codex skills live at .agents/skills/<name>/SKILL.md, and Antigravity reports it discovers workspace skills from .agents/skills/ (walking cwd → repo root), not .antigravity/skills/. BUG-1117 asserted .codex/skills/ is the discoverable Codex surface. These cannot all be right. Acceptance: an empirical check per vendor (current Codex CLI, Antigravity CLI) that records which directories each discovers, whether it follows a symlinked SKILL.md, and the vendor version tested. Record the outcome in docs/agents/seat-parity.md and fix whichever of the scaffolder / porting doc / BUG-1117 note is wrong.
    2. If both vendors read .agents/skills/, scaffold ONE shared pack there (portable body wins over the Claude master) instead of two per-vendor dirs. Keep the old dirs working only as long as a supported vendor version still reads them.
    3. Dev repo: extend make sync-templates (and make check-templates) to link the non-Claude pack as symlinks into aida-core/templates, the same way .claude/skills is linked, so this repo dogfoods Codex/Antigravity skills without running scaffold apply (which would also rewrite README.md/CLAUDE.md/AGENTS.md here).
    4. .agents/ is currently untracked in this repo and holds a third-party skill (typesafe-ai, MIT). Decide whether that directory is tracked, and whether third-party skills belong in it or in a gitignored local area.
  created_at: 2026-09-26T04:59:41.162430675Z
  modified_at: 2026-09-26T04:59:41.162430675Z
  session_id: 01a0d0c8-1a2e-7430-a480-c859ff5cfe99
- id: 01a0dc19-f1c2-7000-a0a7-4622fdc4c9b7
  author: joe
  content: |-
    Refinements from Codex review (2026-09-25), folded into acceptance:
    - Test matrix, recorded with exact client versions and whether /skills lists the skill AND $aida-handoff invokes it: Codex × .agents/skills × {regular file, symlink}; Antigravity × .agents/skills × {regular file, symlink}; plus the existing .codex/skills and .antigravity/skills as compatibility cases. OpenAI's current migration guidance maps Claude skills to .agents/skills/<name>/SKILL.md, so that is the leading candidate for the canonical path — but the matrix decides it.
    - Namespace ownership: AIDA manages ONLY aida-* entries under .agents/skills/. It must never create, prune, or refresh anything else there (e.g. the existing third-party typesafe-ai skill, or local/). Test: scaffold apply/refresh/prune leaves a non-aida skill directory byte-identical.
    - Likely target (confirm after the matrix): .agents/skills/ = canonical portable pack; .codex/skills/ and .antigravity/skills/ kept only as compatibility paths if a supported vendor version still needs them.
    - ONE derived manifest (the Claude skill set minus a commented Claude-only exclusion list, portable body preferred) feeds the scaffolder, scaffold refresh, and make sync-templates/check-templates. No second hand-kept list.
    - Sequencing: run the one-skill aida-handoff test first. Do NOT link the full pack until the matrix results are recorded here.
  created_at: 2026-09-26T05:04:42.946415669Z
  modified_at: 2026-09-26T05:04:42.946415669Z
  session_id: 01a0d0c8-1a2e-7430-a480-c859ff5cfe99
- id: 01a0dc1b-2a0e-7181-a380-3d2c8484b3fe
  author: joe
  content: |-
    Test-matrix progress (2026-09-25 22:05):
    - Setup: .agents/skills/aida-handoff/SKILL.md is a relative symlink to aida-core/templates/skills/aida-handoff.md and resolves (frontmatter verified).
    - Versions on this machine: Antigravity CLI (agy) 1.2.11, Codex CLI 0.157.0.
    - Antigravity × .agents/skills: agy reports that it indexes .agents/skills/*/SKILL.md at session start, and only on restart. It has NOT yet been observed listing aida-handoff in a fresh session, so the symlink cell is still open.
    - Codex × .agents/skills: indirect evidence only. agy found a skills host-root reference to .agents in the Codex 0.157.0 binary (host_roots.rs), which agrees with OpenAI's migration guidance. The fresh-session check (/skills lists it, $aida-handoff invokes it) is still pending.
    - Not yet run: the regular-file (cp) variants, and the .codex/skills / .antigravity/skills compatibility cases.
  created_at: 2026-09-26T05:06:02.894805808Z
  modified_at: 2026-09-26T05:06:02.894805808Z
  session_id: 01a0d0c8-1a2e-7430-a480-c859ff5cfe99
- id: 01a0dc1e-954c-7011-880c-87d35f04968e
  author: joe
  content: |-
    TEST RESULTS (2026-09-25, run by Antigravity against the local runtimes; Codex skill-root listing observed):
    | | Codex 0.157.0 | Antigravity 1.2.11 |
    |---|---|---|
    | Discovers .agents/skills/ | YES (listed as skill root r5 = <repo>/.agents/skills) | YES |
    | Symlinked SKILL.md | NO, silently skipped (the walker's DirEntry::file_type does not follow symlinks) | YES |
    | Regular-file copy | YES, indexed (aida-handoff listed with its description) | YES |
    | Hard link | YES | YES |
    | Symlinked skill DIRECTORY that contains a regular SKILL.md | YES | not tested |

    DECISIONS THIS IMPLIES:
    1. Canonical non-Claude path = .agents/skills/<name>/SKILL.md, shared by Codex and Antigravity. The .codex/skills and .antigravity/skills packs become compatibility-only; drop them unless a supported vendor version still needs them. Also correct BUG-1117's claim and align docs/agents/seat-parity.md.
    2. SKILL.md files for Codex MUST be regular files. Symlinks, the pattern used for .claude/, do not work for Codex, and the failure is silent.
    3. Dev repo (make sync-templates): write generated REGULAR-FILE COPIES of each master (portable body preferred), and make make check-templates fail on any byte drift from the master. Do NOT rely on hard links: an editor's atomic save (write-temp-then-rename) or a git checkout/rebase replaces the file, which silently splits the link, so the copy drifts with no signal. A drift check against copies is visible and deterministic. The generated copies can be gitignored in this repo, since make regenerates them.
    4. Downstream scaffolder: already writes regular files with checksum markers. Retarget it to .agents/skills/ and manage only aida-* entries (typesafe-ai and local/ must stay untouched).
    5. Silent-skip guard: aida doctor (or scaffold status) flags any .agents/skills/*/SKILL.md that is a symlink, because Codex will not see it.

    Current test state: .agents/skills/aida-handoff/SKILL.md is a HARD LINK to the master (same inode 41943280). It works for both vendors today but is fragile per point 3.
  created_at: 2026-09-26T05:09:46.956085563Z
  modified_at: 2026-09-26T05:09:46.956085563Z
  session_id: 01a0d0c8-1a2e-7430-a480-c859ff5cfe99
filed_at:
  code_sha: 68ab16316787b6252da9cfcc6db9bbe72262edec
  branch: main
  dirty: true
  aida_version: 0.15.0
  aida_build_sha: f9c900b6ff
  vendor: claude
  session: 01a0d0c8-1a2e-7430-a480-c859ff5cfe99
