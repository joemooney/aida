id: 01a0b2bf-39f2-7f91-9f14-69d808dd20fe
spec_id: BUG-1209
agreed_id: BUG-1209
title: 'aida list --limit N human footer prints ''N requirements'' with no denominator — the agent/TOON surface already prints ''count: N of M'''
description: |-
  `aida list --limit 5` prints a footer of `5 requirements` whether 5 matched or 500 did. The truncation is invisible, so a human reads a bounded slice as the complete set.

  The agent surface already gets this right. Same store, same moment:

    aida list --format toon --limit 5   ->  count: 5 of 10 open
    aida list --format human --limit 5  ->  5 requirements

  The denominator is already computed and in scope. git_backend_cmd.rs captures total_after_filters immediately before reqs.truncate(n), and the human path already has a count-of-total renderer — it is just gated on agent_default_cap.is_some(). agent_list_default_cap (lib.rs:1574) returns None whenever limit.is_some(), so the one case where the user explicitly asked for a slice is the one case the hint is suppressed. The TASK-970 implicit agent cap gets the disclosure; the explicit --limit does not.

  Three human footer sites print the bare count: git_backend_cmd.rs lines 1877, 1898, 2099 (println of "{} requirements", reqs.len()).

  Proposed: when total_after_filters > reqs.len(), the human footer reads `5 of 10 requirements (--limit 5; drop it or raise N to see the rest)`; when nothing was trimmed it stays exactly `10 requirements`. Reuse list_count_denom_label so the human and TOON denominators agree rather than drifting into two vocabularies.

  Note this is the same class as the existing closed/decisions hidden-count hints, which DO fire on the human path — the limit is the only silent truncation left.

  Acceptance:
  - `aida list --limit N` where more than N matched prints the count and the total, and names --limit as the cause.
  - `aida list --limit N` where N or fewer matched is byte-identical to today.
  - All three human footer sites covered (plain, --show-tags, and the third render branch).
  - The human denominator label matches the TOON one for the same query (open lens vs matched).
  - TOON / --json / --short output unchanged.
status: InProgress
priority: Medium
owner: ''
feature: Uncategorized
created_at: 2026-09-18T04:21:11.794027533Z
modified_at: 2026-09-18T06:45:50.211144296Z
req_type: Bug
tags:
- aida:list
- batch:followups-0917b
- papercut
- proxy-groomed
- surface-parity
relationships:
- rel_type: !Custom implemented-by
  target_id: 01a0b337-1b82-73a0-a98d-4dd74da2cb82
  created_at: 2026-09-18T06:32:13.286078109Z
  created_by: joe
comments:
- id: 01a0b343-a024-7140-a1bb-6938b3ca5f65
  author: joe
  content: |-
    [rework ROUND 2 — one item still open; the previous commit on this branch is already on the PR and does NOT count. Produce a NEW commit, or punt naming the finding you dispute.]
    PR #1917 RequestChanges (drain reviewer, .aida/review-verdicts/PR-1917.json): `aida list --format human --limit 0` is accepted, truncates N matched rows to zero, and prints only "No requirements found." with no disclosure. Handle the empty human-render path: when an explicit --limit truncated a non-empty match set to zero, print the same footer shape as the non-zero case ("0 of N open — truncated by --limit 0" or the renderer's existing count-of-total form) instead of the no-results message; the genuine no-match case keeps "No requirements found.". Add a regression test in bug_1209_list_limit_footer_tests for --limit 0 with N>0 matches (footer discloses N) and for a true empty match (no footer). Consider whether --limit 0 should be rejected by clap (value_parser range 1..) — if you choose rejection instead, say so and test the error text; either is acceptable, silent emptiness is not.
    Finish with aida queue done BUG-1209.
  created_at: 2026-09-18T06:45:48.708507565Z
  modified_at: 2026-09-18T06:45:48.708507565Z
- id: 01a0b343-a501-7133-a958-729a5b7e8055
  author: joe
  content: |-
    REVIEW FINDINGS TO ADDRESS (PR #1917):
    Verdict: CHANGES REQUESTED
    Findings:
    1. BUG-1209: aida list --format human --limit 0 is accepted, truncates 13 matched open requirements to zero, and prints only 'No requirements found.' with no 0-of-13/--limit disclosure. Reproduce with /home/joe/ai/aida/target/debug/aida list --format human --limit 0. Handle the empty human-render path (and add regression coverage) so every explicitly truncated human list discloses the filtered total.
    Contract: produce at least one commit, or punt explicitly naming the finding you dispute; never pass through silently with no changes.
  created_at: 2026-09-18T06:45:49.953265894Z
  modified_at: 2026-09-18T06:45:49.953265894Z
execution_mode: drain
implementation_info:
  implemented: true
  implemented_at: 2026-09-18T06:32:16.242171907Z
  implemented_by: joe
failure_reason:
  phase: reviewer
  phase_index: 3
  kind: verdict:request-changes
  detail: reviewer verdict is RequestChanges — not Approved
  recovery_hint: 'Address the review feedback, then push fixups: `aida queue rework BUG-1209 --work`'
  shelved_by: advisor
  shelved_at: 2026-09-18T06:44:51.279323773Z
