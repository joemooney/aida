id: 01a0b23b-21ab-7cf3-9fbf-6cb73d60162f
spec_id: BUG-1199
agreed_id: BUG-1199
title: harvest/reconstitute (and other raw current_exe() callers) fail with 'No such file or directory' after the drain's phase-6 rebuild replaces the aida binary — the running process's current_exe() becomes '<path> (deleted)'; ledger + advisor brief lost
description: |-
  Observed 2026-09-17 18:52 in the followups-0917 drain, member TASK-1253 / PR-1904 (second live run of the ADR-45 advisory harvest). The step ran the agent, filtered 4 → 3 candidates, wrote .aida/harvest/task-1253-drain-01a0b236-….json, then failed on the ledger write: "⚠ advisory harvest did not complete (could not run `aida comment add TASK-1253 [aida:harvest] …<whole ledger body>…" — the with_context("could not run …") branch of harvest::run_aida, i.e. Command::output() itself errored (not a non-zero exit). The underlying io error is appended AFTER the multi-line ledger body and was cut off in the drain log, so the cause is unknown. The advisor later ran `aida harvest TASK-1253 --from <file> --yes-all` successfully (same comment-add path), so the failure was transient.

  Consequences: (1) the propose-only [aida:harvest] ledger — the audit record ADR-45 requires on every run — was never written for this run (the confirm run's ledger exists, but the drain's proposed/skipped-with-reasons record is gone); (2) the advisor brief was not filed (it follows the ledger write), so nothing surfaced in aida awaiting — the candidates were only found by reading the drain log; (3) the error is undiagnosable from the log.

  Fix:
  - In harvest::propose_only, write the ledger body to <candidates-file>.ledger.md (or embed it in the candidates JSON) BEFORE attempting the comment write, so the audit survives a store/exec failure; on failure, print the io error FIRST and the path of the persisted ledger, not the whole body.
  - File the advisor brief even when the comment write fails (the brief is local runtime state and is what makes the proposal visible in aida awaiting).
  - Add a bounded retry (the store may be briefly locked by a concurrent write — cf. CacheLocked) before giving up.
  - In run_aida, include the io::Error kind + os error in the context and truncate the echoed args (never dump a multi-line body into an error line).
  Also worth a follow-up: `aida harvest <SPEC> --from <file>` should detect an unwritten drain ledger next to the file and fold it into the confirm ledger so the record is complete.

  Acceptance:
  - Simulated comment-add failure in propose_only → ledger persisted next to the candidates file, brief filed, error line ≤ 2 lines naming the io error; drain continues to merge.
  - Existing propose-only ledger test extended; a unit test for the persisted-ledger path.
status: Approved
priority: High
owner: ''
feature: Uncategorized
created_at: 2026-09-18T01:56:54.827970711Z
modified_at: 2026-09-18T02:32:47.012967618Z
req_type: Bug
tags:
- batch:exe-0917
- batch:followups-0917
- current-exe
- drain
- drain-safe
- follow-up:TASK-1249
- harvest
- ledger
comments:
- id: 01a0b259-d70f-7362-90b0-6cec2432f4ae
  author: joe
  content: |-
    ROOT CAUSE (advisor, 2026-09-17 19:30, reproduced twice: TASK-1253 at 18:52 and TASK-1258 at 19:28, both AFTER the first batch member's phase 6 'build verify' rebuilt target/release/aida at 18:28 / 19:16):
    the drain log's full error is "could not run `aida comment add … --author harvest`: No such file or directory (os error 2)". harvest::aida_exe() (harvest.rs:723) and reconstitute::aida_exe() (reconstitute.rs:243) call std::env::current_exe() per invocation; once cargo has replaced the binary the still-running drain's /proc/self/exe reads "<path> (deleted)", which Command::new cannot spawn. The orchestrator itself is immune because RealPhaseDriver resolves self.aida_exe ONCE at construction; solo_cmd.rs:18 and pr_cmd.rs:~1895 already special-case the "(deleted)" suffix. TASK-1255's harvest (18:27, before the first rebuild) succeeded — consistent.

    Scope widening: this is a CLASS bug — every raw current_exe() subprocess site in a long-lived process breaks after a rebuild (harvest, reconstitute, pr_review_story_already_exists in lib.rs, the TUI's app.rs aida_exe(), …). Fix: one shared crate::aida_exe_path() that (1) strips a trailing " (deleted)" and re-validates the path exists, (2) falls back to `aida` on PATH, (3) is resolved once and cached for the process; replace all raw call sites; add a unit test for the "(deleted)" strip. Keep the ledger-persist-before-comment + brief-regardless items from the original description — they are still right (any spawn failure must not lose the audit).
  created_at: 2026-09-18T02:30:27.343867959Z
  modified_at: 2026-09-18T02:30:27.343867959Z
  session_id: 01a0adee-16e1-7982-ac9f-e15abb207b6c
- id: 01a0b25a-1b18-7ec2-a43b-54271a89b459
  author: joe
  content: 'Implementation pointer: the hardened resolver already exists — resolve_aida_exe() (BUG-562, used by solo_cmd.rs: strips the ''(deleted)'' suffix and falls back to the on-PATH aida) and pr_ship_post_merge_aida_exe() (SPEC-411, pr_cmd.rs). Do not write a third: consolidate on resolve_aida_exe(), make harvest::aida_exe / reconstitute::aida_exe / lib.rs pr_review_story_already_exists / aida-tui app.rs aida_exe call it, then sweep the remaining raw std::env::current_exe() subprocess sites (grep shows ~36 across autoprogress, compete_cmd, drain_cmd, maintenance_schedule, supervise_cmd, supervisor, upgrade_cmd, plan_cmd, queue_cmd, session — judge each: long-lived process → must use the resolver). Add a clippy-style source guard test: no  outside the resolver module.'
  created_at: 2026-09-18T02:30:44.760764591Z
  modified_at: 2026-09-18T02:30:44.760764591Z
  session_id: 01a0adee-16e1-7982-ac9f-e15abb207b6c
- id: 01a0b25a-9f12-7202-b698-95c057f72bf2
  author: joe
  content: '(correction to the previous comment''s last sentence, which lost a word to shell quoting): add a source-guard test asserting that no file outside the resolver module calls std::env::current_exe() directly.'
  created_at: 2026-09-18T02:31:18.546337595Z
  modified_at: 2026-09-18T02:31:18.546337595Z
  session_id: 01a0adee-16e1-7982-ac9f-e15abb207b6c
execution_mode: drain
