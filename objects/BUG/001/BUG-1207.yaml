id: 01a0b2b5-d13b-7361-8e0a-3bdab7f18cba
spec_id: BUG-1207
agreed_id: BUG-1207
title: 'aida list --format human never truncates the Title column: long titles wrap and shred the table on a narrow terminal'
description: |-
  git_backend_cmd.rs sets title_max = if show_tags { 50 } else { usize::MAX }, so the no-tags path (the default `aida list`) prints req.title at full length. A 150-char title wraps across 2-3 terminal lines and destroys the column alignment for every row after it.

  Every other column is fixed-width padded (14/12/13/10), so the fix is to derive the Title budget from the actual terminal width: total_cols - (flow_width + 53) with a sane floor (~24) and a fallback when stdout is not a tty (no truncation, current behavior, since the consumer is then a pipe or a file).

  crossterm is already a workspace dependency (aida-tui uses it), so crossterm::terminal::size() needs no new crate.

  Workarounds today: `tput rmam` before the command (terminal-level clip, keeps color), or piping to `cut -c1-$(tput cols)` (aida drops color when not a tty, so byte-cut is safe), or `less -SR`.

  Acceptance:
  - Default `aida list --format human` at a tty fits every row in one terminal line, with an ellipsis on truncated titles.
  - Non-tty stdout (pipe, file, --format toon/json) is unchanged.
  - The header rule width matches the computed title column.
  - Applies to the same table in the --tags branch (currently a hardcoded 50).
status: Completed
priority: Medium
owner: ''
feature: Uncategorized
created_at: 2026-09-18T04:10:55.163831878Z
modified_at: 2026-09-18T07:42:19.986211482Z
req_type: Bug
tags:
- aida:list
- batch:followups-0917b
- papercut
- proxy-groomed
- severity:cosmetic
relationships:
- rel_type:
    custom: implemented-by
  target_id: 01a0b32d-1c35-76d0-b66d-804c0dd73475
  created_at: 2026-09-18T06:21:19.293451261Z
  created_by: joe
comments:
- id: 01a0b331-31ab-7ac1-bb93-5b38cfe06807
  author: joe
  content: |-
    [rework ROUND 2 — items still open; the previous commit on this branch is already on the PR and does NOT count. Produce a NEW commit that changes these, or punt naming the finding you dispute.]
    PR #1916 RequestChanges (drain reviewer, .aida/review-verdicts/PR-1916.json):
    1. --show-origin: the title budget still subtracts the default 53-column fixed prefix, but --show-origin's fixed columns consume 56; at 120 cols the header/rows overflow and the divider is 3 short of the title boundary. Compute the fixed-prefix width from the columns actually rendered for the active flag set (one function that returns the prefix width for {default, --show-origin, --tags, combinations}) and derive the title budget from it.
    2. --tags: the title takes the entire remaining width and the tag chips are appended after it, so tagged rows still overflow. Reserve the chip width (or a capped chip budget) before computing the title budget so title + chips <= terminal width.
    3. Tests: add rendered-table coverage that asserts every row/header/divider length <= width for default, --show-origin, --tags, --show-origin --tags, at a couple of widths (80, 120), and that the non-TTY path is untouched (no truncation). Use the existing table renderer with an injected width rather than a real terminal.
    Also: if you added a new crate for terminal width, prefer the workspace crossterm (already a dependency elsewhere) so no new external dep enters the graph; keep the non-TTY fallback exactly as before.
    Finish with aida queue done BUG-1207.
  created_at: 2026-09-18T06:25:40.779957788Z
  modified_at: 2026-09-18T06:25:40.779957788Z
- id: 01a0b331-3633-78a3-8a73-6fb773b33082
  author: joe
  content: |-
    REVIEW FINDINGS TO ADDRESS (PR #1916):
    Verdict: CHANGES REQUESTED
    Findings:
    1. BUG-1207: --show-origin still computes title width with the 53-column default prefix even though its fixed columns consume 56 columns; at 120 columns the header/rows exceed the terminal and the divider is three columns short of the rendered title boundary.
    2. BUG-1207: --tags gives the title the entire remaining terminal width and then appends a space plus tag chips, so tagged rows still exceed the terminal width; add rendered-table coverage for default, --show-origin, --tags, and non-TTY output.
    Contract: produce at least one commit, or punt explicitly naming the finding you dispute; never pass through silently with no changes.
  created_at: 2026-09-18T06:25:41.939519973Z
  modified_at: 2026-09-18T06:25:41.939519973Z
- id: 01a0b356-e22a-7ee2-a1c0-4ecd4dec84ca
  author: joe
  content: |-
    [rework ROUND 3 — one item still open; commits 7de6e89d1 + 88cf4ac91 are already on the PR and do NOT count. Produce a NEW commit, or punt naming the finding you dispute.]
    PR #1916 RequestChanges (drain reviewer, .aida/review-verdicts/PR-1916.json): non-TTY output must be byte-identical to pre-PR. Before this PR, piped `aida list --show-tags` truncated titles to 50 columns and used the fixed table geometry; render_list_table now passes usize::MAX for the title and derives tag/rule widths from content when terminal_width is None, and the new test non_tty_rendering_does_not_truncate_title_or_tags pins that CHANGED behaviour. Fix: when stdout is not a TTY (terminal_width None) reproduce the pre-PR geometry exactly — title_max 50 with --show-tags, usize::MAX without, fixed column widths as before — and confine all terminal-aware sizing to the TTY path. Replace the incorrect test with a regression test that renders piped --show-tags output for a fixture and asserts it equals the pre-PR rendering (capture the expected string from main, not from the branch). Keep the TTY-width tests from round 2.
    Finish with aida queue done BUG-1207.
  created_at: 2026-09-18T07:06:50.794208359Z
  modified_at: 2026-09-18T07:06:50.794208359Z
- id: 01a0b356-e699-77e3-afd1-edd19adaeb72
  author: joe
  content: |-
    REVIEW FINDINGS TO ADDRESS (PR #1916):
    Verdict: CHANGES REQUESTED
    Findings:
    1. BUG-1207: preserve the pre-PR non-TTY --show-tags behavior. Previously titles were truncated to 50 characters and the table used its existing fixed geometry even when stdout was piped; render_list_table now passes usize::MAX for the title and derives tag/rule widths from content when terminal_width is None, while non_tty_rendering_does_not_truncate_title_or_tags asserts the incompatible new behavior. Add a regression test matching the old piped --show-tags output and keep terminal-aware sizing confined to TTY output.
    Contract: produce at least one commit, or punt explicitly naming the finding you dispute; never pass through silently with no changes.
  created_at: 2026-09-18T07:06:51.929714787Z
  modified_at: 2026-09-18T07:06:51.929714787Z
- id: 01a0b36a-8c78-7262-913d-6e2a068ed8b2
  author: joe
  content: 'PR #1916 review fix: restored legacy non-TTY --show-tags geometry (50-column title truncation, untruncated formatted tags, fixed 111/113 rule widths) while keeping terminal-aware sizing confined to TTY output. Added regression coverage for default piped --show-tags output.'
  created_at: 2026-09-18T07:28:19.576590996Z
  modified_at: 2026-09-18T07:28:19.576590996Z
  session_id: 01a079fe-4ad8-7ee0-8086-16d6a1b7f0e5
- id: 01a0b377-5f52-71e2-89cf-6118ff2a318f
  author: joe
  content: |-
    [aida:proxy-approval]
    pr: #1916 [AI:codex] fix(list): fit human titles to terminal width (BUG-1207) — round 3
    verdict: APPROVE + merged
    proxy: claude driver-advisor, session 01a0adee
    independent: yes (implementer = codex headless, three rounds; drain reviewer verdicts r1/r2 = RequestChanges (width budgets per flag set; then piped --show-tags geometry changed), r3 reviewer exited 1 twice before launching — no verdict; this advisor reviewed the round-3 diff directly; verdict .aida/review-verdicts/PR-1916-advisor.json)
    gates: Build (ubuntu-latest) SUCCESS, merge-hold-gate SUCCESS, MERGEABLE; merged from a plain shell, no override
    reasoning: TTY path fits the title to the real terminal width with the fixed-prefix width computed per flag set and the tag-chip budget reserved; non-TTY path reproduces the pre-PR geometry exactly (title 50 with --show-tags, unbounded without, tags untruncated, legacy fixed totals, legacy header) and is pinned by non_tty_show_tags_preserves_legacy_fixed_geometry; workspace crossterm, no new crate. No harvest ran (the drain never reached phase 4; pr ship does not harvest).
    merged: 2026-09-18 00:44 -07:00 → main 4fa18dab8b; spec auto-bumped Completed
  created_at: 2026-09-18T07:42:19.986211482Z
  modified_at: 2026-09-18T07:42:19.986211482Z
  session_id: 01a0adee-16e1-7982-ac9f-e15abb207b6c
history:
- id: 01a0b376-3d28-7cd1-9c5f-05aaadfedadd
  author: aida-auto-bump
  timestamp: 2026-09-18T07:41:05.704366542Z
  changes:
  - field_name: status
    old_value: Done
    new_value: Completed
processing_record:
- id: 01a0b376-3d2f-7681-a865-3cf7e3b2b160
  timestamp: 2026-09-18T07:41:05.711198240Z
  agent: aida
  commit_sha: 4fa18dab8b0d1f94720f0a4d87380e618d1f42b3
  summary: Completed via merge 4fa18dab
  punted:
  - 'punted: reviewer verdict is RequestChanges — not Approved'
  - 'punted: reviewer verdict is RequestChanges — not Approved'
  - 'punted: the reviewer session exited 1 (attempt 2/2)'
execution_mode: drain
implementation_info:
  implemented: true
  implemented_at: 2026-09-18T06:10:51.430547856Z
  implemented_by: joe
  completed_at: 2026-09-18T07:41:05.704164367Z
  completion_sha: 4fa18dab8b0d1f94720f0a4d87380e618d1f42b3
