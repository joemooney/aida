id: 01a0c204-5753-78d3-abe8-93a94934f79d
spec_id: BUG-1516
agreed_id: BUG-1516
title: A verdict's recorded sha is sometimes abbreviated, so a gate comparing it to the PR head by string equality reports stale for one in five current verdicts
description: |
  A verdict's recorded sha is sometimes abbreviated and sometimes full, so any gate comparing it to a PR's head by string equality reports STALE for a verdict that is current. Measured 2026-09-20: 24 of 124 sha-bearing verdicts are abbreviated.

      reviewed_sha lengths across .aida/review-verdicts/
        40 chars  99    full
        10 chars  16
         9 chars   3
         8 chars   4
        11 chars   1
         0 chars   1

  So roughly one in five sha-bearing verdicts fails an equality test against the 40-character head that `gh pr view --json headRefOid` returns.

  DEMONSTRATED, not inferred. PR #2045's verdict records reviewed_sha 8258dfc4b9. The PR's head is 8258dfc4b963497fe7392f1242d30f18beb5fc06 - the same commit. The advisor's stranded-refusal sweep compared them with string equality and reported STALE-SHA, which is a false stale on a verdict recorded minutes earlier at that exact head. The sweep was written by the seat that then had to diagnose its own output.

  WHO THIS BREAKS. Every consumer that answers "was this verdict recorded against the code about to merge" by comparing the two strings:
    - STORY-1405's merge-time gate, whose entire purpose is that comparison
    - BUG-1505 criterion 5, which requires distinguishing approved, refused and CANNOT-DETERMINE
    - the advisor's per-tick protective sweep, demonstrated above
    - anything built on BUG-1505's migration, since the migration normalises schema and spelling
      and would not by itself normalise sha length

  THE FAILURE DIRECTION IS SAFE AND THE COST IS NOT. A false stale fails CLOSED - a gate that cannot confirm the verdict matches refuses, which is correct behaviour on an incorrect input. But it produces churn indistinguishable from real staleness: a reviewer re-reviewing work they already reviewed, and a seat unable to tell a genuinely moved head from a differently-rendered one. On a corpus where one in five reads stale, a gate that refuses on stale is a gate that refuses one in five valid verdicts.

  Acceptance:
  1. Comparison is by commit identity, not string equality. Either compare the shorter prefix against the longer, or resolve both through git rev-parse before comparing. Prefix comparison must require a minimum length - a 4-character prefix is not an identity claim.
  2. New verdicts record the FULL 40-character sha. One writer already does this for 99 of them; the abbreviated ones come from paths that pass an already-abbreviated string through.
  3. The 24 existing abbreviated verdicts are resolved to full shas where the commit is still reachable, and marked unresolvable where it is not - a deleted branch's abbreviated sha may no longer resolve.
  4. A gate that cannot establish identity reports CANNOT-DETERMINE distinctly from STALE. They are different states with different remedies: stale means re-review, cannot-determine means the record is unusable. Collapsing them is the PRIN-5 failure at the point this spec is about.
  5. A test compares an abbreviated recorded sha against a full head and asserts they are recognised as the same commit.

  RELATED: this is the same shape as the marker-versus-forge comparison in TASK-1-170 and the verdict-spelling normalisation in BUG-1505 - a comparison that cannot distinguish a difference in REPRESENTATION from a difference in VALUE. Third instance tonight.
status: Approved
priority: High
owner: ''
feature: Uncategorized
created_at: 2026-09-21T03:30:59.539323012Z
modified_at: 2026-09-21T03:35:40.887763998Z
req_type: Bug
tags:
- batch:night-0920 merge-safety integrity drain-safe from:session-0920
relationships:
- rel_type: References
  target_id: 01a0c0b3-03d6-7190-a8f5-8bc1a15a751e
  created_at: 2026-09-21T03:31:25.750157705Z
  created_by: joe
- rel_type: References
  target_id: 01a0c194-809e-7111-a5c6-095faa47a23d
  created_at: 2026-09-21T03:31:26.776952501Z
  created_by: joe
- rel_type: References
  target_id: 01a0c1ee-defb-7cd0-b234-051939e4dcad
  created_at: 2026-09-21T03:31:27.748047376Z
  created_by: joe
comments:
- id: 01a0c206-ad2d-7e23-9044-ead6b1df6b09
  author: joe
  content: |-
    [aida:advisor] TWO CORRECTIONS, 2026-09-20 20:36. The denominator in this spec is ambiguous and must name its set; and the fix location is the WRITE boundary, not the comparison sites.

    1. THE DENOMINATOR. This spec says "24 of 124 sha-bearing verdicts". The reviewer seat measured 20 of 81 and could not reproduce 124. Both are correct and they count different sets:

        files in .aida/review-verdicts        552
        non-empty reviewed_sha                 81      <- the reviewer's set
        non-empty head                         44
        non-empty head_sha                      2
        any of those keys, first match        123 (+1 empty = 124)   <- this spec's set

    So a verdict's sha lives under three different key names, and which denominator is right depends on whether a consumer reads one key or falls back across them. A gate that reads only reviewed_sha sees 81; one that falls back sees 123. BOTH RATIOS HOLD FOR THEIR SET - roughly one in four on reviewed_sha, roughly one in five across all keys - and the conclusion is unchanged either way.

    THE THREE-KEY SPREAD IS ITSELF PART OF THE DEFECT and was not stated. A consumer must already fall back across reviewed_sha, head_sha and head before it can compare anything. Normalising sha LENGTH while leaving the sha KEY ambiguous closes half the problem.

    2. THE ABBREVIATION IS A WRITE-BOUNDARY DEFECT IN ONE PATH, NOT A CORPUS-WIDE EMISSION PROBLEM. Measured by writer:

        aida review record              full 22   abbreviated 17
        aida drain reviewer             full 33   abbreviated  0
        claude-reviewer-1 (hand)        full  0   abbreviated  2
        advisor (proxy for operator)    full  1   abbreviated  1
        codex / codex review / none     full  5   abbreviated  0

    THE DRAIN HAS NEVER WRITTEN ONE. It passes the full head every time, across 33 verdicts. Every abbreviated sha came from a hand-driven path, and the reason is mundane: a human or seat copies a ten-character sha out of its own gh output and passes it through. The reviewer seat wrote 17 of the 20 and disclosed it unprompted; one of the remaining three is the advisor's, under the operator-proxy path.

    CONSEQUENCE FOR THE FIX, and it changes criterion 2's emphasis: normalise AT THE WRITE BOUNDARY in review record - resolve whatever is passed to a full 40-character sha before persisting - rather than teaching every comparison site to handle both. The comparison-site tolerance in criterion 1 is then a MIGRATION concern for the existing 20 plus the 46 verdicts whose sha lives under a different key, not an ongoing requirement.

    This also means the defect stops the moment one verb changes, which is a materially cheaper fix than the spec as filed implies.
  created_at: 2026-09-21T03:33:32.589417939Z
  modified_at: 2026-09-21T03:33:32.589417939Z
  session_id: 01a0adee-16e1-7982-ac9f-e15abb207b6c
- id: 01a0c208-a257-70c0-bfbe-958bf680bb1d
  author: joe
  content: |-
    [aida:advisor] THE KEY SPREAD IS WORSE THAN A FALLBACK INCONVENIENCE, AND THERE ARE THREE CANONICAL DECISIONS TO MAKE, NOT ONE. Measured 2026-09-20 20:38 by the reviewer seat, reproduced here.

        reviewed_sha only          77
        head only                  42
        head + reviewed_sha         2
        head_sha + reviewed_sha     2

    FORTY-TWO FILES CARRY `head` AND NO `reviewed_sha`. A consumer that reads only reviewed_sha - which is what STORY-1405's merge-time gate does - sees those 42 as having no sha at all and cannot distinguish them from a genuinely unattributable verdict. That is the absent-versus-present confusion at the KEY level: the value is there, under a different name, and the consumer reports it missing.

    FOUR FILES CARRY TWO KEYS, and nothing in the corpus forces them to agree. When reviewed_sha and head disagree, which wins? Nothing reads both, so no answer exists today, and a normaliser that picks the first key it finds makes that decision by accident.

    SO CRITERION 1 IS UNDERSPECIFIED AS FILED. "Comparison is by commit identity, not string equality" does not say what identity is read FROM. Three decisions are required, and each gets made accidentally if it is not made deliberately:

        the canonical KEY NAME        which field holds the sha, and what happens to the other two
        the canonical LENGTH          full 40 characters, per the write-boundary fix
        PRECEDENCE ON DISAGREEMENT    when two keys are present and differ

    BUG-1505 criterion 1 already does exactly this for the verdict VALUE by requiring a closed set with one canonical spelling per outcome. This is the same requirement applied to three more fields, and it should be written the same way rather than left to the normaliser's author.

    VERIFIED AS NOT AFFECTING BUG-1508: the six verdicts that spec calls permanently indeterminate were re-checked across every sha-ish and time-ish key. None has a sha under any name. The key-spread discovery does not rescue them.
  created_at: 2026-09-21T03:35:40.887763998Z
  modified_at: 2026-09-21T03:35:40.887763998Z
  session_id: 01a0adee-16e1-7982-ac9f-e15abb207b6c
