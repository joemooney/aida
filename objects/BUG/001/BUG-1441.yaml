id: 01a0bedb-f28f-7ba1-b0d2-6c6ddce5b60a
spec_id: BUG-1441
agreed_id: BUG-1441
title: aida drain status shows an empty in-progress view while actively working a spec, because current and members disagree
description: |
  `aida drain status` reports `current: <SPEC>` while that spec has no row in the members table, so the in-progress view is empty and an operator reasonably concludes nothing is running.

  OBSERVED 2026-09-20 12:47Z, drain pid 795254, running 34 minutes:

    status: active
    current: BUG-1424
    members[11]:
      TASK-1293  failed   PR-2003
      BUG-1425   queued
      BUG-1426   queued
      ... nine more, all `queued`

  BUG-1424 does not appear in that list at all. No member carries an in-phase state. The drain-state file confirms the work is real — `current_phase: 2 (ci)`, entered 12:39:20 — and the process has been alive for 34 minutes. So the drain is genuinely working a spec that its own members table cannot see.

  WHY IT HAPPENS. The batch was formed at launch. BUG-1424 was requeued afterwards, via `aida queue rework`, and the launcher's batch refresh picked it up as work without adding it to the members list. So `current` and `members` are populated by different paths and can disagree.

  WHY IT MATTERS MORE THAN A DISPLAY GLITCH. This is the surface an operator uses to answer "is anything happening". It said no while the answer was yes, and the only way to establish the truth was to read `.aida/drain-state.json` and the process table by hand. A status surface that can be wrong in the reassuring direction is worse than one that is merely incomplete — a false "nothing in progress" invites a restart, which would have killed a live CI wait.

  LIKELY THE SAME ROOT CAUSE AS THE EMPTY DRAIN SUMMARY. Earlier tonight a drain exited reporting `QueueDrained { shipped: 0, shelved: 0 }` after thirteen specs had merged. If shipped/shelved counts are derived from member rows, then every spec worked-but-not-a-member is invisible to the summary for the same reason it is invisible here. Worth testing that hypothesis before fixing either in isolation — one cause, two symptoms, and BUG-1286 carries the other.

  ACCEPTANCE
  1. A spec being worked appears in the members table with its phase, regardless of how it entered the batch. `current` and `members` cannot disagree about what is running.
  2. A member added by a batch refresh after launch is recorded the same way as one present at launch. If refresh-added work is deliberately tracked differently, the status output says so rather than omitting it.
  3. `aida drain status` never renders an empty in-progress view while `current` names a spec. If there is genuinely nothing in progress, that is stated explicitly rather than implied by an absence.
  4. A test covers the refresh path specifically: form a batch, add a spec after launch, and assert it appears with its phase in the status output.
  5. Verify whether the shipped/shelved counts in `QueueDrained` share this cause. If they do, fix both together and record it on BUG-1286; if not, say so, because that rules out the simplest explanation for a thirteen-merge drain reporting zero.

  Reported by the operator, who observed the empty in-progress view directly; the drain state, process liveness and member list verified by the advisor before filing.
status: Completed
priority: High
owner: ''
feature: Uncategorized
created_at: 2026-09-20T12:48:00.655624765Z
modified_at: 2026-09-21T12:19:32.473657227Z
req_type: Bug
tags:
- batch:night-0920
- diagnostic-decay
- drain
- drain-safe
- from:operator-0920
- observability
- parent:EPIC-62
relationships:
- rel_type: Child
  target_id: 01a09180-071a-7712-8b42-9e6f1b646259
  created_at: 2026-09-20T12:48:03.002075387Z
- rel_type: !Custom implemented-by
  target_id: 01a0c043-99b8-7a51-964a-4b08d5a9f188
  created_at: 2026-09-20T19:21:01.096590521Z
  created_by: joe
comments:
- id: 01a0c3e8-3df9-7962-b41e-9207e8594656
  author: joe
  content: |-
    [aida:proxy-approval]
    pr: #2026 fix(drain): render the missing current member in every representation (BUG-1441)
    merge commit: 62a1396adf7633cd6981f0cb31acd961d8e871a6
    verdict: APPROVED by claude-reviewer-1 at reviewed_sha 0ed4bed7b88f
    proxy: claude driver-advisor, session 01a0adee
    independent: yes (implementers = codex for c90c4bc927, claude-product-1 for 9c38af7d83 and 0ed4bed7b8; reviewer = claude-reviewer-1; advisor merged, authored none of the diff)

    gates cleared:
      - Build (ubuntu-latest): SUCCESS
      - merge-hold-gate: FAILURE at 12:04:11, then SUCCESS at 12:04:53 after the reviewer cleared the hold on re-approval
      - no .aida/merge-holds/PR-2026 marker at merge time
      - mergeStateStatus CLEAN
      - reviewed_sha 0ed4bed7b88f is a 12-character prefix of head 0ed4bed7b88f8033e31b813193fee8dcf6576812, matched on the shared prefix

    TWO ROUNDS. Round 1 was the drain reviewer's finding that the missing-current-member fallback lived only in render_human_inner. Round 2 was claude-reviewer-1 catching that the fix's insertion displaced render_human_with_context's doc header and its trace:STORY-948, inverting aida why for both functions. Both resolved.

    VERIFIED ON MAIN AFTER MERGE, not taken from the verdict: trace:BUG-1441 sits at :1448 immediately above display_members at :1449, and trace:STORY-948 at :1468 immediately above render_human_with_context at :1469. Each function resolves to its own spec.

    squash body edited at merge: yes, in one place. The third commit's body carried a first-person session tally (this is the third time tonight I have made this slip). That is session narrative; the durable part is the rule it ends with, which I kept verbatim, plus a pointer to BUG-1550 where the class is tracked with all three instances and a programmatic remedy. Everything else in all three bodies went in unchanged.

    NOTE ON THE ROUND-1 ATTRIBUTION. The original finding was recorded by the 'aida drain reviewer' actor at 363bd8f9f7fb, NOT by claude-reviewer-1. The PR-keyed verdict file that named it was destroyed by BUG-1539 when the second reviewer recorded over it, which is why downstream discussion misattributed it. Credit for the seam observation belongs to the drain reviewer.

    timestamp: 2026-09-21T05:19 MST
  created_at: 2026-09-21T12:19:32.473657227Z
  modified_at: 2026-09-21T12:19:32.473657227Z
  session_id: 01a0adee-16e1-7982-ac9f-e15abb207b6c
history:
- id: 01a0c3e6-3317-7fb2-a49a-76f1a58a7e32
  author: aida-auto-bump
  timestamp: 2026-09-21T12:17:18.615849810Z
  changes:
  - field_name: status
    old_value: NeedsAttention
    new_value: Completed
processing_record:
- id: 01a0c3e6-3328-70c3-acae-3bb8aaf6f90b
  timestamp: 2026-09-21T12:17:18.632021711Z
  agent: aida
  commit_sha: 62a1396adf7633cd6981f0cb31acd961d8e871a6
  summary: Trace comment restored to render_human_with_context; each function now resolves to its own spec and the aida why inversion is gone. Clean two-line move, nothing executable changed. Prior-round findings carried forward by hand, including the drain reviewer's reconstruction, because BUG-1539 destroys them otherwise.
  punted:
  - 'punted: reviewer verdict is RequestChanges — not Approved'
  - 'punted: The fix is right and complete - one view, all three renderers, with measured reasoning for not normalizing at the loader and a test pinning that the synthetic never persists. Blocking on one thing: the new function was inserted between render_human_with_context''s doc/trace header and its body, which inverts aida why for both functions. Verified empirically, not from the doc.'
  - 'punted: Fix is right and complete - one view, all three renderers, measured reasoning against loader normalization, a test pinning non-persistence. Blocking on one thing: the new function was inserted between render_human_with_context''s doc/trace header and its body, inverting aida why for both. Also preserving a reconstruction of the drain reviewer''s original finding, which my own PR-keyed re-record destroyed via BUG-1539.'
  review_verdict: approved
implementation_info:
  implemented: true
  implemented_at: 2026-09-20T19:08:15.345823313Z
  implemented_by: joe
  completed_at: 2026-09-21T12:17:18.615356302Z
  completion_sha: 62a1396adf7633cd6981f0cb31acd961d8e871a6
