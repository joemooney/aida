id: 01a0b6e5-444d-7a00-a3c2-d1237930528c
spec_id: BUG-1244
agreed_id: BUG-1244
title: 'Drain wave cross-spec contamination: the BUG-1236 implementer ran on the story-1221 branch, queue done accepted a commit that only touched another spec, the spec flipped Done with no work of its own, '
description: |-
  Observed 2026-09-18 16:18-16:40, wave followups-0918e (batch drain, --no-human=both), reconstructed from .aida/events.jsonl, .aida/headless-logs/bug-1236-01a0b6d0-*.jsonl, and the store log:
  - 16:18 PhaseEntered implementer for BUG-1236. The implementer's git commands in its log are `git push origin story-1221` — it was working on STORY-1221's branch/worktree (STORY-1221 had just been shelved after review round 1 in the same wave), and it produced c25ba5c1 "fix(protocol): seed missing contracts on refresh (STORY-1221 BUG-1236)" — the STORY-1221 round-2 rework, nothing for BUG-1236 (no merge_hold change at all).
  - The local branch bug-1236 exists with ZERO commits over origin/main and was never pushed; no PR, no PhaseDonePr, no review story for BUG-1236.
  - queue done BUG-1236 accepted this and flipped BUG-1236 to Done ("done on a branch — awaiting merge").
  - 16:24 CI phase and 16:34 reviewer phase for "BUG-1236" ran against PR #1948 (STORY-1221's PR); the reviewer wrote .aida/review-verdicts/PR-1948.json round 2 (CHANGES REQUESTED, 16:37) — while the advisor, seeing STORY-1221 shelved and no review story for round 2, merged #1948 at 16:35. Auto-drafted failure record: BUG-1243. The product seat reset BUG-1236 to Approved at 16:40.
  Three defects, one root:
  1. Worktree/branch selection for a wave member must be the member's own (bug-1236), never a sibling's shelved worktree; if the intended worktree is unavailable the phase must punt, not fall through to whatever is checked out (BUG-553 class: reset/isolate between specs).
  2. queue done <SPEC> must verify the branch it is finishing belongs to SPEC (clarified 2026-09-19 after the #1956 round-3 review): (a) the branch names a DIFFERENT requirement id (type-prefixed, numeric core; slice suffixes and node-qualified ids belong to their spec) → refuse; (b) the branch names SPEC → accept; (c) the branch names no requirement id (main, master, pr-N, batch-*, single-branch/*, feature/*) → commit evidence decides ONLY when commits exist since the merge-base with the default branch: no commits → accept; a commit references SPEC (subject or trailer) → accept; commits reference a different spec and none reference SPEC → refuse naming the foreign spec; commits reference no spec → accept. --force overrides any refusal and is recorded in the store commit subject.
  3. The CI and reviewer phases must bind to the PR opened for THIS spec (PhaseDonePr for this run) — never resolve a PR by branch name inherited from a sibling; if no PR exists for the spec, punt/shelve, do not review another spec's PR under this spec's name (and do not write that spec's verdict file).
  Acceptance: unit tests for the queue-done ownership check (refuse / --force paths); an orchestrator test where a member's worktree is a sibling's branch punts instead of committing; a test that the reviewer phase refuses when the run has no PhaseDonePr for its spec; the drain banner names the worktree + branch it is operating on at phase 1 start.
status: InProgress
priority: High
owner: ''
feature: Uncategorized
created_at: 2026-09-18T23:41:13.677938849Z
modified_at: 2026-09-19T10:11:39.972594950Z
req_type: Bug
tags:
- batch:followups-0918h
- drain
- integrity
- keyboard-gate
- orchestrator
- parent:EPIC-28
- queue-done
- worktree
relationships:
- rel_type: Child
  target_id: 019e3291-30a8-7e12-bd9a-fae6bb9a3b64
  created_at: 2026-09-18T23:41:16.742657613Z
- rel_type: !Custom implemented-by
  target_id: 01a0b917-9cc6-70a2-a22c-d90125318611
  created_at: 2026-09-19T09:55:44.665408748Z
  created_by: joe
comments:
- id: 01a0b70c-2027-7513-871b-7334c1370e95
  author: joe
  content: |-
    [advisor] Recurrence 2, 2026-09-18 17:09-17:18, same wave: BUG-1236 was picked up again (run 01a0b6ff) after the product seat reset the SPEC to Approved at 16:40 — but the QUEUE ENTRY for BUG-1236 still read status=Done (queue list --format json shows "status": "Done" on the entry). The drain's pickup therefore took the Done/rework path instead of a fresh implement, and the rework brief it attached was the last review verdict recorded under BUG-1236's run — #1948's round-2 findings (the missing protocol tests). Codex dutifully wrote those tests and opened PR #1952 with trailer (STORY-1221 TASK-1278); BUG-1236 flipped Done a second time with still no work of its own; the hold marker on PR-1952 names BUG-1236.
    Mechanism to add to the fix list: (4) a queue entry's status must not diverge from the spec's — resetting a spec from Done to Approved (or any status edit) must reset its queue entry, or the pickup must read the SPEC status and treat a Done-entry/Approved-spec mismatch as fresh; (5) the rework brief must be keyed to a PR that this spec's run opened (PhaseDonePr for this spec), never to a verdict file written while this spec's phase was reviewing a sibling's PR.
    Repair applied at the gate: #1952 retitled to TASK-1283 (the spec that actually asked for those tests); after this run's reviewer finishes, BUG-1236's queue entry is removed and re-added fresh and the spec reset to Approved so the next pickup implements BUG-1236 itself.
  created_at: 2026-09-19T00:23:40.327329078Z
  modified_at: 2026-09-19T00:23:40.327329078Z
  session_id: 01a0adee-16e1-7982-ac9f-e15abb207b6c
- id: 01a0b80c-204b-7243-97f3-3f3ec84f66d1
  author: joe
  content: |-
    REVIEW FINDINGS TO ADDRESS (PR #1956, advisor round 1 — CI red, no drain reviewer ran):
    Verdict: CHANGES REQUESTED
    Findings:
    1. CI red is caused by the new guard itself: workflow_hints::branch_belongs_to_spec returns false for `main`, so `queue done` run from the main checkout (aida-cli/tests/agent_write_confirm.rs::queue_done_non_interactive_without_yes_marks_done_not_cancelled and aida-cli/tests/journey.rs::agent_journey_toon_list_search_queue_done_and_status both do exactly that) is refused. Change the rule to: refuse ONLY when the branch names a DIFFERENT spec — extract [A-Z]+-\d+ ids from the branch name; if any id is present and none equals the target, refuse; if no spec id is present (main, master, feature/foo, batch-followups-0918h, single-branch cluster names), allow. Apply the same rule in the phase-1 workspace check. Add tests for main, a batch branch and a sibling spec branch.
    2. Acceptance items missing: (3) the CI and reviewer phases must resolve the PR from THIS run's PhaseDonePr event, never from a branch inherited from a sibling, and must punt when no PR exists for this spec; (4) a queue entry whose status is Done while the spec is Approved/InProgress is treated as a FRESH pickup (or `aida edit --status` resets the entry) — this is what produced recurrence 2; (5) the rework brief attached at pickup is keyed to a PR this spec's run opened, not to the last verdict file written under this spec's phase.
    3. The acceptance's drain banner (worktree + branch at phase-1 start) only prints a real workspace on retry/resume; on the first attempt implementer_workspace() returns the project root + current branch, which is the misleading case. Print the minted lease's worktree and branch once the lease exists (or omit the line until then) rather than the driver cwd.
    Contract: produce at least one commit addressing 1 (the red) and as much of 2-3 as fits; if items 2(3)-(5) are split into a follow-up, say so and file it as a child BlockedBy nothing.
  created_at: 2026-09-19T05:03:17.579914008Z
  modified_at: 2026-09-19T05:03:17.579914008Z
  session_id: 01a0adee-16e1-7982-ac9f-e15abb207b6c
- id: 01a0b80c-6d8e-7f53-9f67-83ab5ccb0e15
  author: joe
  content: |-
    REVIEW FINDINGS TO ADDRESS (PR #1956-advisor):
    Verdict: CHANGES REQUESTED
    Findings:
    1. BUG-1244 at c1510c82 — CI red (aida-cli/tests/agent_write_confirm.rs queue_done_non_interactive_without_yes_marks_done_not_cancelled; aida-cli/tests/journey.rs agent_journey_toon_list_search_queue_done_and_status) and partial scope. Cause of the red: the new queue-done guard refuses whenever the current branch does not NAME the target spec, so `queue done` from main / master / a batch or single-branch cluster branch is refused — the two failing tests run queue done on main. The guard must refuse only when the branch names a DIFFERENT spec (matches [A-Z]+-\d+ and that id != target), and must allow main, non-spec branches and batch/single-branch names. Same rule for the phase-1 workspace check. Scope: fixes (1) worktree check and (2) queue-done ownership are present; (3) CI/reviewer phases bind to THIS run's PhaseDonePr PR, (4) queue-entry status must not diverge from the spec (Done entry + Approved spec = fresh pickup), (5) rework brief keyed only to a PR this spec's run opened — are absent and are the mechanism behind recurrence 2. Also: the phase-1 refusal path uses FailureKind::Failed with the worktree path in the reason (good), but the drain banner promised in the acceptance (worktree + branch at phase-1 start) prints only when a retry/resume workspace is known — for the first attempt it prints the project root + current branch, which is the misleading case.
    Contract: produce at least one commit, or punt explicitly naming the finding you dispute; never pass through silently with no changes.
  created_at: 2026-09-19T05:03:37.358280976Z
  modified_at: 2026-09-19T05:03:37.358280976Z
- id: 01a0b810-4ff8-71f1-b4f3-fd945d955ea8
  author: joe
  content: 'CI red on PR #1956 (run 35422445837, Build ubuntu-latest): two integration tests fail — aida-cli/tests/agent_write_confirm.rs:152 queue_done_non_interactive_without_yes_marks_done_not_cancelled, and aida-cli/tests/journey.rs:386 agent_journey_toon_list_search_queue_done_and_status. Both exercise aida queue done in a fresh throwaway project, so the contamination guard is refusing or altering queue done where no sibling PR/worktree exists. Make the guard fire only when the session''s worktree/branch belongs to a DIFFERENT spec than the one being marked done (or when the rework brief targets a sibling PR); a plain single-spec queue done must behave exactly as before. Run both tests locally (cargo test -p aida-cli --test agent_write_confirm --test journey) before pushing.'
  created_at: 2026-09-19T05:07:51.928322959Z
  modified_at: 2026-09-19T05:07:51.928322959Z
- id: 01a0b863-7490-78a2-9da9-60c2ded52c12
  author: joe
  content: 'Round 3: narrowed branch ownership refusal to explicit foreign spec IDs while allowing main/non-spec/batch branches; pinned phase 1 to the intended spec workspace; bound reviewer/CI flow to the run-local phase-1 PR; scoped rework guard to a PR whose metadata names this spec and whose PR matches the run; added current-spec-status and recurrence tests. Verified original red agent_write_confirm + journey tests and 204 auto_complete tests.'
  created_at: 2026-09-19T06:38:40.784820461Z
  modified_at: 2026-09-19T06:38:40.784820461Z
  session_id: 01a079fe-4ad8-7ee0-8086-16d6a1b7f0e5
- id: 01a0b867-330b-74c1-a8de-69bd72868e42
  author: joe
  content: |-
    REVIEW FINDINGS TO ADDRESS (PR #1956, advisor round 2):
    Verdict: CHANGES REQUESTED (one item; everything else from round 1 is resolved — PhaseDonePr binding, lease-resolved workspace, rework-recovery ownership check, status joined from the spec)
    Findings:
    1. workflow_hints::branch_belongs_to_spec still over-refuses: the id regex ([a-z]+-\d+(?:-\d+)*) treats ANY word-dash-digits as a spec id. Real branches this breaks: `pr-1948` (the reviewer worktree branch pr_rebase_handler creates → reads as PR-1948, so finishing a review story from it is refused), `batch-followups-0918h` (reads as FOLLOWUPS-0918), slice branches like `task-1189-4` (reads as TASK-1189-4 ≠ TASK-1189). Fix: extract only ids whose prefix is an AIDA requirement-type prefix (derive the set from RequirementType / the id dispenser, do not hand-list), and compare on the numeric core so slice suffixes and node-qualified agreed ids (TASK-1-127) match their spec. Tests: accept pr-1948 for STORY-1231, batch-followups-0918h, task-1189-4 for TASK-1189, bug-1244-keyboard for BUG-1244; refuse story-1221 for BUG-1236.
    Contract: one commit for this item; the drain reviewer's findings (if any) stack on top.
  created_at: 2026-09-19T06:42:46.155714004Z
  modified_at: 2026-09-19T06:42:46.155714004Z
  session_id: 01a0adee-16e1-7982-ac9f-e15abb207b6c
- id: 01a0b867-6387-7a63-a8a8-ca53978eaf55
  author: joe
  content: '[advisor round 2, addendum] Verified branch shapes the code itself creates (lib.rs / pr_cmd.rs): reviewer worktrees are pr-N; batch worktrees are batch-<name> (HYPHEN — the round-2 whitelist checks the slash form batch/, so real batch branches are not whitelisted); single-branch clusters are single-branch/<batch>-<shortsha>. Test against exactly these strings: pr-1948, batch-followups-0918h, single-branch/followups-0918h-abc1234, task-1189-4, bug-1244-keyboard (all accept for their spec); story-1221 for BUG-1236 (refuse).'
  created_at: 2026-09-19T06:42:58.567645342Z
  modified_at: 2026-09-19T06:43:15.828896879Z
  session_id: 01a0adee-16e1-7982-ac9f-e15abb207b6c
- id: 01a0b86d-b50d-79e0-9783-a1644da8ad03
  author: joe
  content: 'Round 2 fixed the two integration tests; CI now fails the Rust portability ratchet (run 35427171838): new non-portable test assumptions — aida-cli-lib/src/auto_complete.rs:6565: tmp literal: driver.workspace = Some(("/tmp/aida-story-1221".to_string(), "story-1221".to_string()));  Move the code behind #[cfg(unix)] / #[cfg(target_os = "linux")], make it portable, or shrink/regenerate the baseline only for existing debt. ##[error]Process completed with exit code 1. . Replace hard-coded unix paths/separators in the new tests with tempdir + PathBuf joins (or cfg-gate a genuinely unix-only assertion), then run scripts/check-portability.sh --check-allowlist-growth origin/main locally until it is clean. Do not add entries to scripts/portability-allowlist.txt.'
  created_at: 2026-09-19T06:49:52.653592270Z
  modified_at: 2026-09-19T06:49:52.653592270Z
- id: 01a0b86d-ba14-7ff1-9a9b-eca5174c97dd
  author: joe
  content: |-
    REVIEW FINDINGS TO ADDRESS (PR #1956-advisor):
    Verdict: CHANGES REQUESTED
    Findings:
    1. BUG-1244 at 55a4f569. Round-1 items landed: (3) PhaseDonePr binding — capture_phase_done_pr freezes the phase-1 PR per run and the reviewer phase refuses with FailureKind::NoPr when the run has none or when review_pr_number differs from it (test reviewer_refuses_when_this_run_has_no_phase_done_pr); (1) implementer_workspace now resolves from the spec's own lease (or the default worktree path/branch) instead of the driver cwd, and the phase-1 args are pinned to it even on the first attempt; (5) rework recovery checks rework_pr_head_matches_spec before adopting a discovered PR, and rework_no_op_failure ignores a PR that is not this run's; (4) covered by test resolved_queue_entry_uses_current_spec_status_after_reopen (status is joined from the spec). One finding remains on (2), the queue-done ownership rule that caused the round-1 CI red: branch_belongs_to_spec now allows main/master and batch/|cluster/|single-branch/ prefixes and refuses when any extracted id differs — but the id regex ([a-z]+-\d+(?:-\d+)*) treats ANY word-dash-digits as a spec id, so `pr-1948` (the reviewer worktree branch pr_rebase_handler creates) reads as PR-1948, `batch-followups-0918h` reads as FOLLOWUPS-0918, and a slice branch `task-1189-4` reads as TASK-1189-4 ≠ TASK-1189: queue done from those is refused. Required: extract only ids whose prefix is an AIDA requirement-type prefix (BUG, TASK, STORY, SPIKE, EPIC, ADR, FR, NFR, CR, …; derive from RequirementType, do not hand-list), and match on the numeric core so slice suffixes (task-1189-4 for TASK-1189) and node-qualified agreed ids (TASK-1-127) are accepted; tests for pr-1948 vs STORY-1231, batch-followups-0918h, task-1189-4, bug-1244-keyboard (accept) and story-1221 vs BUG-1236 (refuse). Ship after that commit, the drain reviewer's verdict and required Build = success.
    Contract: produce at least one commit, or punt explicitly naming the finding you dispute; never pass through silently with no changes.
  created_at: 2026-09-19T06:49:53.940764719Z
  modified_at: 2026-09-19T06:49:53.940764719Z
- id: 01a0b91f-8844-7bb2-a291-01a5a28a7c77
  author: joe
  content: 'Reviewer round 3 on PR #1956 (portability fixed; one fail-open left): aida-cli-lib/src/workflow_hints.rs: branch_belongs_to_spec returns true when ids.is_empty(), so branches such as pr-1948, main, or arbitrary feature branches are accepted for every target without verifying any since-merge-base commit references that target. BUG-1244 requires branch-name ownership OR a target-referencing commit, otherwise refusal. Add the missing commit-evidence gate and command-level unit coverage for both refusal and the explicit --force override.  Concretely: in workflow_hints::branch_belongs_to_spec, when the branch name carries no spec id, do NOT return true — scan the commits since the merge-base with the default branch for a (SPEC-ID) trailer or trace: reference to the target and accept only then; otherwise refuse with the existing message and the --force escape. Tests: pr-1948-style branch with no target-referencing commit → refused; same branch with one commit referencing the target → accepted; --force on the refused case → proceeds and ledgers. Keep the two integration tests green and the ratchet clean.'
  created_at: 2026-09-19T10:04:06.596099110Z
  modified_at: 2026-09-19T10:04:06.596099110Z
- id: 01a0b91f-8d14-7f00-a2a6-79020e65d10b
  author: joe
  content: |-
    REVIEW FINDINGS TO ADDRESS (PR #1956):
    Verdict: CHANGES REQUESTED
    Findings:
    1. aida-cli-lib/src/workflow_hints.rs: branch_belongs_to_spec returns true when ids.is_empty(), so branches such as pr-1948, main, or arbitrary feature branches are accepted for every target without verifying any since-merge-base commit references that target. BUG-1244 requires branch-name ownership OR a target-referencing commit, otherwise refusal. Add the missing commit-evidence gate and command-level unit coverage for both refusal and the explicit --force override.
    Contract: produce at least one commit, or punt explicitly naming the finding you dispute; never pass through silently with no changes.
  created_at: 2026-09-19T10:04:07.828772244Z
  modified_at: 2026-09-19T10:04:07.828772244Z
- id: 01a0b926-10b4-7690-8720-f218ac817588
  author: joe
  content: |-
    [advisor round 3 — disposition on the reviewer's finding] The drain reviewer (PR-1956.json, 03:02) reads the spec literally: "branch name OR a target-referencing commit since merge-base, otherwise refuse", and notes ids.is_empty() → accept. The literal rule is wrong for one real path and I own the wording: queue done from the main checkout with NO commits since merge-base (docs-only work, or a PR opened from another worktree) is legitimate and is exactly what aida-cli/tests/agent_write_confirm.rs and journey.rs exercise — round 1 went red on it. Settled rule (acceptance is being edited to match):
    - Branch names a DIFFERENT spec → refuse (as implemented).
    - Branch names the target (incl. slice/node-qualified forms) → accept.
    - Branch names no spec (main, master, pr-N, batch-*, single-branch/*, feature/*): look at commits since merge-base with the default branch. No commits → accept. Commits exist and at least one references the target (subject/trailer) → accept. Commits exist, NONE reference the target, and at least one references a different spec → refuse ("branch carries SPEC-X work, not SPEC-Y"). Commits exist and reference no spec at all → accept (untagged chores).
    - --force overrides any refusal and is recorded in the store commit subject.
    Tests to add: main with no commits → accept; pr-1948 with commits naming STORY-1221 while finishing BUG-1236 → refuse; feature/x with untagged commits → accept; --force path. That closes the reviewer's finding without re-breaking the integration tests. One commit; then I ship.
  created_at: 2026-09-19T10:11:14.740545222Z
  modified_at: 2026-09-19T10:11:14.740545222Z
  session_id: 01a0adee-16e1-7982-ac9f-e15abb207b6c
execution_mode: drive
implementation_info:
  implemented: true
  implemented_at: 2026-09-19T04:52:24.352799359Z
  implemented_by: joe
failure_reason:
  phase: reviewer
  phase_index: 3
  kind: verdict:request-changes
  detail: reviewer verdict is RequestChanges — not Approved
  recovery_hint: 'Address the review feedback, then push fixups: `aida queue rework BUG-1244 --work`'
  shelved_by: advisor
  shelved_at: 2026-09-19T10:03:48.205007501Z
