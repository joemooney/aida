id: 01a0bda9-1d96-7c90-af1d-4152548280ed
spec_id: BUG-1425
agreed_id: BUG-1425
title: The batch-exclusion event field cannot express not-measured, so zero means measured-zero, not-computed and legacy alike
description: |
  `QueueDrained.excluded_from_batch` is typed `usize`, so the value 0 means three unrelated things and a consumer cannot tell them apart:

  1. Genuinely zero specs were excluded — a real measurement.
  2. Not computed, because this was a chained-batch run (`--batch a,b,c`), where the figure is deliberately not produced.
  3. Written by a binary from before the field existed. The compatibility test asserts this case explicitly, reading a legacy line as 0.

  So a dashboard summing or displaying that column reports "0 specs excluded" for every historical event and every chained run, stating a fact nobody established.

  WHY IT WAS ACCEPTED ANYWAY. TASK-1297's fix is the one that surfaces specs a stale `--batch` filter silently hides, which is the exact failure that starved sixteen approved specs for hours. Holding a working fix for a field-type change, while the operator is asking for throughput, was the wrong trade. The ambiguity is recorded here instead.

  THE FIX IS SMALL: `Option<usize>` with `#[serde(default)]` and `skip_serializing_if = "Option::is_none"`. `None` means not computed or legacy; `Some(0)` means measured and genuinely zero. Backward compatibility is identical — a missing field still reads as None rather than failing to parse.

  ACCEPTANCE
  1. The event field is `Option<usize>`; absence and a measured zero are distinguishable by a consumer.
  2. The existing legacy-line test keeps its coverage and asserts `None` rather than `0`, which is the honest answer for a line written before the field existed.
  3. The chained-batch path either populates the figure or leaves it `None`; it does not emit a zero it did not measure.
  4. Documented alongside the terminality consumer rule, since both are about the same feed and the same reader.

  Deferred from PR #1982 review, where the feature was merged on the operator's throughput directive with this recorded rather than blocking.
status: Completed
priority: Medium
owner: ''
feature: Uncategorized
created_at: 2026-09-20T07:12:52.118388386Z
modified_at: 2026-09-20T13:21:18.217507908Z
req_type: Bug
tags:
- batch:night-0920
- diagnostic-decay
- drain-safe
- events
- from:session-0919
- observability
relationships:
- rel_type:
    custom: related
  target_id: 01a0bb79-a835-7f93-953b-a0c602afd8d9
  created_at: 2026-09-20T07:13:46.381650656Z
  created_by: joe
- rel_type:
    custom: implemented-by
  target_id: 01a0bef6-c476-7f30-8763-9a91068857bf
  created_at: 2026-09-20T13:17:23.115606465Z
  created_by: joe
comments:
- id: 01a0befa-135f-7623-bd18-b3b21f84b354
  author: harvest
  content: |
    [aida:harvest]
    source: PR-2017
    agent: codex
    ran-at: 2026-09-20 06:20 -07:00
    mode: propose-only (drain)
    proposed: 2
    skipped: 0
    candidates-file: /home/joe/ai/aida/.aida/harvest/bug-1425-drain-01a0bef9-c7a0-71d0-903d-35c28f67146d.json
    ? SEM (0.97) — A next-N drain serializes `excluded_from_batch` as an explicit measured zero, while a chained-batch drain omits the field because no aggregate exclusion count is computed.
    ? SEM (0.91) — Notification and watch consumers mention excluded specs only when `excluded_from_batch` is present and greater than zero; an absent value neither triggers the exclusion-count condition nor produces exclusion text.
  created_at: 2026-09-20T13:20:55.135695273Z
  modified_at: 2026-09-20T13:20:55.135695273Z
  session_id: 01a079fe-4ad8-7ee0-8086-16d6a1b7f0e5
history:
- id: 01a0befa-6d88-75b0-b096-1f28c7cc22fc
  author: aida-auto-bump
  timestamp: 2026-09-20T13:21:18.216703432Z
  changes:
  - field_name: status
    old_value: Done
    new_value: Completed
processing_record:
- id: 01a0befa-6d89-7cd0-beef-549cf801d462
  timestamp: 2026-09-20T13:21:18.217506906Z
  agent: 01a079fe-4ad8-7ee0-8086-16d6a1b7f0e5
  brief_ref: .aida/agent-briefs/advisor/BUG-1425-2026-09-20T132056Z.md
  commit_sha: 2f1a18c4cdd5d6a0856f100429aad1d2f63a8787
  summary: Completed via merge 2f1a18c4
execution_mode: drain
implementation_info:
  implemented: true
  implemented_at: 2026-09-20T13:05:07.732329514Z
  implemented_by: joe
  completed_at: 2026-09-20T13:21:18.216403920Z
  completion_sha: 2f1a18c4cdd5d6a0856f100429aad1d2f63a8787
