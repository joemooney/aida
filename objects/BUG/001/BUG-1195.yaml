id: 01a0b0e9-8e9a-7500-805c-50ab0b86853d
spec_id: BUG-1195
agreed_id: BUG-1195
title: 'Drain review phase shelves a spec as tool-exit: the BUG-1186 envelope guard says ''no pickable review story is queued'' while the auto-filed Review PR-N story IS queued, pickable and title-matching'
description: |-
  Observed 2026-09-17 12:40 on the bugs-0917 batch, member BUG-1193 (PR #1901). Phase 2 auto-filed STORY-1191 "Review PR-1901: …" → reviewer queue (log: "✓ filed STORY-1191 (covers BUG-1193) → reviewer queue (PR #1901)"). Phase 3 then ran `aida queue work PR-1901` with the BUG-1186 envelope (AIDA_FROM_PR_REVIEW=1, AIDA_FROM_PR_NUMBER=1901) and bailed: "review envelope is set for PR-1901 but no pickable review story is queued — refusing to fall back to an implementer pickup of the backing spec". Retried once (attempt 2/2), same error, spec shelved as tool-exit. The two previous members (BUG-1191 → PR-1899 → STORY-1189, BUG-1192 → PR-1900 → STORY-1190) reviewed fine minutes earlier on the same binary and the same drain.

  REPRO (deterministic, current binary c5c9ceb8, after the drain ended):
    AIDA_FROM_PR_REVIEW=1 AIDA_FROM_PR_NUMBER=1901 aida queue work PR-1901 --user role:implementer --role implementer --dry-run
    → the same error.
  Yet every input the lookup uses checks out:
    - aida queue list --user role:implementer --for reviewer → STORY-1191 approved, for_role reviewer (also visible under joe / default / role:reviewer).
    - review_title_matches: title starts with "Review PR-1901:" (case-insensitive prefix, exact number).
    - queue_fresh_pickup_policy / pickability: aida why STORY-1191 → actionable (not deferred/archived/Done/terminal/blocked); `aida queue work STORY-1191 --dry-run` resolves to scope PR-1901 with a pr-1901 worktree.
    - Full store load works (aida criteria STORY-1191 succeeds), so the silent `let Ok(store) = storage.load() else { return false }` is probably not the culprit — but queued_review_story_for_pr (lib.rs, fn queued_review_story_for_pr) swallows BOTH storage.queue_list and storage.load errors as `false`, which then reads as "no story" to the guard and turns any transient read failure into a shelve.

  Suspects to instrument (do not guess): (1) queue_list(user_id) under the drain's `--user role:implementer` vs how the entry was keyed when the worktree session's `aida session end` (session 01a0b0c6, user joe?) filed it — the entry's user/role key; (2) e.requirement_id vs r.id equality after the "auto-queue: aida add failed for PR #1901 (see warning above)" retry — was a second story/entry created with a UUID that is not in the loaded store? (3) whether `storage.load()` in that code path is the cache-backed projection that had not yet observed the worktree's write (cache HEAD behind the store) — note `aida cache verify` found 7 status drifts at the same time.

  Fix: make queued_review_story_for_pr return Result and have the guard distinguish "no story" from "lookup failed" (a lookup failure must NOT bail into the guard; log and fall back to the normal review-story pickup or retry after a cache refresh); add a debug line naming which of (entry found / title match / pickability) failed; and add a regression test that a story filed by a sibling-worktree session under a different user id is found by the drain's user id.

  Acceptance:
  - The repro command above resolves PR-1901 to the reviewer route (or the test-equivalent fixture) with no bail.
  - A lookup failure is logged with its cause and never shelves the spec as tool-exit; a genuine no-story case still bails as today.
  - Regression test covering the sibling-worktree-filed story + the drain user id.
status: Approved
priority: High
owner: ''
feature: Uncategorized
created_at: 2026-09-17T19:48:11.546163776Z
modified_at: 2026-09-18T03:10:48.131244646Z
req_type: Bug
tags:
- drain
- follow-up:BUG-1186
- queue
- repro
- reviewer
comments:
- id: 01a0b20a-9a17-7992-a1df-329382fe90a2
  author: joe
  content: |-
    [aida:proxy-approval]
    pr: #1902 [AI:claude] fix(queue): review-story lookup uses the role-fallback listing and distinguishes lookup failure from no-story (BUG-1195)
    verdict: APPROVE + merged (drive mode)
    proxy: claude driver-advisor, session 01a0adee
    independent: yes (author = product/proxy seat; reviewer = this advisor; verdict .aida/review-verdicts/PR-1902.json)
    gates: Build (ubuntu-latest) SUCCESS, merge-hold-gate SUCCESS, MERGEABLE, no drain running
    reasoning: root cause matches the BUG-1193 evidence — the guard's lookup used the per-user queue_list while the plan-builder uses queue_list_with_role_fallback, so the sibling-session-filed story under joe was invisible to role:implementer; read errors were also collapsed to false. Fix routes the lookup through the role fallback, types the outcome (Found/NoEntries/NoTitleMatch/NotPickable/Failed), keeps the PR scope on Failed so a read failure never feeds the envelope guard, and names the failing check in the bail. Storage-backed test reproduces the joe-vs-role:implementer shape; source-shape guard has no newline needle.
    merged: 2026-09-17 18:06 -07:00 → main 70c2798466; spec auto-bumped Completed. Drain binary rebuilt before the next batch (followups-0917).
  created_at: 2026-09-18T01:03:54.391361344Z
  modified_at: 2026-09-18T01:03:54.391361344Z
  session_id: 01a0adee-16e1-7982-ac9f-e15abb207b6c
- id: 01a0b27e-c743-7832-8281-2ac272a054b6
  author: joe
  content: |-
    REOPENED 2026-09-17 20:15 (advisor): #1902 fixed the envelope guard's lookup (queued_review_story_for_pr), but the TASK-85 PR-N pickup in resolve_queue_work_plan (queue_cmd.rs ~7060-7110, the "no queued review story for PR-N — check gh pr view" bail) has the same false-negative and shelved TASK-1261 in the product seat's batch tonight (PR-1908, 20:07, no reviewer launched, no verdict file).

    Deterministic repro on binary 9df74155 (includes #1902):
      aida queue work PR-1908 --user role:implementer --role reviewer --dry-run
      → error: "no queued review story for PR-1908 — …"
    while at the same moment:
      aida queue list --user role:implementer --for reviewer → STORY-1196 "Review PR-1908: …" Approved, for_role reviewer
      aida why STORY-1196 → actionable
      aida queue work STORY-1196 --user role:implementer --role reviewer --dry-run → resolves to scope PR-1908 normally
    Same shape as PR-1901/STORY-1191 on 09-17 12:40. Meanwhile PR-1903/1904/1906 (STORY-1192/1193/1194, all auto-filed by the same `session end` backup trigger) resolved fine in the same drains — so it is data-dependent, not the filing path.

    Narrowing done: that block already reads entries via queue_list_with_role_fallback (line ~6855), so the user key is NOT the cause here. Remaining suspects, in order: (1) the join `store.requirements.iter().find(|r| r.id == e.requirement_id)` — the queue entry's requirement_id vs the story's id in the FULL store load (storage.load()); note the failing cases both had an "auto-queue: aida add failed for PR #N (see warning above)" retry in the drain log right before — check whether the retry produces a queue entry whose requirement_id is not the id of the YAML that finally landed (or two entries, one dangling), and whether the full store load sees the story at all; (2) queue_fresh_pickup_policy(req, &store, force_needs_attention) on the story in THAT store snapshot. Instrument: log which filter dropped each candidate entry (same typed-diagnosis idea as #1902's ReviewStoryLookup) and reuse classify_review_story_lookup here so the two paths cannot diverge again.

    Acceptance addition: the repro above resolves PR-1908 (or a fixture with the same shape) to the reviewer route; the pickup and the guard share one classifier; the bail names the dropped filter.
  created_at: 2026-09-18T03:10:48.131244646Z
  modified_at: 2026-09-18T03:10:48.131244646Z
  session_id: 01a0adee-16e1-7982-ac9f-e15abb207b6c
history:
- id: 01a0b209-329f-7c20-8606-457c8775cde7
  author: aida-auto-bump
  timestamp: 2026-09-18T01:02:22.367052258Z
  changes:
  - field_name: status
    old_value: Done
    new_value: Completed
processing_record:
- id: 01a0b209-32a5-7942-a0ac-5264a501b553
  timestamp: 2026-09-18T01:02:22.373737002Z
  agent: aida
  commit_sha: 70c2798466526bfe0a3e33d9773ed1959e986cb8
  summary: Completed via merge 70c27984
implementation_info:
  implemented: true
  implemented_at: 2026-09-18T00:52:05.275920320Z
  implemented_by: joe
  completed_at: 2026-09-18T01:02:22.366914973Z
  completion_sha: 70c2798466526bfe0a3e33d9773ed1959e986cb8
