id: 01a0c141-c494-7333-8219-ca70dcb97c45
spec_id: TASK-1-158
agreed_id: TASK-182
title: 'Free-text finding content containing flag-like strings is re-parsed as arguments: recording a verdict whose text contained --pr N printed a clap parse error'
description: |-
  Recording a verdict whose finding text contains the string "--pr N" printed a clap parse error alongside the success line:

      error: invalid value 'N' for '--pr <PR>': invalid digit found in string

  Observed 2026-09-20 by claude-reviewer-1 while recording BUG-1467's verdict. No data loss — all five findings and the reviewed_sha are intact in BUG-1467.json — so it appears cosmetic.

  WHY IT IS WORTH AN EYE RATHER THAN A SHRUG. Something downstream is re-parsing free-text finding content as command-line arguments. That is benign when the flag-like text is `--pr N` and the value is rejected. It is not obviously benign for every flag-shaped string a reviewer might quote in a finding, and reviewers quote command invocations constantly — today's findings alone contain `aida merge-hold clear 2014`, `--in-reply-to`, `--thread`, `--no-ci-wait` and `aida queue work PR-N`.

  NOT INVESTIGATED and deliberately not asserted: I have not isolated where the re-parse happens, whether the printed error is from the same process or a child, or whether any input could reach a flag that ACCEPTS its value rather than rejecting it. The last of those is the question worth answering first, because it is the difference between noisy output and free text steering a command.

  WHAT WOULD SETTLE IT: record a verdict whose finding text contains a flag with a VALID value for its type — something like `--yes` or a numeric `--pr 999` — and observe whether the invocation changes behaviour. If it does, this stops being cosmetic.
status: Draft
priority: Medium
owner: joe
feature: Uncategorized
created_at: 2026-09-20T23:58:27.988803235Z
modified_at: 2026-09-21T00:00:48.907323386Z
req_type: Task
tags:
- from-advisor:BUG-1467
- kind:observation
- linked:BUG-1467
- severity:minor
comments:
- id: 01a0c143-eb0b-7772-9cb9-de062d102a79
  author: joe
  content: |-
    [aida:advisor] SEARCH NARROWED, NOT SETTLED — recording what I ruled out so the next reader does not repeat it.

    I looked for the reparse and did not find it. Eliminated:

      1. queue_cmd.rs:6732, the `aida findings add` subprocess. It uses Command::args with &note and
         &finding_title as DISTINCT argv elements — no shell, no word splitting. Flag-like text inside
         the note is passed as part of one argument and cannot be parsed as a flag. This is the call I
         expected to be the culprit and it is not.
      2. review_verdict.rs spawns no child process at all.
      3. The only split_whitespace calls in queue_cmd.rs (1144, 10686) operate on TAGS, not on finding
         or note text.

    So the reparse happens somewhere I have not looked, and the obvious candidate is clean.

    I did NOT run the settling experiment — recording a verdict whose text contains a flag with a valid value for its type — because doing it for real writes a verdict file, and I did not want to pollute the store to answer a cosmetic-looking question. Someone with a scratch project should run it there; that is the cheap version.

    The question that decides severity is unchanged and unanswered: can free text reach a flag that ACCEPTS its value rather than rejecting it? `--pr N` failing on a non-numeric value is benign. The same mechanism meeting `--yes` is not. Until that is answered this stays minor, and it should not be closed as cosmetic on the strength of the observed instance alone.
  created_at: 2026-09-21T00:00:48.907323386Z
  modified_at: 2026-09-21T00:00:48.907323386Z
  session_id: 01a0adee-16e1-7982-ac9f-e15abb207b6c
