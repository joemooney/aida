id: 019e4bc9-b645-7e43-8f81-1fa977599ee3
spec_id: TASK-439
agreed_id: TASK-439
title: 'MCP resolve_punt/escalate_punt: use write_atomic, not std::fs::write, for punt-response files'
description: |
  `aida-cli/src/mcp.rs:749` (`tool_resolve_punt`) and `:790` (`tool_escalate_punt`) both write `.aida/punts/<spec>.response.json` with a plain `std::fs::write(&path, body)`. A crash between `open` and `write_all` completing leaves a truncated file the orchestrator's phase-3 → phase-4 handshake reader can mis-parse.

  The architecture doc (`docs/architecture/mcp-coordination-surface.md:99-104`) claims "Lease and directive writes go through `write_atomic`" — punt-response writes are silently outside that guarantee. `write_atomic` (`mcp.rs:1374`) already exists in this file; the fix is dropping it in place of the two `std::fs::write` calls.

  Severity is minor because (a) punt responses are written by the advisor tier (not under contention) and (b) JSON parsers reject truncated JSON cleanly. But the failure mode — orchestrator reads "valid JSON missing a field" because of a fortuitous truncation — would be hard to diagnose.

  Raised in review of PR-162: https://github.com/joemooney/aida/pull/162#issuecomment-4511420914
status: NeedsAttention
priority: Medium
owner: ''
feature: Uncategorized
created_at: 2026-05-21T18:26:11.141458341Z
modified_at: 2026-05-22T06:06:25.714412631Z
req_type: Task
tags:
- from-review:PR-162
- severity:minor
relationships:
- rel_type: !Custom implemented-by
  target_id: 019e4dd9-e9f9-7a80-8663-0844a4ae671c
  created_at: 2026-05-22T04:03:11.791074248Z
  created_by: joe
implementation_info:
  implemented: true
  implemented_at: 2026-05-22T03:59:39.765054528Z
  implemented_by: joe
attention_reason:
  category: other
  detail: 'Redundant drain: TASK-439 is already implemented, committed, pushed, and under review as PR-166 (https://github.com/joemooney/aida/pull/166). Commit d3953d99 on branch pr-166 (worktree /home/joe/ai/aida-pr-166) carries the write_atomic fix the spec asks for, and STORY-393 has an active reviewer lease 019e4dda on it. Pre-existing spec status was Done; this orchestrator session (drain pid 596956, session 019e4e47, branch task-439-2) was spawned on top of the in-flight work and has nothing to implement — re-doing the fix here would create a competing branch racing PR-166.'
  lean: 'Cancel this drain. Let PR-166 finish its review/merge cycle; the spec''s natural state transition is Done → Completed when PR-166 merges (the aida pull auto-bump handles it per STORY-86). End session 019e4e47 and remove worktree /home/joe/ai/aida-task-439 with ''aida session end 019e4e47 --force''. Worth a follow-up TASK: investigate why ''aida queue work TASK-439 --auto-complete --no-human=both'' was launchable while the spec was already Done with an open PR — the queue-work entry path should refuse a re-drain on a Done spec with a live PR.'
  raised_by: implementer
  raised_at: 2026-05-22T06:06:25.714412631Z
