id: 019e816e-1c3d-7d00-88e8-589a5a354e82
spec_id: TASK-618
agreed_id: TASK-618
title: 'Distributed queue: same user_id on two machines edits one registry/queues/<user>.yaml → orphan-branch conflict'
description: |
  VERIFIED 2026-05-31 (advisor, answering operator's 'what if different people queue on different machines'). The default 'aida queue add' writes registry/queues/<user_id>.yaml INSIDE the orphan aida-store branch (git_backend.rs queue_add commits it; 'update queue' commits are on origin/aida-store), sharded per user_id. Distinct users (alice.yaml / bob.yaml) never collide — clean rebase, mutually visible after sync (this is the intended sharding design). HAZARD: two machines using the SAME user_id (both 'joe', or both unconfigured → the BUG-89 'default' fallback when $USER/$AIDA_USER/$USERNAME are all unset, common in CI/containers) write the SAME <user>.yaml → concurrent commits → merge conflict in the queue YAML on 'aida db sync' rebase (store leg uses --rebase), or a rejected non-ff push. Options to consider: (a) warn when the resolved user_id is 'default' and a remote queue file for 'default' already exists with a different machine fingerprint; (b) make queue YAML merges conflict-tolerant (append-merge by entry uuid rather than line-merge); (c) document distinct-user_id-per-machine as a hard requirement for shared-branch collaboration. Not urgent (sharding works for distinct users); file so it's captured before multi-machine collaboration scales.

  ## ✅ Operator decision (2026-06-06 disposition sweep)

  WARN when the resolved user_id is 'default' AND a remote 'default' queue file already exists with a different machine fingerprint — targets the actual silent collision hazard (acute for the BUG-89 CI/container fallback) cheaply. Sharding already works for distinct users, so conflict-tolerant YAML merges (heavier) are premature.
status: Approved
priority: Medium
owner: ''
feature: Uncategorized
created_at: 2026-06-01T04:25:37.597664669Z
modified_at: 2026-06-06T15:14:55.314285878Z
req_type: Task
tags:
- aida:queue
- distributed-coordination
- from-advisor
- kind:observation
- needs-human-input
- ready-to-implement
comments:
- id: 019e9c0e-ceb3-7bf0-a1ec-6343d39ebe98
  author: joe
  content: |-
    🔵 DECISION REQUEST (disposition sweep 2026-06-06) — needs human input.
    Q1: How to handle the same-user_id-on-two-machines queue-YAML conflict (both write registry/queues/<user>.yaml → orphan-branch merge conflict; acute for the BUG-89 "default" fallback in CI/containers)?
      a) Document distinct-user_id-per-machine as a hard requirement for shared-branch collaboration (no code)
      b) Warn when the resolved user_id is "default" and a remote "default" queue file already exists with a different machine fingerprint
      c) Make queue-YAML merges conflict-tolerant (append-merge by entry uuid instead of line-merge)
      REC: b — targets the actual hazard (the silent "default" collision) with a cheap guard; sharding already works for distinct users so (c) is premature. Captured observation, not urgent.
  created_at: 2026-06-06T08:31:16.659512621Z
  modified_at: 2026-06-06T08:31:16.659512621Z
- id: 019e9d80-44da-7f63-9097-ac0cdb5cc9bd
  author: joe
  content: '✅ OPERATOR DECISION (2026-06-06): WARN when the resolved user_id is ''default'' AND a remote ''default'' queue file already exists with a different machine fingerprint — targets the actual silent collision hazard (acute for the BUG-89 CI/container fallback) cheaply. Sharding already works for distinct users, so conflict-tolerant YAML merges (heavier) are premature.'
  created_at: 2026-06-06T15:14:49.690305281Z
  modified_at: 2026-06-06T15:14:49.690305281Z
