id: 01a0bc55-dcbd-74c2-827f-fb765ad6d414
spec_id: TASK-1298
agreed_id: TASK-1298
title: The pre-unattended readiness check covers the substrate but not the machine, and passed forty minutes before the disk hit 100 percent
description: |
  The pre-unattended readiness check has no disk line, and on 2026-09-19 it passed forty minutes before the root filesystem hit 100% with about 6 MB free. A readiness check that clears a 20-hour unattended run on a disk that is about to fill is not a readiness check.

  WHAT IT CHECKED AND WHAT IT MISSED. The pass covered the launcher, the live drain, the store and the queue depth — all the substrate-level things, all healthy, all true. It checked nothing about the machine the substrate runs on. Disk, inodes and memory were absent, and the two failures that actually cost time that night were both machine-level: an out-of-memory sweep killed the drain's launcher, and a full disk caused a subagent to delete the shared build directory, which removed the development binary and silently regressed the orchestrator by eight days.

  WHY THIS CLASS IS THE ONE THAT HURTS UNATTENDED. A substrate failure announces itself: a spec shelves, an event is written, a queue stops moving, and the recovery paths we have built all engage. A machine failure does not. Cargo cannot write, so a build fails, so a spec shelves with a build error that reads exactly like a code defect. Everything downstream triages the wrong thing, and the triage looks reasonable at every step. Nobody investigates the disk because nothing points at it.

  THE CHEAPEST USEFUL VERSION is a handful of thresholds, not a monitoring system. This should be a few lines in the existing readiness surface rather than a new subsystem.

  ACCEPTANCE
  1. The readiness check reports free space on the filesystem holding the repo, the build directory and the temp directory, and FAILS below a threshold sized for the work ahead rather than a fixed small number. A 20-hour multi-lane run needs more headroom than a single supervised build.
  2. It reports available memory and warns when an out-of-memory killer would plausibly engage under a full build. The earlyoom configuration on this machine already avoids cargo and prefers browsers, which is worth reading rather than duplicating.
  3. It checks that the binary the caller will actually run EXISTS and reports its build sha and mtime. On 2026-09-19 the development binary was deleted while the path entry that pointed at it remained, so every call silently fell through to an eight-day-old release with no error at any layer. See the companion defect where development status reports ACTIVE for a binary that is not there.
  4. Each failure names the remedy, not just the number. "Disk at 97%" is a fact; "disk at 97%, the workspace build directory is 121 GB, reclaim with cargo clean" is an action.
  5. A test covers the threshold decision as a pure function of the inputs, so the thresholds are adjustable without re-running a disk-full.
  6. The check is runnable on demand and is named in whatever procedure precedes an unattended window, so it is a step rather than a habit.

  NOT IN SCOPE: continuous monitoring, alerting, or reclaiming anything automatically. This reports and refuses. The reclamation defect is BUG-1287, which is why the disk filled in the first place.

  Filed by the advisor after the 2026-09-19 incident chain; the readiness pass that missed it was run by the product seat and reported in good faith, which is the point — the check was followed correctly and did not cover the thing that failed.
status: Done
priority: High
owner: ''
feature: Uncategorized
created_at: 2026-09-20T01:02:18.813550852Z
modified_at: 2026-09-20T12:51:50.890767140Z
req_type: Task
tags:
- batch:night-0920
- drain-safe
- from:session-0919
- operations
- parent:EPIC-62
- readiness
- unattended
relationships:
- rel_type: Child
  target_id: 01a09180-071a-7712-8b42-9e6f1b646259
  created_at: 2026-09-20T01:02:22.404692813Z
- rel_type: !Custom related
  target_id: 01a0bb7d-7f12-78d1-a0ea-4c530343130e
  created_at: 2026-09-20T01:02:50.709387877Z
  created_by: joe
- rel_type: !Custom implemented-by
  target_id: 01a0be82-d9dc-7571-8a6f-bf31da987924
  created_at: 2026-09-20T11:10:46.744170358Z
  created_by: joe
comments:
- id: 01a0bcf9-dba2-7b30-a540-f46c89497406
  author: joe
  content: |-
    [advisor:groom] DEMOTED Approved → Draft and dequeued, applying the operator's 2026-09-19 rule: file freely, keep everything Draft unless it blocks shipping.

    I filed this Approved and queued earlier tonight, under the previous arrangement where a verified defect went straight into the working set. That was right then and it is inconsistent now, so rather than leave the queue carrying specs under two different rules I am applying the new one to my own filings.

    Nothing about the defect changed. The evidence, the diagnosis and the acceptance all stand exactly as written. What changed is that this does not block anything from shipping today, and the queue should carry what does.

    Promote it when that stops being true, or when someone has capacity for the backlog rather than the critical path.

    TWO I DID NOT DEMOTE, for contrast, so the line I drew is visible: BUG-1287 stays Approved because it is the root cause of the disk exhaustion that silently regressed the orchestrator by eight days tonight, and BUG-1303 stays Approved because it is a defect in machinery the unattended-recovery work depends on and is now known to flake on the primary platform rather than only the secondary one. Both of those can cost us a night. This cannot.
  created_at: 2026-09-20T04:01:26.434124704Z
  modified_at: 2026-09-20T04:01:26.434124704Z
  session_id: 01a0adee-16e1-7982-ac9f-e15abb207b6c
- id: 01a0bedf-75ea-75c1-9c20-3ff303448b26
  author: joe
  content: |-
    [rework round 2] PR #2014 — CHANGES REQUESTED, one finding. AC1 and AC2 verified. The gap is inode headroom, which the spec's own incident scope names explicitly.

    REQUIREMENT 1 — CHECK INODES, NOT ONLY BYTES.
    `machine_readiness.rs` reports byte-space for the repo, build and temp filesystems but never inode availability. A filesystem with free bytes and no free inodes fails every write exactly as a full one does, and reports 100% healthy on the check we built to catch a full disk. Add inode reporting with a workload-scaled threshold, an actionable remedy line, and a pure threshold test alongside the existing ones.

    CURRENT MEASUREMENT, so the threshold is set against reality rather than a guess. On this machine right now:

        inodes   5,092,000 used of 60,981,248  (9% full)   55,889,248 free
        byte-space headroom was the thing that hit 100% last night, not inodes

    So inode exhaustion is NOT close here, and the fix should not pretend otherwise by picking an alarming threshold. What makes it worth checking anyway is the workload shape: 79 sibling worktrees and 13 agent lanes, each a full checkout plus a cargo target tree. Cargo targets are enormous in FILE COUNT as well as bytes — an incremental build directory is hundreds of thousands of small artifacts. The ratio of inodes to bytes consumed by this workload is far higher than a typical one, which is exactly why a byte-only check can pass while inodes are the binding constraint.

    Scale the threshold to that: a fraction of free inodes that accounts for a build tree's file count, not a flat number. State the reasoning in the PR so the next person can re-derive it rather than tuning it blind.

    WHY THIS MATTERS BEYOND COMPLETENESS, and it is the spec's whole thesis. TASK-1298 exists because a readiness check passed forty minutes before the disk hit 100%. That check covered the substrate — drain, launcher, store, queue depth — and not the machine. Shipping the machine half while omitting inodes recreates the same shape one level down: a machine check that reports healthy in a failure mode it does not look at. The remedy must be actionable too — "inodes low" without naming what to delete is a readiness check that tells you to panic rather than what to do.

    A NOTE ON THE REMEDY LINE, since we learned it the hard way last night. When byte-space ran out, the recovery that actually worked was removing `target/` from worktrees holding no active lease — non-destructive, regenerable, and it freed 201 GB. The inode remedy is the same action for the same reason, and naming it directly is worth more than a generic "free space" message.

    AC1 AND AC2 ARE VERIFIED and worth keeping intact: probe/disk_checks with live JSON for repo, build and temp byte-space, and the available-memory check. Do not disturb them while adding the inode path.

    Push fixups to the existing branch. Do not open a new PR.
  created_at: 2026-09-20T12:51:50.890767140Z
  modified_at: 2026-09-20T12:51:50.890767140Z
  session_id: 01a079fe-4ad8-7ee0-8086-16d6a1b7f0e5
execution_mode: drain
implementation_info:
  implemented: true
  implemented_at: 2026-09-20T11:10:49.922163270Z
  implemented_by: joe
failure_reason:
  phase: reviewer
  phase_index: 3
  kind: verdict:request-changes
  detail: reviewer verdict is RequestChanges — not Approved
  recovery_hint: 'Address the review feedback, then push fixups: `aida queue rework TASK-1298 --work`'
  shelved_by: advisor
  shelved_at: 2026-09-20T11:26:22.826088698Z
