id: 01a0d742-f5f4-7a03-b805-b2e5ed5906b7
spec_id: TASK-1505
agreed_id: TASK-1505
title: Add a separate rebuildable SQLite cache for history events
description: Add a derived SQLite history cache, separate from the requirements query cache, to make aida history and shipped-event queries fast and reduce cache contention. Store decoded Git event fields such as timestamp, commit SHA, spec ID, event kind, status transition, author, and shipped marker, with indexes supporting time windows, shipped filtering, spec lookup, and author/type filters. Git/aida-store remains canonical; the history database must be disposable and record schema version, source store HEAD, and indexed progress. Prefer incremental updates from the last indexed store commit, rebuild safely when missing or incompatible, and fall back to the existing Git scan when the history cache is stale or unavailable. Expose cache freshness or rebuild activity in diagnostics so stale results are not mistaken for current history.
status: Approved
priority: Medium
owner: ''
feature: Uncategorized
created_at: 2026-09-25T06:31:24.916031923Z
modified_at: 2026-09-25T08:15:47.044722011Z
req_type: Task
tags:
- cache
- closure:pending
- contention
- history
- performance
- sqlite
relationships:
- rel_type: Parent
  target_id: 01a0d7a1-cfb9-7ed2-add8-b69091986e6d
  created_at: 2026-09-25T08:15:05.110222597Z
- rel_type: Parent
  target_id: 01a0d7a1-efbd-7881-a8a1-ac3318dd905a
  created_at: 2026-09-25T08:15:11.304075061Z
- rel_type: Child
  target_id: 01a0d7a1-cfb9-7ed2-add8-b69091986e6d
  created_at: 2026-09-25T08:15:27.445449260Z
  created_by: joe
- rel_type: Child
  target_id: 01a0d7a1-efbd-7881-a8a1-ac3318dd905a
  created_at: 2026-09-25T08:15:47.044715322Z
  created_by: joe
comments:
- id: 01a0d743-ce5f-78f2-b235-1c02e1660146
  author: joe
  content: 'Design refinement: treat the history cache as append-only by default. When the aida-store HEAD is a descendant of the indexed HEAD, decode only the new commits and extend the cache. Do not rebuild merely because the cache is old. Rebuild or repair only for corruption, incompatible schema, missing indexed commits, or detected non-fast-forward/history rewrite; record the reason and source-head transition in cache metadata.'
  created_at: 2026-09-25T06:32:20.319413190Z
  modified_at: 2026-09-25T06:32:20.319413190Z
- id: 01a0d745-ccd3-7360-a7f4-10907e926ee1
  author: joe
  content: 'Additional refinement: record last_append_at, last_event_at, indexed store HEAD, and indexed commit timestamp. A cheap latest-event timestamp comparison may skip unnecessary scans, but timestamps are only a fast-path hint because commit clocks and rebases can be non-monotonic. HEAD ancestry / commit identity remains the correctness check before extending the append-only cache.'
  created_at: 2026-09-25T06:34:30.995898395Z
  modified_at: 2026-09-25T06:34:30.995898395Z
- id: 01a0d796-ef7e-71b0-97d4-48c2afa33ecc
  author: joe
  content: 'PROXY DECISION (orchestrator for Joe): approved. It adds a new derived database, so it is architecture-class: it needs a sketch and independent advisor signoff before implementation, and is queued only after that signoff.'
  created_at: 2026-09-25T08:03:08.286095519Z
  modified_at: 2026-09-25T08:03:08.286095519Z
  session_id: 67239b78-62f2-41d8-9600-1eea416800a6
- id: 01a0d79d-b286-72d0-a0bd-b1dba68524cc
  author: claude
  content: |
    SKETCH (awaiting advisor signoff): TASK-1505 separate rebuildable history-event cache

    Baseline (measured 2026-09-25, AIDA store = 37,612 commits, 6 merges): the events walk spawns 1 `git show --name-status` per commit + 2 `git show <rev>:<path>` per changed object, about 38 ms/commit. `history events --since <1 day> -n 50` = 7.3 s; `--shipped -n 10 --max-commits 3000` = 113 s. The default `--shipped -n 10` (window 50 commits) returned only 4 ships: the window cut them off with no warning. MCP `history` tool defaults to a 500-commit window, about 19 s. Digest mode (`aida history`, 0.12 s) is already fast.

    1. LOCATION
    - `history.db` sits beside the query cache: `CachedGitBackend::default_cache_path(store).with_file_name("history.db")`, giving `<project>/.aida/history.db` plus `-wal`/`-shm`, with the indexer lock at `.aida/history.db.lock`.
    - These files are gitignored by the existing deny-by-default `.aida/*` rule, so no .gitignore change is needed. The no-`.aida` fallback resolves to a sibling `.aida-store.history.db`, the same as the cache.db fallback.
    - It never lives inside `.aida-store/`, which would pollute the orphan branch. It is a separate SQLite file, so it has its own WAL and its own write lock. It never shares a connection or lock with cache.db.

    2. SCHEMA (HISTORY_SCHEMA_VERSION = 1, HISTORY_DECODER_VERSION = 1)
    - `meta(key PK, value)` holds: schema_version, decoder_version, store_root_sha (the store's identity), tip_sha (newest indexed commit), floor_sha (oldest indexed commit), complete (0/1: whether the floor has reached the root), built_at, updated_at, and indexer_pid/indexer_started_at (diagnostic only).
    - `commits(seq INTEGER PK, sha TEXT UNIQUE, commit_ts INTEGER, author_iso TEXT, git_author TEXT, subject TEXT)`.
      - seq keeps git-walk order. Catch-up rows get seq = max+1.., assigned oldest to newest. Backfill rows get seq = min-1...
      - commit_ts is the committer date in unix seconds, which is what `git log --since/--until` filters on. author_iso is the raw %aI value; `human_timestamp()` is applied at render time, never stored.
      - Every commit is recorded, including commits that touch no objects, so a `--max-commits N` window is reproducible as "the newest N rows by seq".
    - `events(id INTEGER PK, commit_seq INTEGER, ordinal INTEGER, sha TEXT, commit_ts INTEGER, spec_id TEXT, req_type TEXT, kind TEXT, status_from TEXT, status_to TEXT, author TEXT, is_ship INTEGER, is_comment INTEGER, is_meta INTEGER, kind_json TEXT)`.
      - ordinal preserves the event order within a commit.
      - author is the already-resolved `pick_author` value.
      - kind_json is a serde round-trip of `EventKind`, so rendering is byte-identical to the walk.
    - Indexes:
      - `(commit_seq DESC, ordinal)` for newest-first scans and the limit
      - `(commit_ts, commit_seq)` for since/until windows
      - partial `(commit_seq) WHERE is_ship=1` for `--shipped`
      - `(spec_id COLLATE NOCASE, commit_seq)` for `--id` and the progression view
      - `(req_type COLLATE NOCASE, commit_seq)`
      - `(kind, commit_seq)` for `--status-changes` and `--comments`
      - `(author, commit_seq)` for exact author matches. `--author` is a substring match today, so it runs as LIKE over rows the other indexes have already narrowed.
    - The archived and deferred spec sets stay as Rust post-filters on spec_id, applied while streaming rows before `take(limit)`. Those sets come from the caller, as they do today.

    3. INDEXING (the indexer takes a flock on history.db.lock, non-blocking)
    - Batched decoder: `git log --no-abbrev --raw --no-renames --diff-merges=cc --format=%H%x09%ct%x09%aI%x09%ae%x09%s <range>` plus one long-lived `git cat-file --batch` for blob contents. For a merge, the before blob is the first parent's OID, which matches today's `git show <merge>` (cc) plus `<sha>^` semantics. Each (commit, path, before, after) goes through the existing `decode_into_events`, so there is one decoder and no drift.
    - Catch-up: if tip_sha is an ancestor of HEAD, index `tip..HEAD` in one transaction and move tip_sha in the same transaction, so a crash leaves the db at the old tip and never half-indexed.
    - Backfill (first build, or after a reset): walk newest-first from floor_sha^ (or HEAD when the db is empty) in chunks of about 2,000 commits. Each chunk is one transaction that also moves floor_sha, which makes the backfill resumable. complete=1 once the root commit is indexed.
    - Truncate and restart the backfill when any of these hold:
      - tip_sha is missing or is not an ancestor of HEAD (force-push, rebase, `store compact --squash`)
      - store_root_sha differs (a different store now sits at this path)
      - schema_version or decoder_version differs. The decoder version is bumped whenever `decode_into_events`, `diff_modified` or `EventKind` changes meaning.
      - the file is corrupt (NotADatabase/Corrupt): delete it and its sidecars, as BUG-683 does.
    - Read-path budget: `AIDA_HISTORY_INDEX_BUDGET_MS` (default 1500) caps the inline indexing work per query. Explicit `aida cache rebuild --history` runs a full build with no budget.

    4. CONSISTENCY AND STALENESS
    - Each query reads `git_ops::head_sha(store)` once, catches up within the budget, then answers inside one SQLite read transaction (a WAL snapshot). The result is stamped with the tip it was served from.
    - Coverage rule: the cache answers only when it provably holds the whole answer. All three must hold:
      - (a) tip_sha == HEAD after catch-up
      - (b) one of: complete=1; or the since-bound predates the floor's commit_ts; or `limit` matches were found at or above the floor. The scan is newest-first, so those matches are exactly the newest ones.
      - (c) the `max_commits` window lies inside the indexed seq range
    - If any of these fail, the query falls back to the git walk (section 6). The cache never returns a partial answer.
    - Commits that git-canonical writes make to the store never touch history.db. The next reader sees HEAD moved and catches up. Staleness always comes from comparing HEAD shas, never from timestamps.

    5. CONCURRENCY
    - The history path never takes the BUG-1612 store write lock (`store_lock::acquire`). It only reads immutable objects by SHA plus one HEAD ref. If a writer races it, the indexer lands on the older HEAD, and the next query catches up.
    - It never opens cache.db and never writes the TASK-1484 `cache.db.lock-info` sidecar, so a cache rebuild, a long `aida edit` or a drain holding the query cache cannot slow history down, and history cannot slow them down. That is the point of the spec.
    - Indexer exclusion is `fs2` `try_lock_exclusive` on `history.db.lock`. The kernel releases it if the process crashes, so a stale-lock heal is not needed.
      - If another process holds it, this reader does not wait. It serves the committed snapshot when coverage holds, and otherwise falls back to the git walk.
      - SQLite runs with busy_timeout=0 and the short BUG-681-style ladder (50/100 ms). A reader never sits on the 25 s cache ladder.
    - History-db retries use their own ladder. `AIDA_CACHE_RETRY_*` stays scoped to cache.db.

    6. FAIL-OPEN READS
    - Any error falls back to the git walk: open, pragma, schema drift (`no such column/table`), a lock, disk full, a read-only filesystem, a decode error, or a coverage miss.
    - The fallback is today's `collect_filtered_events` body, renamed `collect_filtered_events_git` and left unchanged. It is also the oracle in the parity tests.
    - `AIDA_HISTORY_CACHE=0` disables the cache. Errors are logged once to stderr only under `AIDA_DEBUG`. For humans, a dim footer "(history index catching up — scanned git log)" appears only on a fallback. JSON and MCP output gain an additive field, `source: "history-cache"|"git-walk"`, plus `index_tip`.

    7. CONSUMERS AND EXPECTED SPEEDUPS (warm, tip == HEAD)
    Every caller of `collect_filtered_events` uses the cache.
    - `aida history events`, including `--since/--until/--type/--author/--status-changes/--comments/--oneline`: 7.3 s to under 100 ms
    - `--shipped`: 1-113 s to under 50 ms. It can now find the real last N ships (see OQ2).
    - `aida history <ID>` progression view and `--id --full`: already pathspec-scoped (TASK-1055), so the gain is about 10x on specs with many edits
    - `collect_event_records`, used by MCP `history` tool (about 19 s to under 100 ms) and `status_cmd::collect_absence_change_digest` (a 2,000-commit window, about 75 s worst case, to under 100 ms)
    - Catch-up costs about 1-3 ms per new commit. The one-time full build is estimated at 1-3 min for 37k commits with the batched decoder, against about 24 min with today's per-blob spawns.

    8. FILES / SYMBOLS
    - NEW `aida-cli-lib/src/history_cache.rs`. It sits next to its only decoder, so no aida-core API change is needed.
      - `HistoryCache::{open, path_for_store, ensure_fresh(budget), query(&HistoryOpts) -> Option<(Vec<Event>, usize)>, status() -> HistoryCacheStatus, rebuild_full}`
      - `index_range`, `BatchedBlobReader` (cat-file --batch), `Coverage`
      - `HISTORY_SCHEMA_VERSION`, `HISTORY_DECODER_VERSION`
    - `aida-cli-lib/src/history.rs`:
      - `EventKind` and `Event` become `pub(crate)` with `Serialize`/`Deserialize`
      - split `collect_filtered_events` into a cache-first wrapper plus `collect_filtered_events_git`
      - lift the filter chain into `fn apply_event_filters` so both paths share it
      - `run` and `run_single_spec_progress` print the fallback footer
    - `aida-core/src/git_ops.rs`: `root_commit(repo)` and `rev_list_count(repo, range)`. `head_sha` and `is_ancestor` are reused.
    - `aida-cli-lib/src/cli.rs`: `CacheCommand::Rebuild { history: bool }`. Update the match at lib.rs:14910 to fit.
    - `aida-cli-lib/src/cache_cmd.rs`: `Status` prints a History section (path, versions, tip vs HEAD, floor/complete, event count, updated_at, current indexer). `Rebuild --history` runs the full build.
    - `aida-cli-lib/src/doctor_cmd.rs`: an informational history-index freshness line in the Cache check. It never fails doctor.
    - `aida-cli-lib/src/mcp.rs` (history tool): the additive `source` and `index_tip` fields.
    - `aida-cli-lib/src/lib.rs`: `mod history_cache;`

    9. TESTS (aida-cli-lib/src/tests/task_1505_history_cache_tests.rs)
    - history_cache_parity_with_git_walk_all_filters: a fixture store with add/modify/delete/status/comment/tag/META plus a merge. The cache and the git walk must return equal `HistoryEventRecord`s for every filter combination and limit.
    - history_cache_merge_commit_decodes_like_git_show_cc
    - history_cache_max_commits_window_parity
    - history_cache_incremental_catch_up_from_tip
    - history_cache_rebuilds_on_non_ancestor_tip (amend or force-reset the store)
    - history_cache_rebuilds_after_store_compact_squash
    - history_cache_rebuilds_on_schema_or_decoder_version_bump
    - history_cache_rebuilds_on_foreign_store_root
    - history_cache_partial_backfill_serves_covered_window_only
    - history_cache_partial_backfill_falls_back_when_uncovered
    - history_cache_corrupt_file_self_heals_and_falls_back
    - history_cache_unwritable_dir_falls_back_to_git_walk
    - history_cache_ignores_requirements_cache_and_store_locks: another thread holds the cache.db write transaction, the lock-info sidecar and `store_lock::acquire`; the history query still finishes in under 1 s.
    - history_cache_concurrent_indexer_serves_snapshot_or_falls_back
    - history_cache_disabled_by_env
    - cache_status_reports_history_freshness
    - mcp_history_reports_source_field

    10. OUT OF SCOPE
    - Digest mode (`build_digest_rows`), which is already fast.
    - Code-repo `git log` walks (changelog, review, trace, pr).
    - Speeding up the fallback walk itself. It stays the unchanged oracle and could move to the batched decoder in a follow-up.
    - Background or scheduled warming of the index (a `schedule tick` hook).
    - Keeping pre-squash history after `store compact --squash`. The cache mirrors git and stays disposable.
    - Sharing one history.db across sibling worktrees.
    - Moving the decoder into aida-core.

    11. OPEN QUESTIONS FOR ADVISOR
    - OQ1 Location: per-project `.aida/history.db` (proposed), or per-store under the store's git-common-dir so sibling session worktrees share one index instead of each paying the first build?
    - OQ2 `--max-commits` semantics: keep the default window for byte-parity (proposed: cache honors the window exactly), or let the cache ignore the implicit 5x-limit default so that `--shipped -n 10` returns the true last 10? The second is a visible behavior fix and arguably a separate BUG.
    - OQ3 Ordering: the walk emits git's default commit-date order. The cache orders by seq, which is walk order at catch-up time. Does the advisor accept "equal on the linear fixture plus the merge fixture" as the parity bar, or should both paths switch to `--topo-order`?
    - OQ4 First build: inline with the 1.5 s budget plus resumable newest-first backfill (proposed), or only on explicit `aida cache rebuild --history` or a warm-up during `aida sync`?
    - OQ5 Should plain `aida cache rebuild` also reset history.db (simpler for users), or stay requirements-only (keeps the decoupling explicit)?
    - OQ6 Keep the module in aida-cli-lib (proposed, least churn), or move the decoder and cache into aida-core now so non-CLI surfaces can reuse them?
  created_at: 2026-09-25T08:10:31.430165381Z
  modified_at: 2026-09-25T08:10:31.430165381Z
  session_id: 67239b78-62f2-41d8-9600-1eea416800a6
- id: 01a0d7a0-dd88-7c60-afc3-722424267caf
  author: joe
  content: |
    ADVISOR SIGNOFF: APPROVED — with binding amendments A1-A10 below. Implement as two slices (see SLICING).

    VERIFICATION OF THE SKETCH'S CLAIMS (independent, 2026-09-25)
    - The git walk is as described. `collect_filtered_events` (history.rs:405) runs one `git log -n<max_commits>`, then one `git show --name-status` per commit and two `git show <rev>:<path>` per changed object. Its callers are `run` and the progression view, plus `collect_event_records` (used by MCP at mcp.rs:2535, which sets the window to max(5*limit, 500), and by status_cmd.rs:1067, which uses a 2,000-commit window). Confirmed.
    - The silent truncation is real. In events mode the default window is `(limit*5).max(50)` (git_backend_cmd.rs:7787), and `run` never reports that the window ran out. I re-ran `aida history events --shipped -n 10`: it took 0.77 s and returned 4 ships, with no notice.
    - Re-timed: `--since "1 day ago" -n 50` took 6.1 s (the sketch measured 7.3 s). `--shipped -n 10 --max-commits 300` took 4.9 s, about 16 ms per commit. The per-commit cost depends on how many objects each commit touches, so the sketch's 38 ms figure is plausible for its window. The order of magnitude holds. The store has 37,615 commits and 6 merges.
    - Lock and cache claims check out: busy_timeout=0 plus the BUG-681 short ladder, the BUG-683 corrupt-file self-heal, the `cache.db.lock-info` sidecar, the `AIDA_CACHE_RETRY_*` scoping, and fs2 `try_lock_exclusive` in store_lock.rs. fs2 and rusqlite are already dependencies of aida-cli-lib.

    ON THE DESIGN
    - A separate history.db is sound. It is derived and disposable, git stays canonical, and it shares no connection, WAL or lock with cache.db. Decoupling from the query cache is the right call.
    - The answer-only-when-complete rule with a fall back to the git walk is sound. So is never touching store_lock or cache.db. The history path reads immutable objects by SHA plus one ref. A racing writer only makes the cache one commit stale, and HEAD comparison catches that on the next query.
    - Incremental and rewrite handling are correct in principle: tip ancestry, root identity, version stamps and a truncate-and-restart on mismatch. The mechanics below must be tightened before this counts as correct with merges or a bounded budget.

    BINDING AMENDMENTS
    A1 (a new pre-req BUG: renames). The walk calls `git show --name-status` with rename detection on, which is git's default. On this store, `git log -n3000 --diff-filter=R` finds 426 R lines. For example, commit "chore: update 1 requirements, delete 1" pairs BUG-389 with BUG-390 at R076. The walk parses `R076\told\tnew` as one path containing a tab. Both blob reads fail, so the walk silently drops the Deleted event and the Added/Modified event. The sketch's batched decoder uses `--no-renames`, so the cache would disagree with its own oracle on every one of these commits. File a separate BUG to add `--no-renames` to the walk's `git show` (and to the digest's `git log --name-status`), with a regression test. Land it before or with slice 1. After that, both paths emit D+A and the parity oracle is honest.
    A2 (backfill resumption with merges). "Walk from floor_sha^" follows only the first parent, so it loses or duplicates side-branch commits after a merge. The same goes for "complete=1 once the root is indexed". Fix both:
      - Pin a `backfill_anchor` sha in meta when a backfill starts.
      - Enumerate commits deterministically with `git rev-list <anchor>`, in the same order options the cache uses for seq, and resume by an indexed-count offset or an exclusion set, never by `floor^`.
      - Set complete=1 when that enumeration is exhausted.
      - Use the sorted set of root commits (`rev-list --max-parents=0`) for `store_root_sha`, because a store can have more than one root after syncs.
      Add a test that resumes a chunk boundary in the middle of a merged side branch.
    A3 (budgeted catch-up). "tip..HEAD in one transaction" and a 1.5 s budget conflict when the gap is large, for example a sibling session far behind. Before indexing:
      - Count the gap with `rev-list --count tip..HEAD`.
      - If the gap fits the budget, index it in one transaction.
      - Otherwise index in chunks, oldest first, from `rev-list --reverse --topo-order tip..HEAD`. Only commit a chunk whose newest commit C satisfies `count(tip..C) == rows in the chunk prefix`, so the indexed set stays ancestor-closed and tip_sha stays meaningful. If no such boundary exists, fall back to the walk for this query.
      - Size backfill chunks by time, not a fixed 2,000 commits. At the sketch's own estimate, 2,000 commits is 3-10 s, well over the budget. Commit small transactions until the budget runs out.
    A4 (the `--id` window). With `--id`, git applies `-n<max_commits>` only to commits that touch that spec's path. Some of those commits decode to zero events, such as a modified_at-only edit. To make the window reproducible, record a per-commit touched-object row, `touches(commit_seq, spec_id)`, and define an `--id` window as the newest N touching commits. "Newest N rows by seq" is correct only without `--id`. Add `history_cache_id_window_parity`.
    A5 (the author filter). Today `--author` is a case-sensitive substring match (`e.author.contains(a)`). SQLite `LIKE` is case-insensitive for ASCII. Use `instr(author, ?) > 0` or a Rust post-filter. Do not use LIKE.
    A6 (the fallback path). `default_cache_path(store).with_file_name("history.db")` is wrong when there is no `.aida` directory. The cache fallback is `<root>.cache.db`, so with_file_name produces `<parent>/history.db`, a stray file in the project root. Add an explicit `history_db_path()` that mirrors `default_cache_path_with_roots` (including the BUG-1598 temp-root guard) and falls back to `git_root.with_extension("history.db")`.
    A7 (coverage accounting). Rule 4(b), "limit matches at or above the floor", must count matches after every filter, including the Rust post-filters for archived and deferred specs. It must also be checked together with rule (c), the window. Treat `--since`/`--until` as pure commit_ts filters. Document that git's `--since` walk-stop can differ under clock skew, and keep that case out of the byte-parity bar.
    A8 (measurement). The 1-3 min full-build figure is an estimate. The slice 1 PR must report a measured cold full build and a measured warm query on the real store: `--shipped -n 10`, `--since 1d -n 50`, and the MCP default. Record them on this spec.
    A9 (the fallback footer). Show it only on a fallback caused by a cache miss or error, never when `AIDA_HISTORY_CACHE=0`. Word it for users, with no internal nouns.
    A10 (decoder-version discipline). Add a unit test that fails when `EventKind`'s serde shape changes without a `HISTORY_DECODER_VERSION` bump. A snapshot of the serialized variants is enough.

    OPEN QUESTIONS — DECISIONS
    OQ1 Location: SHARED across sibling worktrees. Resolve the store path with `canonicalize()` before the `.aida` walk-up. Sibling session worktrees reach the canonical store through the `.aida-store` symlink, so they all land on the canonical project's `.aida/`. The content is a pure function of immutable commits, and the tip, root and version checks make sharing safe. Put the versions in the filename, `history-v<schema>-<decoder>.db` with a matching `.lock`, so a dev build with a bumped decoder and the released binary never reset each other's index back and forth. An unwritable shared directory, such as a sandboxed session, fails open to the walk (A6 still applies). `cache rebuild --history` may prune files for other versions.
    OQ2 Window: the cache HONOURS the window exactly in this task. It is the parity oracle, and a semantics change should not ride inside a performance change. The silent truncation is a real user-facing bug, so FILE IT SEPARATELY NOW. Its minimal fix works without the cache: when the walk exhausts an implicit (defaulted) window before finding `limit` matches, print a short notice that it searched only the most recent N changes and how to widen that, and add a `window_exhausted` flag to JSON/MCP. Once the cache ships, a follow-up on that BUG may drop the implicit window for cache-served queries.
    OQ3 Ordering: the parity bar is exact order equality on linear fixtures. On merge fixtures, require the same set of events and the same event order within each commit, and define the cache's seq order, which is topo-consistent, as authoritative. Do not switch the fallback to `--topo-order` in this task. With 6 merges in 37.6k commits the difference is theoretical, and changing the oracle's order would muddy parity. Document it.
    OQ4 First build: build inline within the budget, with a resumable newest-first backfill, as proposed and amended by A2 and A3. Rule 4(b) means recent-window queries go fast after the first few slices. Keep explicit `aida cache rebuild --history` as the full, unbudgeted build. Do not warm the index during `aida sync` in this task.
    OQ5: plain `aida cache rebuild` stays requirements-only, which keeps the decoupling explicit. It prints one hint line saying the history index rebuilds with `--history`. `cache status` shows both.
    OQ6 Crate: aida-cli-lib, as proposed. Nothing outside the CLI consumes it yet. Move it to aida-core only when a second consumer exists.

    SLICING (heft)
    The spec records heft 0. The work actually covers a new module, a batched decoder, backfill, catch-up and reset mechanics, CLI, doctor and MCP surfaces, and about 17 tests. Split it:
    - Pre-req BUG: A1, `--no-renames`. Small, and independent of this task.
    - Separate BUG: the OQ2 truncation notice. Small, and independent of this task.
    - Slice 1: `history_cache.rs` covering the schema, batched decoder, catch-up, backfill and reset (A2-A4), coverage (A7), fail-open, the env-off switch, the shared versioned location (OQ1 and A6), wiring into `collect_filtered_events`, `cache rebuild --history`, the `cache status` History section (this satisfies the diagnostics acceptance), and the parity, rewrite, corruption, lock-independence and window tests. Include the A8 measurements.
    - Slice 2: the doctor freshness line, the MCP `source`/`index_tip` fields, the fallback footer (A9), and `mcp_history_reports_source_field`.
    The trailing IDs on each slice PR must name the slice so the merge does not complete TASK-1505 early.
  created_at: 2026-09-25T08:13:59.048946359Z
  modified_at: 2026-09-25T08:13:59.048946359Z
  session_id: 67239b78-62f2-41d8-9600-1eea416800a6
filed_at:
  code_sha: dce27130ca391f4e587993afadb05b1447a6643f
  branch: main
  dirty: false
  aida_version: 0.15.0
  aida_build_sha: cb07ece7f0
  vendor: codex
