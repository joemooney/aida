id: 01a0379d-2608-7bc0-829d-6092d78e55c8
spec_id: STORY-781
agreed_id: STORY-781
title: Standard checked-in project manifest — .aida/project.toml scaffolded by init
description: |-
  AIDA scaffolds a project's requirements substrate, its skills and its agent
  config — but there is no standard, checked-in place for a project to say what
  it IS. Description, why it exists, liveness, lifecycle stage and owner are
  either absent, or duplicated ad-hoc into whatever consumer needs them.

  The immediate consumer is `aida-hub`, a catalogue that scans ~104 local
  repositories and answers "what is this, is it alive, how do I run it". Today
  the non-derivable half of that lives in a machine-local
  `~/.config/aida-hub/annotations.toml`, keyed by absolute path. That has two
  weaknesses a standard fixes:

  1. **Machine-local.** Clone the repo elsewhere and the notes are gone.
  2. **Absolute-path keys.** Move or rename the directory and the entry orphans.

  ## Why AIDA owns this, not the consumer

  Decided 2026-08-24 with the project owner.

  If `aida-hub` defined its own manifest format, every project would have to
  adopt aida-hub specifically in order to be well-described. AIDA is already the
  thing that bootstraps projects — `aida init` is a command these repositories
  already run — so the standard rides in on adoption that is already happening,
  and any future consumer (a TUI, a phone client, a `curl` one-liner) reads the
  same file.

  The consumer stays strictly READ-ONLY. This mirrors the stance aida-hub
  already takes toward `~/.ports`: the port manager writes it, aida-hub reads it.

  ## Proposed shape

  - A checked-in manifest, e.g. `.aida/project.toml`, carrying the facts a scan
    cannot derive: description, why it exists, liveness, lifecycle stage, owner,
    and any classification the author wants to assert.
  - `aida init` scaffolds it, pre-filled from what is already knowable
    (directory name, README lead, remote) rather than as an empty form — an
    empty form is how a metadata standard goes stale in a week.
  - `aida doctor` checks it: present, parseable, and not obviously stale.
  - Schema documented so a non-AIDA consumer can read it without reverse
    engineering.

  Consider whether this belongs as a sibling of STORY-780 (`aida init`
  bootstrapping a project from nothing) — the manifest is arguably part of what
  that bootstrap should be writing.

  ## DOWNSTREAM CONSUMER

  `aida-hub` STORY-21 ("Read the AIDA-owned per-repo project manifest") is
  blocked on this story. It covers the reader side only and cannot ship before
  this schema is agreed. Cross-repo: that spec lives in the `aida-hub` store,
  not this one.

  ## Acceptance

  - [ ] The manifest location and schema are defined and documented.
  - [ ] `aida init` writes one, pre-filled from derivable facts rather than blank.
  - [ ] `aida init --refresh` does not overwrite a human's edits, consistent with
        how scaffolded discipline docs already behave.
  - [ ] A project without a manifest keeps working; nothing about it regresses.
  - [ ] A malformed manifest is reported clearly and does not break any command.
  - [ ] `aida doctor` reports presence, parseability and staleness.
  - [ ] The schema is readable by a consumer that is not AIDA.
status: InProgress
priority: Medium
owner: ''
feature: Uncategorized
created_at: 2026-08-25T06:30:40.904764482Z
modified_at: 2026-08-25T06:41:54.900250192Z
req_type: Story
tags:
- aida:doctor
- aida:init
- metadata
- standard
relationships:
- rel_type: References
  target_id: 01a03035-b0ab-7eb0-8282-3b440fe932f8
  created_at: 2026-08-25T06:30:51.139152191Z
  created_by: joe
comments:
- id: 01a0379d-a6fe-7373-819b-6207974f5a8d
  author: joe
  content: 'Downstream consumer is aida-hub STORY-21 (''Read the AIDA-owned per-repo project manifest''), which is blocked on this spec. CROSS-REPO: that spec lives in the aida-hub store, not this one. A graph edge is NOT possible — spec IDs are per-store, and ''aida rel add --to STORY-21'' from here silently binds to this repo''s own rejected STORY-21 (Push AIDA Changes to GitLab). Keep the link as prose in both descriptions.'
  created_at: 2026-08-25T06:31:13.918936580Z
  modified_at: 2026-08-25T06:31:13.918936580Z
  session_id: ad63fa6f-6858-4163-aa31-d1a8d392e121
- id: 01a0379f-3431-7343-97a4-8cd8c9486e5b
  author: joe
  content: The cross-repo linking trap hit while filing this spec is now BUG-790 in this repo.
  created_at: 2026-08-25T06:32:55.601885982Z
  modified_at: 2026-08-25T06:32:55.601885982Z
  session_id: ad63fa6f-6858-4163-aa31-d1a8d392e121
- id: 01a037a0-cb8f-7db3-affa-d204591d8d94
  author: joe
  content: |-
    DECISION: ship INDEPENDENTLY of STORY-780, not bundled. Four reasons.

    1. DIFFERENT MATURITY. STORY-780's own description says "ACCEPTANCE CRITERIA: deliberately not yet
       written", and it still has open design questions (exact flag spelling, whether remote creation
       routes through the EPIC-35 forge abstraction, which language scaffolders to delegate to).
       STORY-781 has seven concrete acceptance criteria. Coupling a specified thing to an unspecified
       one delays the specified one indefinitely.

    2. DIFFERENT SCOPE OF APPLICABILITY -- this is the decisive one. STORY-780 is about creating a NEW
       project from nothing. STORY-781 has to serve EVERY project that runs `aida init`, including the
       ones that already exist and re-run init or `--refresh`. If the manifest shipped only as part of
       bootstrap, no existing project would ever get one, and the downstream catalogue would see it on
       approximately zero repositories. The manifest belongs to `init` proper.

    3. THE DEPENDENCY IS ONE-WAY. STORY-780 orchestrates a sequence that ENDS in `aida init`. If init
       writes the manifest, 780 inherits it for free the day it ships. The reverse is not true. So this
       is closer to a prerequisite than a sibling.

    4. THE DOWNSTREAM CONSUMER IS BLOCKED ON THE SCHEMA, NOT ON BOOTSTRAP. aida-hub's reader needs the
       file format agreed. Holding that behind a bootstrap command nobody has specified yet is a bad
       trade for the one consumer that exists.

    The Related edge to STORY-780 stays. When 780 is implemented it should call the same
    `ensure_project_manifest_scaffold` rather than writing its own copy.
  created_at: 2026-08-25T06:34:39.887427176Z
  modified_at: 2026-08-25T06:34:39.887427176Z
  session_id: 3653ac9c-29de-4f43-909c-f36e8f33e21d
