id: 019e5abd-6593-7ac3-8612-eed9695bb0c5
spec_id: STORY-460
agreed_id: STORY-460
title: Integrator role — delegate merge-cascade conflict resolution off the advisor
description: |
  ## Strategic problem

  With N parallel agents shipping concurrently, the merge-cascade workload grows O(N^2): every PR landing forces stale-base on all open PRs, each requiring a rebase + possible conflict resolution. Today the master advisor does this work, which means:

  - Advisor time is consumed by mechanical work (resolving import-list unions, formatter drift, etc.) that doesn't need design judgment
  - The strategic work the advisor SHOULD be doing (sketch verdicts, substrate growth, gap detection, multi-agent dispatch) is interrupted by integration churn
  - Scaling past 3-4 concurrent agents is bottlenecked at the advisor

  ## Proposal: a new 'integrator' role

  A fourth role alongside implementer / reviewer / dialog(advisor):

  **Scope IN**: rebasing PRs, resolving mechanical conflicts (whitespace, import unions, non-overlapping same-file additions, formatter drift), pushing rebased branches, watching CI, merging (squash) when CI-green-and-verdict-present, deleting merged branches, running aida pull post-merge, re-triggering stale CI.

  **Scope OUT (escalate to advisor)**: conflicts where two PRs make semantically different changes to the same lines, PRs lacking reviewer verdict (route to reviewer queue), real (non-flaky) test failures (route brief to original implementer), anything needing a design call.

  ## Smallest valuable slice (this STORY ships)

  1. Role definition in 'aida role list' — 'integrator' as a known role alongside the existing three
  2. Role-context markdown at 'aida-core/templates/roles/integrator.md' — the discipline above + concrete order-of-operations + escalation handshake
  3. Scaffolded by 'aida init' under '.aida/roles/integrator.md'
  4. 'aida queue add --for-role integrator' routing already works (queue knows arbitrary role names)
  5. Brief surface 'aida brief <agent> <SPEC> --role integrator' optional flag to inject the role-context into the brief note
  6. CLAUDE.md / discipline-pack section documenting the role

  ## Follow-up specs (separate)

  - **Integrator skill** ('aida-integrate' under .claude/skills/): the procedural recipe (list PRs, dependency-order them, rebase loop, escalation criteria). Could be a code-and-text skill where aida itself does the mechanical loop and the agent makes per-conflict decisions.
  - **'aida queue integrate'** command: walks open PRs in dependency order, dispatches a per-PR integration task to the integrator queue
  - **Per-agent dispatch** (STORY-459 + TASK-542 dependencies) — when those land, integrator briefs target specific agent instances

  ## Acceptance

  - [ ] 'aida role list' includes 'integrator'
  - [ ] 'aida role enter integrator' switches role with appropriate role-context loaded
  - [ ] 'aida-core/templates/roles/integrator.md' shipped as part of scaffolding
  - [ ] 'aida init' scaffolds '.aida/roles/integrator.md' alongside existing roles
  - [ ] CLAUDE.md template + discipline-pack documents the role
  - [ ] Test: launch agent into integrator role, confirm role-context loads, queue --for-role integrator routes correctly

  ## Composes with

  - STORY-459 (queue --for-agent — composes with --for-role integrator for specific dispatch)
  - TASK-542 (agent --name + auto-seq — integrator instances get stable names)
  - TASK-541 (brief --depends-on — integration tasks have explicit pickup order)
  - EPIC-31 (agent-lifecycle keystone — integrator launches via 'aida agent new <type> --role integrator')
  - feedback_one_master_advisor_until_subsystems — integrator is a load-balancing role, NOT a subsystem advisor; same advisor still has final say on escalations

  ## Origin

  2026-05-24 user during multi-agent MVP pace: 'with all this parallel work I am worried that merge conflicts will be a big job. I do not want to tie the advisor role up in this and would like it to be able to delegate work to a maybe integrator role.' Empirical evidence: in the last 24h, master has done 4+ multi-PR conflict resolutions during cascade events (PR-268, PR-272, PR-273, PR-267 sequence), each consuming 15-30min of advisor-time on mechanical work.

  trace:STORY-X

  ## ✅ Operator decision (2026-06-06 disposition sweep)

  DEFER with the EPIC-31 cluster — the integrator role identifier already validates (nothing broken); the remaining role-scaffolding infra (templates/roles/integrator.md + .aida/roles/ scaffold + discipline section) wants the same design pass as the EPIC-31 launcher/registry cluster, not a standalone bugs-phase ship.
status: Approved
priority: High
owner: ''
feature: Uncategorized
created_at: 2026-05-24T16:07:02.291738788Z
modified_at: 2026-06-07T22:42:13.561207451Z
req_type: Story
tags:
- advisor-discipline
- batch:multi-agent-coordination
- batch:substrate-architecture
- blocked
- deferred:post-stability
- from-user-direction
- load-balancing
- multi-agent
- role
- scaling-pattern
comments:
- id: 019e9c11-5098-7632-9c18-9a50f915a1f2
  author: joe
  content: |-
    🔵 DECISION REQUEST (disposition sweep 2026-06-06) — needs human input.
    Q1: The integrator role IDENTIFIER already exists (AGENT_ROLES, AgentRoleInfo, validate_registered_agent_role, --role integrator) but the spec's actual deliverables — role-context template, scaffolding, no `aida-core/templates/roles/` dir exists at all — are unbuilt. Multi-agent load-balancing is a strategic-cluster feature; project phase is bugs→stability→marketing. How to dispose?
      a) Ship the narrow remaining slice now: write `aida-core/templates/roles/integrator.md` + scaffold `.aida/roles/` via `aida init` + discipline-pack section (the only acceptance items not already met).
      b) Defer the whole STORY post-stability with the rest of the multi-agent cluster (EPIC-31/STORY-425); the role identifier already validates so nothing is broken meanwhile.
      c) Reject — the role-context file is low-value scaffolding now that the identifier + queue --for-role routing already work; the "integrator discipline" can live in the discipline-pack without a per-role template.
      REC: b — the role stub is harmless and the remaining work is real role-scaffolding infra that wants the same design pass as the EPIC-31 launcher/registry cluster, not a standalone bugs-phase ship.
  created_at: 2026-06-06T08:34:00.984753283Z
  modified_at: 2026-06-06T08:34:00.984753283Z
- id: 019e9d3c-7689-7110-86e2-f741c722bde7
  author: joe
  content: '✅ OPERATOR DECISION (2026-06-06): DEFER with the EPIC-31 cluster — the integrator role identifier already validates (nothing broken); the remaining role-scaffolding infra (templates/roles/integrator.md + .aida/roles/ scaffold + discipline section) wants the same design pass as the EPIC-31 launcher/registry cluster, not a standalone bugs-phase ship.'
  created_at: 2026-06-06T14:00:45.961093400Z
  modified_at: 2026-06-06T14:00:45.961093400Z
- id: 019ea0b4-9c10-7f13-82a1-f7f5c9d99b85
  author: joe
  content: 'PARKED for keyboard (autonomous burn-down 2026-06-06): Integrator role scaffolding — same compete-vs-harness call as STORY-519/520: the harness delivers integration (proven this session); defer the in-aida role build.'
  created_at: 2026-06-07T06:10:51.536841644Z
  modified_at: 2026-06-07T06:10:51.536841644Z
- id: 019ea435-e52a-7b60-9610-b867644abfda
  author: joe
  content: 'Design sketch (no impl) filed: docs/plans/2026-06-07-story-460-integrator-role-sketch.md. Honors the DEFER-with-EPIC-31 decision — shows how the integrator rides EPIC-31''s launcher/registry pass rather than shipping standalone. Covers: (1) role def — scope IN (rebase/mechanical-conflict/CI-watch/squash-merge clean+verdicted/branch-delete/pull) vs scope OUT (semantic conflicts, missing verdict, real test fails -> escalate); (2) scaffolding rides render_agent_launch_context -> role_guidance_for (which has NO integrator arm today, falls to generic ''other =>'' — gap to close); (3) escalation = existing file-based finding/brief handshake, punt-and-continue; (4) load-balancing role NOT subsystem advisor, one master keeps final say; (5) smallest slice + sign-off items. KEY TENSION flagged: STORY-460 acceptance says templates/roles/*.md but the real role substrate is TOML (.aida/roles/*.toml, RoleState) + the discipline pack — recommend re-spec to TOML-role + discipline-page + role_guidance_for arm rather than a new markdown role-file tree. Also: adding integrator to STARTER_ROLES flips the guarded starter_set_is_agent_wired_only invariant — needs sign-off (rec: opt-in until the active aida-integrate loop ships). Status unchanged.'
  created_at: 2026-06-07T22:30:56.042747527Z
  modified_at: 2026-06-07T22:30:56.042747527Z
