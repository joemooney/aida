id: 01a03035-b0ab-7eb0-8282-3b440fe932f8
spec_id: STORY-780
agreed_id: STORY-780
title: 'aida init: bootstrap a project from nothing (create dir + scaffold + remote) in one ordered command'
description: |-
  There is no supported path from "nothing" to "an initialized AIDA project". `aida init` assumes the
  caller is already standing inside a directory; `--git-init` covers "this folder is not a repo yet",
  but nothing creates the directory, scaffolds the language project, creates the remote, or pushes.

  Today a new project requires a hand-run sequence (mkdir, cargo/uv/npm scaffold, git init, first
  commit, gh repo create, push main, push aida-store, aida init) whose ORDERING is load-bearing and
  currently documented nowhere except scripts/aida-demo.sh and operator memory.

  WHY THE ORDERING MATTERS (the real motivation, not convenience):
  `aida init` creates the orphan `aida-store` branch. The two obvious naive sequences both fail:
    - `gh repo create --add-readme` then `aida init`  -> remote holds a commit the clone does not,
      so the user reconciles divergent history on day one.
    - `aida init` then `gh repo create`               -> two branches now need pushing; pushing them
      in the wrong order, or forgetting `aida-store` entirely, is a silent failure that surfaces
      much later as a missing store on a second clone.
  This is exactly the substrate-as-bouncer case: guarantee the invariant with a programmatic gate,
  not a paragraph in a doc that readers skip.

  SURFACE DECISION (settled with the operator 2026-08-23):
  Extend `aida init` -- do NOT take the top-level verb `aida new`. `new` is short, memorable, and has
  higher-value future claims (new spec, new agent, new role); spending it on project bootstrap is a
  bad trade. Exact flag spelling is open: `aida init --create <name>` is the straw man.

  SCOPE BOUNDARY (hold this line):
  AIDA owns the SEQUENCE and its ordering. AIDA does NOT own language templates. Language scaffolding
  delegates to the native tool -- cargo new / uv init / npm create. The moment AIDA ships its own
  Rust/Python/Node templates it owns keeping them current forever: long tail, low value, and it
  fattens the visible surface, which cuts directly against the Trojan-horse positioning in CLAUDE.md.
  Remote creation stays opt-in (--github / --gitlab) and should route through the EPIC-35 forge
  abstraction rather than adding another direct `gh` call site.

  ACCEPTANCE CRITERIA: deliberately not yet written. This spec was filed from live friction while
  bootstrapping the aida-hub project on 2026-08-23. The sequence is being hand-executed now and the
  exact commands run will be captured back here as the acceptance criteria, so the command is
  specified from a sequence that was actually walked rather than one that was imagined.
status: Approved
priority: Medium
owner: ''
feature: Uncategorized
created_at: 2026-08-23T20:00:20.139796022Z
modified_at: 2026-08-23T20:14:17.266012314Z
req_type: Story
tags:
- aida:init
- first-run
- onboarding
- papercut
relationships:
- rel_type: References
  target_id: 01a03042-2f76-7703-a8ae-19bbda7c9fc6
  created_at: 2026-08-23T20:14:17.266009235Z
  created_by: joe
comments:
- id: 01a03041-b2a9-7543-a615-f1c7f145123b
  author: joe
  content: |-
    LIVED SEQUENCE — captured while bootstrapping aida-hub, 2026-08-23. This is the walked path, not
    an imagined one; it should become the acceptance criteria.

    THE ORDERING THAT WORKS (no divergence, no missing store):
      1. scaffold the language project into a new dir   (delegate: cargo/uv/npm)
      2. git init -b main
      3. first commit
      4. gh repo create --private --source=. --remote=origin --push    <- remote created AFTER local
         history exists. This is the key move: nothing to reconcile, because the remote is born from
         the local repo rather than seeded with its own README commit.
      5. aida init            <- creates the orphan store AND auto-pushes origin/aida-store
      6. git push origin main <- REQUIRED: see finding 4 below

    FOUR TRAPS HIT, each one a requirement:

    1. LANGUAGE SCAFFOLDERS MAY BE TTY-ONLY WIZARDS.
       `cargo leptos new --git leptos-rs/start-axum --name aida-hub` fails with "IO error: not a
       terminal" when stdin is not a TTY. So "delegate to the native tool" is not sufficient on its
       own -- the delegation needs a non-interactive path, and must fail loudly rather than hang.
       Workaround used: clone the template repo directly, strip .git, resolve the cargo-generate
       placeholders ({{project-name}}, {{crate_name}}) and liquid conditionals by hand.

    2. PORT COLLISION IS A REAL STEP, NOT A DETAIL.
       The template defaulted to site-addr 127.0.0.1:3000, already claimed by another app in the
       machine-global registry, and reload-port 3001, claimed by a sibling project. Silent collision
       at first run. A bootstrap that scaffolds a web project should check the registry and claim a
       free port as part of the sequence.

    3. TEMPLATE LICENSES SILENTLY BECOME YOUR LICENSE.
       The template shipped an Unlicense (public-domain dedication). Copied verbatim, that declares
       the NEW project public domain -- almost certainly not intended, and easy to miss. Bootstrap
       should drop or explicitly re-ask for the license rather than inherit one.

    4. `aida init` PUSHES THE STORE BUT NOT MAIN — an asymmetry that strands work.
       init creates a "chore: scaffold AIDA" commit on main and pushes origin/aida-store, but leaves
       that main commit UNPUSHED. The user is left believing the push completed (the output reports
       the store push and nothing about main). A second clone at that moment gets the store but not
       the scaffolding. Either push both, or say plainly that main still needs pushing.
       NOTE: this one is independently fixable TODAY, ahead of the larger bootstrap command.

    SURFACE (settled 2026-08-23): fold into `aida init`; do NOT claim the top-level verb `aida new`,
    which has higher-value future uses (new spec / agent / role).
  created_at: 2026-08-23T20:13:27.081259661Z
  modified_at: 2026-08-23T20:13:27.081259661Z
  session_id: 3653ac9c-29de-4f43-909c-f36e8f33e21d
