id: 019f06b8-ec9e-7650-bc0e-f34cdaffc45a
spec_id: STORY-706
agreed_id: STORY-706
title: 'aida focus core: set/show/clear + scope reads + auto-link adds + loud visibility'
description: |-
  First slice of aida focus (the context/lens; workspace deferred). Build:
  - `aida focus <spec>` writes the focus to .aida/focus (per-worktree); `aida focus` shows current focus + progress rollup; `aida focus --clear` drops it. Precedence: AIDA_FOCUS env > .aida/focus > none (mirror the TUI focus resolution).
  - READ commands (list, queue list, search, history, findings) filter to the focus's transitive descendant tree (reuse aida_core::graph_walk::walk_union — the SPIKE-71-robust closure). `--all` / `--no-focus` per-command escape hatch.
  - WRITE: `aida add` links the new spec into the focus tree (bidirectional parent edge) — solves TASK-942.
  - LOUD VISIBILITY (the critical constraint): the statusline shows the active focus; focus-scoped command output carries a header like "▸ focused: EPIC-54 — showing 13 of 2400". Never silently subset.
  - The redesign TUI reads the same .aida/focus (unify with the existing .aida/tui-focus from STORY-697).

  ACCEPTANCE: aida focus set/show/clear works + persists per-worktree; read commands scope to the focus tree (with --all escape); aida add auto-links to focus; every focus-scoped output is clearly labeled; the TUI honors the shared focus.
status: InProgress
priority: Medium
owner: ''
feature: Uncategorized
created_at: 2026-06-27T01:36:50.334595154Z
modified_at: 2026-06-27T16:40:45.243848835Z
req_type: Story
tags:
- cli
- context
- focus
relationships:
- rel_type: Child
  target_id: 019f06b8-d0d7-7580-8bae-fbdf2204d81d
  created_at: 2026-06-27T01:36:57.943537840Z
  created_by: joe
comments:
- id: 019f06be-776e-71f0-9b83-04c7522438db
  author: joe
  content: 'OPERATOR INSIGHT (2026-06-26): focus must propagate to QUEUE operations, not just reads. Machinery already exists: ''aida queue add --scope <epic>'' restricts routing to sessions whose lease scope matches (auto-populated from session scope), and ''aida queue work EPIC-54'' drains the epic''s CLUSTER (its queued children via the parent edge). So NO redundant queue tag is needed — the scope + parent edge already encode it. THE GAP: queuing under a focus does not currently carry the scope — the TUI queue verb runs ''aida queue add --for implementer'' WITHOUT --scope, so the item lands in the general queue unprotected (a drain scoped elsewhere can sweep it). FIX (part of aida focus): under a focus, queue add gets --scope <focus-epic> and queue work scopes to the epic''s cluster — so focus-queued work is drained only in the epic''s context (protected from outside drains by the lease-scope restriction), with no extra tagging.'
  created_at: 2026-06-27T01:42:53.550545181Z
  modified_at: 2026-06-27T01:42:53.550545181Z
